# Scratchpad — inline session notes

**Append-only during a session. Processed and emptied during designer-triggered audit pass.**

See `workflow.md` → "DUAL-CADENCE OPERATING MODEL" for the protocol.

**Entry format (one line per correction-resolution turn):**

```
YYYY-MM-DD HH:mm — <component or file changed> — <what changed in 1 line> — Why: <one phrase>
```

**Watch-rules for the next audit pass (kept here as living reminders, NOT processed away):**

- Sibling-surface inheritance failures (any new surface that ships without a precedent grep)
- Token-threshold flags (any raw value spotted at 3+ uses without a token — note here, fix in audit)
- Build-broken catches (anything Vercel/typecheck would fail)
- Designer-explicit corrections vs implicit ones (explicit = decisions.md, implicit = might be taste.md)
- **S35 (refined at S34 second audit)**: Inline rule enumeration applies to ALL edits, including copy strings.
- **S35 — pre-flight for ANY copy edit**: read the surrounding surface (title + body + CTA) as one unit. Do any words repeat across the trio? Is each piece doing a distinct job (state / hint / action)?
- **S36 (refined at S34 THIRD audit)**: Audit pass must run TWO Gate 5 passes — Pass A (touched-surface sync) AND Pass B (codebase-wide grep sweep for stale references to deleted components, renamed entities, amended rules, new codified patterns).
- **S36 NEW from S35 audit** — Gate 2.2 sibling-survey BREADTH: survey ALL siblings, pick dominant pattern. Sample size 1 is inconclusive. Single-sibling sample = widen grep before adopting.
- **S36 NEW from S35 audit** — Pre-audit trigger: if 5+ scratchpad rows accumulate AND no audit has been triggered this session, trigger one VOLUNTARILY. Don't wait for the meta-question. The asking IS the failure (codified Gate 6 hard-fail).
- **S36 NEW from S35 audit** — Pre-flight rule enumeration applies to EVERY edit including: copy tweaks, eyebrow adds/drops, body lines, CTA orientation, viewport-aware decisions. Knowledge ≠ habit; codification ≠ application. Cite the rule in the scratchpad row inline at edit time.

---

## Pending audit entries

2026-05-18 — `AppCard.tsx` DELETED + `AppLinkButton.tsx` (NEW) — Why: designer call ("more like a button with icon text and external link"). Replaced static card visual (48px logo + name + bg-white-05 border, full-width row) with clickable anchor button: 32px logo + name flex-1 + ExternalLinkIcon (existing primitive at `ui/ExternalLinkIcon.tsx`, 14px size for this context) + `href + target="_blank" + rel="noopener noreferrer"`. Lighter visual weight (32px logo vs 48px, smaller padding `px-m py-s` vs `p-m`), hover-state via bg + border transitions, communicates "tap to leave for this app" intent via the trailing arrow icon. `POLYBUZZ_URL` + `TALKIE_URL` placeholders (https://www.polybuzz.ai + https://www.talkie-ai.com) — confirm with designer before launch. **Both consumers updated** (popup + details popup) — kept consistent per "same role + same viewport = same pattern" taste rule (line 193). **Generalization probe — STRONG candidate for taste.md:** *"External off-ramp surfaces (alternative apps, third-party providers, external integrations) read as buttons-with-external-link-icon, not as static cards. The arrow icon is a leave-this-app signal; the card pattern implies stay-and-browse. Use AppLinkButton-style chrome whenever the click destination is outside the current app."* Watch for 2nd domain usage (e.g. integration-suggestion cards, third-party-tool links) before promoting.

2026-05-18 — `WindDownPopup` Other apps section moved from above-CTA to bottom-of-popup with divider — Why: designer call ("a section at the bottom of the popup"). Section now sits BELOW the primary CTA + tertiary link, separated by `mt-l pt-l border-t border-white-10`. Reads as a "footer section" / off-ramp area — the primary read-flow is title → body → acknowledge → (optionally) explore alternatives below. The label `OTHER APPS TO TRY` is left-aligned via `text-left` on the container override (popup is otherwise items-center text-center). **Generalization probe — MEDIUM candidate:** *"Tertiary off-ramp options live below the primary action, separated by a divider — communicates 'this is supplementary, not part of the main message.' Sibling to the codified rule 'Metadata and manage links live with the what-just-happened group, not with the CTA' (line 309) — same idea applied to escape paths instead of metadata."* Watch for 2nd consumer.

2026-05-18 — **Top strip removed entirely; popup converted to render on both viewports** — Why: designer call ("we can remove the yellow banner at the top, we won't be using that, only this popup will come in both desktop and mobile"). **Major design pivot late in S35.** Files deleted: `WindDownTopStrip.tsx`. Files reverted: `Header.tsx` (top: var(--winddown-strip-height) → top-0; z-40 → z-50), `Sidebar.tsx` (top: calc(60px + var(...)) → top-[60px]), all 4 page main containers (`h-[calc(100vh-var(...)-60px)]` → `h-[calc(100vh-60px)]`), `SidebarSection.tsx` style guide doc, `tailwind.config.ts` (`header` token simplified: `calc(60px + var(...))` → `60px` — kept the token for semantic clarity). `WindDownPopup` converted to parallel-mount (BottomSheet + CenterPopup, S34 pattern) so it renders on both viewports. `WindDownNotice` orchestrator simplified: removed strip state, ResizeObserver, CSS-var publishing, sessionStorage strip-dismissed key, window event `wsup:winddown-popup-closed`. WindDownNoticeSection style guide updated: removed strip mockups + strip-related anatomy entries. **Gate 6.5 RULE INVALIDATION (Gate 6.5 outcome path #3 — "existing rule is wrong → delete it"):** 2 rules promoted earlier today are now obsolete: (a) knowledge-base.md *"Wind-Down Notice global system — 3-surface coordinated announcement pattern"* — was 3 surfaces, now 2 (strip removed); (b) taste.md *"Page-level critical notices degrade from popup-to-strip across exposure count and viewport"* — entire premise (degraded persistence) is gone; the new model is single-exposure popup. (c) knowledge-base.md *"Dynamic layout offset via CSS var"* — still a valid general pattern, but no longer in active use in WSUP; will downgrade to "documented but inactive". Updated in knowledge-base.md + taste.md + project-insights.md inline. **What the new model communicates:** single-exposure forced acknowledgment — user MUST tap "Okay, I understand" exactly once, then never sees the popup again. No persistent reminder chrome. This is a MORE aggressive once-and-done than the original popup-then-strip degradation. Trade-off: lighter chrome footprint, but loses the "keep reminding" behavior. Designer's call — they're betting the popup's force-acknowledgment is enough.

2026-05-18 — `AppCard.tsx` (NEW) — Gate 3 component extraction at 2 consumers — Why: `WindDownPopup` and `WindDownDetailsPopup` both now render Polybuzz + Talkie cards (designer call: "other apps to try also needs to come on the main winddownpopup"). Pattern: 48px logo + name in 2-column flex row, `bg-white-05 border border-white-10`, full-width. Extracted to `src/components/shared/AppCard.tsx` at the 2-consumer threshold per codified Gate 3 rule. Both popups import from shared location; style guide section keeps its own `AppCardMock` (emoji placeholder) for documentation independence. **Generalization probe:** transferable pattern? *External-app-suggestion card* could surface in future "alternatives" / "integrations" / "competitor-context" UIs. Light enough to extract; if 3rd consumer arrives with different chrome (e.g., adding URL/CTA per card), revisit anatomy.
2026-05-18 — `WindDownDetailsPopup` email footer — `sticky bottom-0 + bg-profile-sheet-bg + border-top` → flows naturally after content (no sticky positioning, no border-top) — Why: designer call ("this is now not sticky but after the content"). Reads as part of the message close, not a UI-anchored action block. Trade-off: user must scroll to bottom to see emails (no longer always-visible) but the popup is short enough that this is fine, AND the unstuck flow signals "this is part of what I'm telling you" not "this is an action footer." **Generalization probe (NOT promoted, watching):** *"Email contact / async-action paths at the END of an informational popup should flow naturally with content, not stick to the viewport. Sticky positioning communicates 'urgent action available'; flowing positioning communicates 'here's how to follow up after reading.'"* If pattern repeats, codify.
2026-05-18 — `WindDownPopup` + `WindDownDetailsPopup` + `WindDownNoticeSection` mockups — Major content/behavior pivot — Why: designer call to shift refund + data-download request paths from in-app action CTAs to email contact, reducing one-tap query volume during wind-down. **Popup changes:** primary CTA "Get my refund" → "Okay, I understand" (acknowledgment-only); "Download my data" secondary REMOVED; CloseButton REMOVED; scrim-click + Esc-key dismissal blocked by passing `onClose={() => {}}` to BottomSheet; "Read the full update" tertiary link kept. Interface refactored: `onClose/onRefund/onDownload` → `onAcknowledge`. **DetailsPopup changes:** exaggerated reasoning paragraph BACK after title ("The economics were broken from day one..."); both refund + download CTAs REMOVED from action footer; new "Other apps to try" section added with AppCard pattern (48px logo + name) showcasing Polybuzz + Talkie at `public/external/polybuzz.png` + `talkie.png`; action footer replaced with email contact copy (refund@wsup.ai + data@wsup.ai placeholders, mailto: links via .link utility). Interface refactored: removed `onRefund/onDownload` props. **WindDownNotice orchestrator** updated to match new popup interfaces. **Rules cited at edit time (S35 forcing function APPLIED):** (1) Exit-affordance uniqueness — popup has single dismissal path (CTA), rule literally compliant; (2) Action-labels-inherit-context — "Okay, I understand" is acknowledgment-aware; (3) Wind-down-phase acquisition-surface-hide rule (project-insights.md S35 codification) — removing refund/download action paths in favor of email contact extends the same intent. **New pattern flag (Gate 4 instance 1):** `AppCard` external-app-suggestion card (logo + name + bg-white-05 + border-white-10). Watch for 2nd consumer to formalize as shared component. **New rule candidate for taste.md (NOT promoted yet, watching for confirmation):** *"Forced acknowledgment for load-bearing announcements — when an announcement is critical AND time-bounded, disable scrim/Esc dismiss; require explicit CTA tap. Trade-off: friction for users vs reduced 'I didn't see this' support volume. Apply only when the support-volume calculus genuinely tips."* **New rule candidate for taste.md (NOT promoted yet):** *"During product deprecation, async action paths (email contact) beat in-app one-tap CTAs for refund/data-request flows — minimizes synchronous query volume."*

2026-05-18 — `explore/page.tsx` — `StreakClaimPopup` mount + `streakPopupOpen` + `streakLoginVariant` state + `windDownBlocking` gate plumbing removed. StreakClaimPopup import dropped. — Why: designer call ("we can now hide the daily check in popup that comes on explore page, no need to show it"). Applied codified wind-down-phase rule from project-insights.md (today): hide acquisition / upsell / engagement-amplification surfaces during wind-down — the "Claim your free credits" + "Need more? Explore other ways to earn" framing contradicts the shutdown message. The `windDownBlocking` gate became dead code since its sole purpose was gating StreakClaim — removed too. `wsup:winddown-popup-closed` event is still dispatched by WindDownNotice (harmless, available for any future listener). Rule citation: project-insights.md "Wind-Down phase rule — hide acquisition/upsell surfaces" (codified in same session). **S35 forcing function applied** — rule enumerated before edit, cited in this row inline at edit time.

_Below — previous audit close marker retained for reference._

_Empty — S35 audit pass closed (2026-05-18). 17 scratchpad rows promoted across decisions.md (10 new entries) + taste.md (4 new rules + 2 amendments) + knowledge-base.md (3 new entries) + project-insights.md (Wind-Down phase + surface inventory + acquisition-surface-hide rule) + workflow.md (Gate 2.2 sibling-survey breadth + audit-trigger habit refinement) + evolution.md (S35 entry with catch-by-catch table). Gate 5 ran both passes: Pass A synced WindDownNoticeSection style guide; Pass B caught + fixed SidebarSection's stale `top-[60px]` + 4 stale comments in source files. Build verified (tsc green + next build green; style-guide bundle 79.4 → 81.1 kB, +1.7 kB for new section). Designer triggered the Gate 6 meta-question — hard fail acknowledged, recurring failure mode logged in evolution.md as 3rd-straight-session pattern. Scratchpad clean._
