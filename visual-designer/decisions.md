# Visual Designer — Design Decisions
Last updated: 2026-05-15 (S34 second audit pass — DownloadDataSheet Images+Videos rows + ResultStep failure body copy fix + BuyCreditsResultStep sibling-correction applied; 3 more entries promoted; Gate 6 hard-fail trigger acknowledged — designer asked the meta-question AGAIN, which is the failure signal)

---

| S34 second audit — BuyCreditsResultStep + BuyCreditsSheetShowcase failure body "Your card wasn't charged. Try again or contact support." → "Your card wasn't charged. Contact support for help." | **Sibling-correction flagged at DownloadDataSheet body fix, applied at THIS audit pass.** Same CTA-verb-redundancy pattern: body said "Try again" while the CTA literally said "Try again". The "Try again or contact support" framing was doing path-enumeration, but the CTA already specifies the retry path — body only needs to add the alternative (support). Reworded to drop the redundancy while preserving the support path + card-not-charged reassurance. Also added text-balance to the style guide showcase (was missing). Cleaner 2-line wrap at 280px: "Your card wasn't charged." (24 chars) + "Contact support for help." (24 chars). **Generalization (taste.md already promoted — strengthens existing rule):** *"Action labels inherit context from the parent surface — don't repeat what's already specified" applies to result-step bodies too. Body explains state + offers ALTERNATIVE paths; CTA carries the primary path. Body should not repeat the CTA's verb.* This was the SAME catch I flagged for DownloadDataSheet — applying it consistently across the result-step family at audit. **Discipline note:** the original DownloadDataSheet failure body shipped with the same violation. I fixed it inline only after designer caught it. The codified rule has existed since S30. Gate 7 inline application gap — same as the S34 first-audit pattern of "7/9 catches violated already-codified rules."

| S34 second audit — DownloadDataSheet ResultStep failure body "Something went wrong. Try again or check your connection." → "Something went wrong. Check your connection." | **Designer-caught CTA-verb-redundancy + alternative-vs-sequential framing miss.** Original body had two issues stacked: (1) "Try again" verb repeated the CTA — violates action-labels-inherit-context rule; (2) "Try again OR check your connection" framed them as alternatives, but in reality "check connection" is a SEQUENTIAL prerequisite (check first, then retry) — violates copy-precision-match-product-state rule. New copy: title is past-tense outcome ("Couldn't download") → body is forward-action hint ("Check your connection") → CTA is the simplest path ("Try again"). Three discrete pieces, each doing one job. **S35 forcing function applied inline at FIX time — enumerated 3 rules touched before editing.** But forcing function didn't fire at WRITE time when the original copy was shipped — that's the inline-discipline gap.

| S34 second audit — DownloadDataSheet SelectStep + style guide section — added Images (156 · 38 MB) + Videos (12 · 480 MB) rows | **Designer feature extension.** 2 new state hooks (`pickImages`, `pickVideos`), 2 new `<Row>` instances in select step, reset-on-open extended, prop chain updated, canDownload OR's all 4. Row order encodes top-to-bottom natural flow: profile (Characters) → conversation transcripts (Chats) → conversation media light (Images) → conversation media heavy (Videos). Mock metadata reflects realistic size ratios (videos heaviest at 480 MB; characters lightest at 120 KB). Style guide mockup + anatomy text synced (2 rows → 4 rows + order rationale + size-ratio note). **S35 forcing function applied inline at write time — enumerated 4 rules touched, no violations.** This was the first edit after the S34 audit and the discipline kicked in: scratchpad row explicitly cited rule compliance, code shipped clean on first pass. The discipline WORKS when actually applied; the gap is remembering to apply it for every edit, not just the obvious ones.

---

| S34 audit — StatusResultIcon primitive extracted at `ui/StatusResultIcon.tsx` | **Gate 3 — 3 consumers (BuyCreditsResultStep, DownloadDataSheet ResultStep, DownloadDataSheetSection mockup) shipping identical chrome.** 72px circle with `size-[72px] rounded-full bg-status-X/[0.15] border border-status-X/[0.30]` + 32px ✓ glyph on success / 28px ✕ glyph on failure. Extracted once duplication crossed threshold; BuyCreditsResultStep also gained text-balance on its failure body para in the same pass (sibling-correction; it had the same max-w-[280px] orphan-wrap shape as DownloadDataSheet did before its own text-balance fix). Style guide section added in Components tab with both variants side-by-side. **Generalization (knowledge-base.md — promoted today):** *result-step status icon is a single primitive shared across all flows. Per-flow content (title, body, CTA) is local; the icon chrome is system-wide.* Success glyph at 32 vs failure at 28 because checkmark mass is concentrated to one side — needs the bump to read at parity weight with the evenly-distributed X.

| S34 audit — Onboarding rewind affordance added (DeckActionButtons + OnboardingDeckStep + useOnboardingFlow) | **Designer feature request — recovery path after a misclick / regret pass.** 52×52 icon-only circular button positioned BETWEEN Pass and Like (designer's call after first build had it left-of-Pass; "between" feels more pivotal / accessible to both thumbs). Neutral chrome (border-white-20 + text-text-body + bg-white-05) — no status color since rewind isn't success or alert; it's a reversal of intent. Only renders when `canRewind=true` (deckIndex > 0); STAYS mounted from first pass onward — no per-swipe pop (the gating-on-animation-state version was bouncing layout). First appearance fades in via `fade-in` keyframe (one transition, intentional). Brings back the previously-passed card via a symmetric "deck-rewind-in" CSS keyframe that mirrors the pass fly-off (translateX -600→0, rotate -30°→0, opacity 0→1, 320ms ease-out). Existing cards shift one slot back during the rewind animation. RewindGlyph uses lucide rotate-ccw shape (arc centered at viewBox midpoint — the first build had an off-center arc that got trimmed at the top). **Generalization (taste.md — promoted today):** *symmetric animations for symmetric actions — forward + backward operations should share an animation language (same distance, same rotation, same duration). Asymmetric animation = the actions read as different features; symmetric animation = the actions read as inverses of each other.*

| S34 audit — Onboarding empty-state CTA "See them again" → "Go to Explore" | **Designer call — at end-of-deck the user has finished the decision surface and exit IS the natural forward action.** onClick now routes through onSkipFlow (same destination as the header Skip pill, closes the onboarding overlay back to /explore). Rule-conflict surfaced (Gate 6.5): the codified "Exit-affordance uniqueness — don't duplicate the leave-this-surface path" rule (S32 follow-up) said the empty state should NOT have a "Go to Explore" CTA because the Skip pill already serves /explore. Designer override isn't a contradiction — it's a scope amendment: the rule applies to ACTIVE decision surfaces (where a duplicate exit dilutes the primary actions), not END-STATES (where exit IS the primary action). **Generalization (taste.md — promoted today, AMENDING the existing rule):** *"Exit-affordance uniqueness — don't duplicate the leave-this-surface path at the same trigger semantic" scope: applies to active decision surfaces, not end-states. At end-state, exit IS the natural primary action and surfacing it as a primary in-flow CTA is correct.* The header Skip pill remains the universal escape during the active deck; the end-state surfaces it primary because the user has earned the end.

| S34 audit — Ad flows extended to desktop (WatchAdBubble + WatchAdSheet + DummyAd) | **Designer call — both ad flows should work on mobile + desktop.** WatchAdBubble: removed `md:hidden`; switched `max-w-[300px]` → `max-w-chat-bubble` token (290px — was 10px drift from the codified bubble token, missed in S33 audit because the bubble was mobile-only then); wrapper padding `px-m` → `px-m md:px-4xl` to inherit ChatMessages container padding so the bubble's left edge aligns with regular AI bubbles on desktop. WatchAdSheet: restructured to mount BOTH BottomSheet (mobile) + CenterPopup (desktop) in parallel via a shared `<WatchAdBody>` (codified parallel-mount pattern matching BuyCreditsSheet). DummyAd: removed `md:hidden`; mounted two `<Image>` blocks (portrait `/dummy-ad.png` md:hidden + landscape `/dummy-ad-landscape.png` hidden md:block) — the asset orients to the available real estate. WatchAdGate dispatcher DELETED — split into separate mount points in page.tsx because the sheet (modal) needs to mount at page root for full-viewport scrim while the bubble (inline) has to live in the chat column. **Generalization (knowledge-base.md — promoted today):** *modal popup vs inline chat-stream component — mount-point separation rule. Modal popups (CenterPopup / BottomSheet variants) mount at PAGE ROOT (sibling of `<main>`) so their fixed-inset-0 scrim escapes any parent stacking context and covers Header + Sidebar. Inline chat-stream components (AI bubbles, banners, system messages) mount inside the chat column so they inherit its responsive width + flow. A single component that bundles BOTH variants is wrong by construction — split into two mount points.* **Sibling rule:** *when a single image asset doesn't compose well at both portrait + landscape, ship two assets and mount two `<Image>` blocks with the same viewport-hide convention.*

| S34 audit — Scrim opacity standardized `bg-black-55` → `bg-black-70` across all 7 modal scrims | **Designer call after overlay audit revealed dual conventions (6×black-55, 1×black-70, 1×black-50).** Audit findings: codified scrim was bg-black-55 (BottomSheet, CenterPopup, MemoryLimitOverlay, ConfirmSheet, LoginSheet, OnboardingOverlay all consistent + style guide explicitly named it). Two outliers: CreditSidebar at black-70, BadgeDetailPopup at black-50. Designer chose black-70 as the new standard for stronger separation from chat content on desktop where Header + Sidebar are visible behind the scrim and need to feel "behind glass," not just slightly tinted. Migration: BottomSheet, CenterPopup, MemoryLimitOverlay, ConfirmSheet, LoginSheet, OnboardingOverlay, BadgeDetailPopup all 55 → 70. CreditSidebar already at 70 (kept). Style guide labels updated (OverlaysSection / MemoryLimitPopupSection / OnboardingOverlaySection / WatchAdGateSection). Glass-chrome usages of bg-black-55 PRESERVED (VariantSwitcherPills pill) — different semantic (foreground chrome, not background dimming; softer wash is correct because the chrome is foreground). **Generalization (taste.md — promoted today, AMENDING):** *the WSUP scrim opacity is `bg-black-70` — strong enough to read as a clear "behind glass" state on desktop where Header + Sidebar are visible behind the scrim. `bg-black-55` is reserved for glass-chrome pills where a softer wash is correct because the chrome is FOREGROUND, not background dimming. Distinction: scrim = ancestor backdrop dimming source content; glass chrome = peer surface contrasting with same content.*

| S34 audit — DownloadDataSheet feature shipped (profile → "Download my data") | **Designer feature request — users need to export their data.** 3-step state machine (select → downloading → result). Select step: eyebrow "ACCOUNT DATA" + title + body + 2 checkbox rows (Characters/Chats) with full-row tap targets + count·size metadata chips; both unticked by default (CTA disabled until one is checked) — convention rejected: "default to all checked because user probably wants everything" — because the at-end state "I downloaded X" vs "wait I didn't mean Y" reads worse than the friction of one extra tap. Downloading step: animated spinner (Tailwind `animate-spin` on inline SVG circle+arc) + "Preparing your download…" + reassurance sub-line; auto-advances to result after 1.5s demo. Result step: StatusResultIcon + action-first title + body + single primary CTA. Variant switcher pill (Success/Failure) positioned top-right of popup chrome, ~10px above the top edge, OUTSIDE the card — sibling-inheritance from LoginSheet's custom-wrapper pattern (BottomSheet/CenterPopup primitives' overflow-hidden would clip the pill). Custom scrim wrapper used (BottomSheet/CenterPopup couldn't host the above-card pill). Style guide section + NAV.Patterns + PatternsTab registration. Multiple result-copy iterations: "Download ready" → "Download complete" (ready implies prepared not done — past-tense outcome matches BuyCreditsResultStep "Credits added" pattern); secondary "Cancel" link removed on failure (CloseButton already serves the exit path — exit-affordance uniqueness); body para got text-balance to fix orphan wrap ("check your connection" was splitting). **Generalization (taste.md — promoted today):** *multi-select destructive-adjacent actions default UNTICKED — primary CTA disabled until choice is made.* Avoids accidental "got the whole thing" click-throughs. Pairs with the existing "Block needs confirm" rule (both about adding friction to actions with persistent consequences). **Generalization (knowledge-base.md — promoted today):** *result-step CTA cardinality — single primary CTA only; the top-right CloseButton handles the give-up path.* **Generalization (knowledge-base.md — promoted today):** *custom scrim wrapper trigger conditions — use BottomSheet/CenterPopup primitives when content fits inside their overflow-hidden card; build a custom scrim wrapper (sibling-inheritance from LoginSheet / MemoryLimitOverlay / OnboardingOverlay / DownloadDataSheet) when the popup needs decorations that extend OUTSIDE the card chrome (variant pills, overhanging DPs, exclamation badges, etc.).*

| S34 audit — Copy: "Download ready" → "Download complete" | **Designer-caught product-state-precision miss.** "Ready" implies the download is PREPARED but not yet DELIVERED (user infers there's another action). The action has completed by the time the step renders. Sibling-inheritance with BuyCreditsResultStep's "Credits added" past-tense framing. Same family of catch as the S33 "is paid" vs "has retired" precision miss. **Generalization (taste.md — promoted today, STRENGTHENING the existing copy-product-state rule):** *for success-state copy on result steps, read the title aloud as a user trying to decide what to do next. If "ready" or "prepared" leaves them wondering "wait, do I need to click something?", the copy is wrong — switch to past-tense outcome ("complete", "saved", "added", "sent").*

| S34 audit — WatchAdBubble copy tighten ("Watch a short ad to keep chatting — your message will send right after." → "Watch a short ad and your message sends right after.") | **Designer call.** Drops redundant "to keep chatting" framing (the eyebrow "Quick ad break" already sets that context); active voice ("sends" vs "will send") feels more immediate; "and" conjunction reads more conversationally than em-dash split. Style-guide mockup + anatomy row synced. No generalization — single-instance copy choice.

| S34 audit — chat-config.ts stripped "(mobile only)" suffix from FLOW_LABELS for ad-bubble / ad-sheet + STATE_LABELS for watch-ad-popup | **Designer-caught stale-label miss.** S33 codified the rule "dev-panel state labels for mobile-only or desktop-only surfaces SHOULD carry the suffix" — when extending both ad flows to desktop, the suffix became stale and misleading. Should have been part of the same edit at desktop-extension time. ModelDeprecatedSheet's "(mobile only)" suffix KEPT because that surface is still mobile-only. **Codified discipline:** *when extending a surface's viewport coverage, the same edit MUST sweep any dev-panel labels with "(mobile only)" / "(desktop only)" suffixes referencing that surface — the labels and the surface live on the same edit-time semantic.*

| S33 end-of-day — `.eyebrow-label` utility extracted to globals.css (replaces 4 inlined copies of the same chrome string) | **Gate 3 + Gate 5 fix surfaced by designer's third Gate 6 meta-question of the day.** The eyebrow-label chrome (`text-xxs uppercase tracking-[0.4px] text-text-dim font-medium`) was inlined in 4 files: WatchAdBubble + ModelDeprecatedSheet (live components) + their style-guide mockups. Two live consumers = Gate 3 trigger. Added `.eyebrow-label` via `@apply` to globals.css; refactored all 4 sites; registered in UtilitiesSection with side-by-side `.label-xs` comparison (eyebrow is 40% opacity + 0.4px tracking, label-xs is 60% + 0.8px — distinct semantic roles). **Why CSS utility vs React component:** matches existing `.label-xs` / `.link` / `.glass` convention. Single-element typography utilities are CSS classes in WSUP; React components are for multi-element chrome. **Generalization (knowledge-base.md — promoted today):** *small single-element typography conventions (uppercase labels, eyebrows, captions) extract as CSS utility classes via `@apply` in globals.css, not React components. The existing `.label-xs` / `.link` / `.glass` family is the precedent. Component extraction (CharacterTagChip, VariantSwitcherPills) is reserved for multi-element chrome.* |

| S33 end-of-day — `ChatStyleSheet` accepts optional `selectedId` prop; initializes draft from it on open | **Real UX gap surfaced by audit pass after the ModelPickerSheet → ChatStyleSheet consolidation.** Mid-chat pill click was opening the picker with `draft = null` — user had to re-select their currently-active model just to confirm it. The original ModelPickerSheet had `selectedId={selectedModelId}` to pre-highlight; ChatStyleSheet inherited none of that when it became the unified picker. Fix: `selectedId` prop initializes draft on open + auto-jumps to the correct tier (if model is in 'other' tier, start at step='other' so the row is visible). New-chat trigger omits the prop → fresh-start picker preserved. Same picker, two init modes, no chrome difference. ChatStateOverlays prop signature gained `selectedModelId`; page.tsx passes it through. **Generalization (workflow.md — promoted today as new audit-pass step):** *when consolidating two surfaces with different commit semantics into one, audit the ENTRY POINTS individually. Each previous entry point had its own implicit state preferences. The consolidated surface must serve every entry point's needs, not just the one its original implementation matched.* |

| S33 end-of-day — Eyebrow label "Chat model update" added above ModelDeprecatedSheet title | **Designer-flagged UX concern.** Naive users who never opened LLM settings see "Llama 3" as alien jargon. Considered 3 approaches: (a) inline prose "the AI behind your chats", (b) inline "chat model" chip after each name, (c) eyebrow label above title. **Picked (c).** Reuses the WatchAdBubble "QUICK AD BREAK" eyebrow pattern. Three-layer hierarchy: eyebrow (category) → title (action) → body (specifics). By the time user reads "Llama 3" in body, they already know it's a model. Initial copy "Model update" → "Chat model update" after designer call (explicit category clarity; "model" alone could mean many things in 2026). **Generalization (taste.md — promoted today):** *for intervention popups whose subject (a model name, a feature flag, a system entity) might be unfamiliar to non-technical users, use an EYEBROW LABEL above the title to set category context rather than expanding the body prose. Three layers, three roles, no overlap. Sibling to "Living inside the conversation, not on top of it" — both about respecting user cognitive load.* |

| S33 end-of-day — Model-picker consolidation: ModelPickerSheet retired, ChatStyleSheet becomes the unified picker for all 3 entry points | **Designer call: "the chat llms popup that we open when we click on llm pill in chatbar in chat screen that is now being replaced with the model selection popup."** Three triggers now all route to ChatStyleSheet via `setChatState('chat-style-popup')`: (a) ChatHeader Switch LLMs, (b) ChatBar LLM pill tap, (c) ModelDeprecatedSheet "Pick a different model" link. Files deleted: `ModelPickerSheet.tsx`, `ModelPickerSheetSection.tsx`. page.tsx + ChatStateOverlays cleaned up (removed `modelPickerOpen` state + prop chain). Stale references swept across 13 files (comments + style-guide anatomy text in 7 sections). Historical context preserved where it had archival value. **Generalization (taste.md — promoted today):** *one picker per consequence-tier. When two surfaces serve the same choice ROLE (model selection here), unify them rather than maintain two pickers with different commit semantics. The "auto-commit on tap" vs "draft + Continue" split that previously distinguished ModelPickerSheet from ChatStyleSheet was friction the user didn't need to learn twice. Codified rule "Density should match the user's information needs at the moment of choice" applies — same choice intent → same surface, same density.* |

| S33 end-of-day — "Qwen Plus Character" → "Qwen Plus" rename across the codebase | **Designer call: "remove the word character after it."** Display name change in `lib/models.ts` auto-propagates everywhere via `getModel(id).name` consumers. Plus `ChatBar.tsx` default `selectedModelName` fallback prop value. Internal id `'qwen-plus-character'` preserved (kebab-case identifier, doesn't match the display string, no rename needed). Body copy in ModelDeprecatedSheet, ChatStyleSheet, toast on switch all reflect new name. Style guide anatomy text updated where the name was hardcoded for explanatory clarity. |

---

| S33 close — REVERTED unused `aboveSheet` slot on BottomSheet primitive (introduced earlier in session) |

---

| S33 close — REVERTED unused `aboveSheet` slot on BottomSheet primitive (introduced earlier in session) | **Audit-pass dead-code catch.** The slot was added when WatchAdSheet's variant pill needed to escape BottomSheet's overflow-hidden clip. Designer subsequently reverted the variant pill on WatchAdSheet (two-flow architecture replaced variant pill entirely). WatchAdSheet no longer uses aboveSheet — the slot has 0 consumers. Per the codified rule *"If you are certain that something is unused, you can delete it completely"*, reverted BottomSheet back to its pre-aboveSheet structure. **Generalization (already covered by the codified delete-unused-code rule):** when a primitive extension is justified for a single consumer and that consumer's usage is later removed, the extension becomes dead code — revert the primitive too, don't leave the unused slot for future hypothetical consumers. The audit pass IS the cleanup moment for these. |

| S33 close — DummyAd image-based placeholder (Homescapes ad screenshot at `public/dummy-ad.png`) replaces my earlier CSS-gradient placeholder | **Designer's better-instinct catch.** I had built ~80 lines of CSS-gradient placeholder with countdown timer + animated close X. Designer found a real Homescapes "How to Loot?" ad screenshot and proposed: just use the image fullscreen, tap-anywhere-to-close. ~30 lines of code, much more authentic. Implementation: `<Image fill object-contain>` inside a single giant `<button>` — entire surface IS the dismiss affordance. Matches the common mobile-ad pattern where the ad-content itself is the click-through to the App Store. **Generalization (taste.md — promoted today):** *for placeholder / stand-in components simulating a third-party SDK or external surface, prefer a real screenshot or asset over CSS-constructed approximations. Single screenshot conveys "this is X" instantly; CSS gradients require viewer to mentally translate. Closer to real = less guesswork in design review + dev handoff.* |

| S33 close — WatchAdGate ships TWO separate ad flows ('ad-bubble' / 'ad-sheet') instead of one ad flow with variant toggle pill | **Designer call after variant-pill version shipped briefly.** Initial design used a single 'ad' FlowMode + variant-switcher pill above the popup (designer toggles between sheet/bubble live). Designer pushed back: each ad UI deserves its own Flow option in the dev panel. Mutually exclusive demo journeys, each triggered by sending a message (real send-time gate). Removed variant pill + onVariantChange props entirely; WatchAdGate wrapper now takes a single `mode` prop derived from flowMode at the page level. **Generalization (taste.md — promoted today):** *when a flow has multiple UI treatments, prefer separate Flow options OVER a variant toggle inside one flow. Two flows = each is a complete demo journey (clean reset, real trigger). One flow with variants = adds switcher chrome to the surface + conflates "which UI am I testing" with "what is this surface for." Flow-level separation cleaner for designer review AND dev handoff.* Sibling to the codified "not every popup needs variant infrastructure" rule. |

| S33 close — WatchAdGate hard send-gate fires on Nth send (N=1 demo, N=9/16/24/32 production). Two surfaces — WatchAdSheet (BottomSheet modal) + WatchAdBubble (inline AI-bubble-style above ChatBar). Drives the DummyAd fullscreen takeover on tap | **Major new component family.** WatchAdGate wrapper dispatches to WatchAdSheet or WatchAdBubble based on `mode`. useWatchAdGate hook holds the full gate lifecycle: sentSinceLastAd counter + heldDraft + dummyAdOpen state + gateOnSend / recordSend / startWatchAd / completeWatchAd handlers. Send-flow integration: handleSend → `adGate.gateOnSend(text)` returns true if at threshold → draft held, popup fires → user taps "Watch ad" → popup closes + DummyAd opens → user taps DummyAd anywhere → ad closes + held draft sends. Style guide section "Watch Ad Gate" documents both flows with inline mockups + anatomy. **Sibling-inheritance:** sheet chrome matches ModelDeprecatedSheet (centered icon + title + body + primary CTA); bubble chrome matches ChatMessages.AIBubble (no avatar, asymmetric rounded corners, system-voice via "QUICK AD BREAK" eyebrow label). |

| S33 close — ModelDeprecatedSheet body copy: "is paid now" → "has retired" + "picks up where you left off" → "takes over" | **Designer-flagged product-semantics correction.** My first framing implied "Llama 3 still available if you pay" — wrong; the real product state is catalog removal (retirement), not tier change. "Has retired" is semantically accurate. "Takes over" (wrapped in `whitespace-nowrap` to prevent text-balance from splitting the verb-particle) is tighter than "picks up where you left off." **Generalization (taste.md — promoted today):** *copy must match the product state precisely, not approximately. Verb choice carries product semantics — "retired" (catalog removal) vs "paid" (tier change) vs "deprecated" (still-available-but-discouraged) are different states with different user implications. When uncertain about product state, ASK before writing — don't infer from the popup's job description.* |

| S33 close — `MAX_SENDS_BEFORE_AD_DEMO` 2 → 1 (after 1 free send, next attempt gates) + removed "Ad complete — message sent" toast from completeWatchAd | **Two designer-tuning calls.** N=1 for demo so the gate fires immediately after the user's first "hi" (no more 3-message warm-up). Toast removal because the held message landing in the chat IS its own confirmation. **Generalization (taste.md — promoted today):** *don't add a confirmation toast for actions where the OUTCOME is itself visible feedback. Toasts earn their slot when the action has no in-context visible result (e.g., a preference toggle with no immediate UI manifestation).* |

| S33 close — Watch Ad gate moved from dev-panel STATE section to FLOW section ('ad-bubble' / 'ad-sheet' Flow options) | **Designer call after first-pass build put it in STATE.** State entries are for instant-preview UI variants; ad-gate is a behavior (gate fires on Nth send), not a preview state. Selecting an ad flow resets the demo (same as new-user / returning) — designer must type+send to trigger. **Generalization (workflow.md territory — flagging next audit):** *dev-panel STATE entries are for "show me this UI now"; FLOW entries are for "run this demo journey." When in doubt: if selecting the entry should reset the chat to a starting point + trigger via real interaction, it's a flow. If it should jump straight to a UI, it's a state.* |

| S33 close — Bell-ring animation on ModelDeprecatedSheet announcement icon (damped oscillation, ~1.4s active + ~3.6s stillness, 5s cycle); Play-pulse animation on WatchAdSheet play icon (scale 1→1.08, opacity 0.9→1, 2s cycle) | **Designer-asked subtle animations.** Bell rings on an alarm semantic ("we have something to tell you"); play button pulses on an invitation semantic ("tap me"). Both use CSS keyframes in globals.css. Bell-ring pivots at `transform-origin: 50% 15%` so the swing reads as bell-pendulum, not generic spin. Play-pulse uses scale + opacity together so the icon feels "alive, ready" without aggressive motion. **Generalization (taste.md — promoted today):** *icon animation should encode the icon's INVITATION, not just liveliness — bells ring (alarm: "look at this"), play buttons pulse (invitation: "tap me"), heart icons can beat (affirmation: "select this"), refresh icons spin (working: "in progress"). Each animation primitive maps to a single semantic class.* Narrows the existing rule "animation should encode meaning, not just modernness." |

| S33 close — Verb-particle pair "takes over" wrapped in `<span className="whitespace-nowrap">` to prevent text-balance from splitting | **Surgical CSS fix on a known unbreakable unit.** text-balance distributes words evenly but doesn't know semantic grouping; verb-particle pairs ("take over", "pick up", "give up", "set up") + tight collocations ("worth meeting", "1 credit per reply") can split badly. **Generalization (taste.md — promoted today as extension of "Fix the wrap constraint, not the copy"):** *when text-balance is in use, wrap KNOWN unbreakable units (verb-particles, tight collocations) in `whitespace-nowrap` spans. Pre-flight check before shipping centered body copy: scan for "VERB + particle" pairs (take over, pick up, etc.) and wrap them; the algorithm will then break elsewhere.* |

| S33 close — Dev-panel state labels for mobile-only / desktop-only surfaces carry "(mobile only)" or "(desktop only)" suffix | **Designer call when WatchAd / ModelDeprecated states didn't show anything on desktop dev view.** Labels: "Llama 3 deprecated (mobile only)", "Watch ad (mobile only)" (later removed when moved to Flow). **Generalization (workflow.md — promoted today as a small convention):** *dev-panel state labels for mobile-only or desktop-only surfaces SHOULD carry "(mobile only)" or "(desktop only)" suffix so the dev panel doesn't read as "broken" when the toggle fires but nothing visibly changes on the current viewport.* |

| S33 close — `ChatStateOverlays.tsx` extracted from page.tsx — 5 chatState-driven popups co-located | **File-size discipline + semantic clustering.** WatchAdSheet addition pushed page.tsx to 304 lines (over 300 cap). The 5 popups (ChatStyleSheet, StreakClaimPopup, CreditServicePopup, ModelDeprecatedSheet, WatchAdSheet at that point) share the same trigger pattern (`open={chatState === 'X'}` + `onClose={setChatState('active')}`) — coherent semantic unit. Extracted with 6-prop bundle. **Generalization (knowledge-base.md — promoted today):** *when a state-machine-driven popup family grows to 4+ members, the popup mounts become a coherent extraction target. Each popup follows the same trigger pattern; the props-bundle cost is paid back by file-size headroom + co-location. Sibling to the earlier ChatBannerStack experiment which was reverted at 2 banners — at 4+ the abstraction earns its weight.* |

| S33 close — VariantSwitcherPills primitive extracted → reverted → re-extracted → currently single-consumer (LoginSheet only) | **Triple-churn caught at audit.** Was extracted, deleted when variant pill was reverted off ModelDeprecatedSheet, re-extracted when WatchAdGate variant pill was added, then variant pill was reverted off WatchAdGate too (two-flow architecture). Net: primitive file still exists with LoginSheet as the only consumer. **Decision at this audit:** LEAVE the primitive — reverting again is more churn than the value of the technically-premature extraction. LoginSheet uses it cleanly; if a future consumer wants a variant pill they have a clean primitive ready. **Watch item for S35:** if VariantSwitcherPills still has 1 consumer at next audit + no near-term plan for a second, dissolve it back into LoginSheet. **Generalization (workflow.md — flagging for next audit):** *primitive extractions for "imminent second consumer" should be done at the moment the second consumer is shipping, NOT speculatively. Reverting an extraction that the planned-second-consumer dropped is cleaner than letting it become permanent single-consumer architecture.* |

| S33 close — DummyAd image asset path: `public/dummy-ad.png` (Homescapes "How to Loot?" screenshot). Component references `/dummy-ad.png` | **Designer dropped the image into `public/` as PNG (not JPG as my placeholder code expected); component path updated to match.** Designer's actual file → component file → style guide doc all aligned. Production swaps the SDK call for this component; until then the static image is the visual handoff spec. |

---

| S33 post-audit — `<CharacterTagChip>` extracted to `components/shared/` after Gate 6 meta-question surfaced Gate 3 debt |

---

| S33 post-audit — `<CharacterTagChip>` extracted to `components/shared/` after Gate 6 meta-question surfaced Gate 3 debt | **Designer asked the Gate 6 meta-question** — *"is every little thing tokenised, componentised, in style guide?"* Per codified Gate 6 hard-fail trigger, *the asking IS the signal* that something wasn't surfaced proactively. Honest sweep found: same tag-chip chrome (`text-xxs font-normal px-xs py-[3px] rounded-pill bg-white-10 backdrop-blur-bg text-white-80 border border-white-10`) duplicated across **3 code files** (CharacterCard, DeckCard, ChatRightSidebar) + 2 style-guide showcases. Gate 3 threshold is 2; pre-existing debt, but my DeckCard tags touch today should have triggered Gate 3 inline. It didn't because my attention was on opacity, not on chrome duplication. **Extracted `<CharacterTagChip>` to `src/components/shared/CharacterTagChip.tsx`** with optional `className` passthrough (supports CharacterCard's responsive `hidden md:inline-block` cap on the 4th+ tag). Refactored all 3 code consumers + both style-guide showcases to consume the primitive. KB entry updated to point at the primitive (was inline chrome string). Style guide TagsCardsSection "Default" showcase now consumes `<CharacterTagChip>` directly with a callout noting "never re-create inline." Build green. **Generalization (workflow.md territory — but not promoting because Gate 3 already codifies this):** the Gate 3 inline rule WORKS — I just didn't run it. The fix is discipline, not new process. Watch item for S34: *every visual edit must run Gate 3 inline at touch-time, not at audit-time. The grep is part of the precedent check.* | 
| S33 post-audit — `text-balance` registered in TypographySection as "Line-break hygiene" subsection | **Same meta-question sweep.** Today I added `text-balance` (Tailwind utility setting CSS `text-wrap: balance`) to OnboardingDeckEmptyState as the fix for a bad line break, and promoted it as a taste rule. But the style guide didn't have an entry for it — dev finding the codebase would discover the utility on the empty state component, not in a registered place. Added "Line-break hygiene — fix the wrap, not the copy" subsection in TypographySection with: (a) `text-balance` entry (clause-balanced wrap), (b) `text-pretty` entry (last-line orphan prevention), (c) pre-flight check copy. Cross-references the OnboardingDeckEmptyState as a live consumer. **Generalization:** when a taste rule references a CSS technique, the technique must also appear in the style guide as a usable utility — taste.md is "why/when," style guide is "where to find it." Codified into workflow.md as part of audit-pass Gate 5: *"if a new taste rule cites a CSS utility / pattern as the technique, verify the utility has a style-guide entry. If not, add one in the same audit pass."* |
| S33 post-audit — OnboardingOverlaySection anatomy notes amended with CharacterTagChip + text-balance references | **Gate 5 follow-through.** The section's anatomy block didn't mention either today's net-new artifacts (the text-balance wrap behavior or the tag chip primitive). Added two anatomy bullets: (1) end-state subtitle's text-balance behavior with cross-link to Typography → Line-break hygiene; (2) tag chips consuming the shared `<CharacterTagChip>` primitive. Dev handoff is now complete — no implementation detail hidden in source-only. |

---

| S33 — DeckCard tags REVERTED from text-text-xsmall back to text-white-80 — Gate 2.2 + codified-knowledge-base contradiction caught by audit pass | **Audit-pass self-catch.** During the inline phase of the hierarchy audit, I changed DeckCard tags `text-white-80` → `text-text-xsmall` framing it as "restore name → desc → tags volume descent." The change shipped, looked OK in browser, designer accepted. **Audit pass cross-check against `knowledge-base.md` line 22 surfaced the contradiction:** codified KB explicitly states DeckCard tags use `text-white-80`, matching the explore CharacterCard sibling (which ALSO uses `text-white-80`). My framing was wrong — *tags on a character-preview card are primary personality-identification signal, not tertiary metadata*. CharacterCard at 80% is the validated convention. Reverted to text-white-80. **Why this matters as a meta-decision:** the dual-cadence audit pass earned its keep here — without it, the regression would have shipped silently because the inline gates (precedent grep + Gate 2.2) were not run against `knowledge-base.md`'s codified chrome rules. The audit's cross-check against codified anatomy IS the safety net. **Generalization (workflow.md territory — promoted today):** *"audit pass must Gate-2.2-cross-check every visual edit against `knowledge-base.md` codified anatomy entries, not just against sibling component files. KB IS the canonical record of what convention should be."* — added to workflow.md as an audit-pass step. |
| S33 — OnboardingDeckEmptyState subtitle: dropped `max-w-[300px]` container constraint + added `text-balance` to the `<p>` (copy unchanged: "Want another look, or should we show you more?") | **Designer's better-instinct catch.** First-pass fix was to rewrite copy ("More matches, or another look?"). Designer pushed back: *"why not just fix the line break instead of changing copy? do it early in mobile and make it a single line in desktop"* — exactly right. The copy was fine; the container constraint and lack of CSS wrap-balancing were the problem. **Fix:** removed the 300px max-width from the title+subtitle container (title is short, doesn't need it), added Tailwind's `text-balance` to the subtitle `<p>` (sets CSS `text-wrap: balance`, which distributes words evenly across lines). Result: desktop single-line, mobile breaks at the clause boundary (comma) instead of orphaning "more?". **Generalization (taste.md — promoted today):** *"When a line break reads bad, first try fixing the WRAP CONSTRAINT (max-width + `text-balance`/`text-pretty`), not the COPY. Copy carries voice and intent — preserve it. The container is the easier lever and usually the actual cause. Copy rewrite is a fallback when CSS can't reach a clean break (e.g., when single-word orphans persist or when the surface's geometry mandates a specific char-count)."* Pairs with the existing taste rule *"Avoid `<br />` in headlines — write copy short enough to wrap naturally"* — that rule's failure-mode is to immediately tighten copy; this rule corrects toward "container first, copy second." |
| S33 — OnboardingDeckStep subtitle "Swipe right to start chatting · left to skip" → "Swipe right to chat · left to pass" | **Designer-flagged audit.** Two issues fixed: (1) **button-label parity** — subtitle taught "skip" but button label says "Pass" (verb mismatch teaches the wrong word); (2) **line-break safety** — original 47-char string sat at the edge of mobile single-line; "Swipe right to start chatting" is verbose mechanic-teaching. New shorter form fits single-line at both viewports. **Generalization (taste.md — promoted today):** *"When a subtitle teaches a gesture or action, the VERB in the subtitle must match the VERB on the action button. Mismatch ('skip' in subtitle / 'Pass' on button) teaches the user the wrong word for the action they'll see executed. Verbs are the contract — they must agree across teaching surface and action surface."* Sibling to the codified rule *"Action labels inherit context from the parent surface"* — both about verb consistency, this one is about verb agreement across the same surface. |
| S33 — OnboardingPreferencesStep subtitle: "A few quick questions and we'll show you characters worth meeting." → "Quick questions before we show you matches." | **Designer-flagged audit.** Original copy had bad line breaks at both viewports — desktop popup split *"worth | meeting"* mid-collocation; mobile orphaned *"worth meeting"* from *"characters"* (modifier from noun). Shorter copy + tighter noun phrase eliminates the break entirely on desktop popup (480px) and gives mobile (414px) clean wrap behavior. The change does NOT repeat "find your match" from the title (DRY-safe). Preserved the "we'll show you" framing. **Generalization (already covered by taste rule "Avoid `<br />` in headlines — write copy short enough to wrap naturally"):** extended in scope today to cover subtitles too, not just headlines. The codified rule's intent — *don't force breaks; size copy to container* — applies anywhere container width is variable. |
| S33 — OnboardingPreferencesStep Step 1 title `text-3xl` → `text-2xl` | **Designer-flagged audit + sibling-surface inheritance.** Grepped `text-3xl` usages in WSUP — found 6: style guide root h1, "About wsup.ai", FAQ heading, home dev index, and Step 1. All others are **landing/marketing-tier surfaces** (long-scroll heroes). `text-2xl` is the **popup-headline scale** (LoginSheet — the canonical sibling — uses `font-semibold text-2xl leading-[1.25]`; Step 2 onboarding title at `text-2xl`; OnboardingDeckEmptyState at `text-2xl`). Step 1 was the lone outlier — popup chrome with marketing-scale title. **Three benefits of the change:** (a) Gate 2.2 sibling-surface compliance with LoginSheet, (b) within-flow harmony — both onboarding step titles now read at the same scale, (c) gives Stage 1's form more vertical breathing room. **Generalization (already covered by Gate 2.2):** *intervention-popup-class surfaces (LoginSheet, BuyCreditsSheet, MemoryLimitPopup, onboarding stages) take `text-2xl` headlines, not `text-3xl`. `text-3xl` is reserved for marketing/landing surfaces.* Pre-flight check: when sizing a popup headline, grep both `text-3xl` AND `text-2xl` in style-guide popup sections — match what the popup family uses, not what reads "big enough." |
| S33 — DeckCard meta line `text-xs text-text-body` → `text-xxs text-text-small` | **Designer-flagged audit.** Original meta line ("male · age 29") rendered at the same size + opacity as the description below it — Gate 8.3 hierarchy red flag. Both were `text-xs text-text-body` (14px / 70%). Description is a *readable sentence*; meta is *supporting demographics*. Same visual weight = both shout equally. Fixed by demoting meta to `text-xxs text-text-small` (12px / 60%) — meta now reads as supporting metadata, description retains body-copy weight. Volume descent: name (100%) → description (70%) → meta (60%) → tags (80% — corrected at audit, see separate row). **Sibling check:** CharacterCard's stats line uses `text-text-xsmall text-xxs` (50%) — meta at 60% is slightly brighter; justified because meta is *demographic identity* (closer to name semantically) while CharacterCard stats are *quantitative metadata* (rank, chats — purely numeric). 60% is the right opacity for "small but human-meaningful" data. Divergence from sibling documented; would converge if a future card design unifies meta + stats. |

Every design decision and its reasoning. New decisions are appended at the top of the active list. **Active set above the Archive divider; pre-S29 history below.** Look in `taste.md` / `knowledge-base.md` / `reasonings.md` for the codified rule a decision was promoted to — those files are the live ruleset, this file is the per-edit moment record.

---

| Decision | Reasoning |
|---|---|
| S32 follow-up #3 — Stage 1 gains a third picker: "I am" (Female / Male / Non-binary); placed FIRST in the form (identity → demographic → preference); Continue disabled until all three picked | **Designer ask.** Previous shape: Age + I'm interested in. New shape: I am → Age → I'm interested in. **Ordering rationale:** identity first matches dating-app convention — *who am I* before *who I want to meet*. The user defines themselves first, then describes the world they want to meet. Putting it last would force the user to backtrack mentally ("oh and by the way, I'm a..."). **Options:** Female / Male / Non-binary. Aligned with the user's stated wording ("Non Binary"); used industry-standard hyphenation. Three options keep visual parity with the existing "I'm interested in" row (Female / Male / No preference) — same width budget, same wrap behavior. **Subtitle copy adjusted:** "Two quick questions" → "A few quick questions" (numeric drift; the explicit-count framing breaks if we add a fourth picker later). **Type propagation:** `OnboardingPreferencesStep` prop signature `{ age, interest } → { identity, age, interest }`; `OnboardingOverlay` + `useOnboardingFlow` consumer types updated in same edit (Gate 5 sync, type-level). Preferences are still demo-only — we don't filter the deck by any of them. **Generalization (taste rule territory):** *form fields ordered by what-you-need-to-fill-FIRST-mentally, not what's-easier-to-collect — identity questions before demographic before preference. The user's mental model is "I am X looking for Y at age Z" → most pickers solve a "Y" or "Z" need; identity needs to be set before either makes sense to commit to.* Sibling to the codified rule about action-first headlines on intervention popups — both are about ordering UI to match the user's mental sequence, not the system's collection sequence. |
| S32 follow-up #3 — Deck end-state ("That's the deck.") with Show me more / See them again CTAs; stacked-cards illustration; Skip-not-duplicated rule | **Designer ask.** Previously the deck flow auto-closed at end-of-deck — user fell off into the void. New: when `index >= ONBOARDING_DECK.length` (i.e. user skipped every card without a like), `OnboardingDeckStep` renders `OnboardingDeckEmptyState` instead of returning null. Component: dim stacked-cards SVG illustration (60% white outline, 80×88 viewBox, two rounded rects offset diagonally) + heading "That's the deck." + subtitle "Want another look, or should we show you more?" + two CTAs. **CTA copy iterations (3 rounds):** my first take was "Run it back" / "Open Explore"; designer caught the duplication (Skip pill already serves /explore — same destination, same intent, two affordances). Removed "Open Explore." Then designer flagged "Run it back" as slang too casual for first-time-user comprehension — codified `taste.md` rule says *"name the user's desired action"* and `Run it back` doesn't *describe* the action; it's idiom. Switched to descriptive **`Show me more`** (primary — since skips signal "these didn't match") + **`See them again`** (secondary — regret-a-skip). **Demo wiring:** both CTAs reset deck index to 0 (we re-cycle the 9-card deck because this is design handoff; dev wires the actual split). **Component-API split for handoff clarity:** `OnboardingDeckEmptyState` accepts `onShowMore` and `onSeeAgain` as separate props (both wired to `onRestartDeck` in demo) — the prop names + inline comment tell dev which CTA wires to "next batch" vs "replay current batch." **`useOnboardingFlow.skipCard` simplified:** removed the auto-`closeFlow` at end-of-deck — now just advances index past length, letting the deck step's empty-state branch render. New `restartDeck()` action that sets index back to 0. **Generalization (taste rule territory):** *don't duplicate exit affordances at the same trigger semantic — Skip and "Open Explore" both being "leave to /explore" violated the codified passive-vs-active separation; "Open Explore" had no distinct intent.* Promoted to `taste.md` as the "exit-affordance uniqueness" sibling to existing rules. |
| S32 follow-up #3 — Deck-stack peek behavior: 3 cards rendered at once (top + 2 peeks); coordinated commit animation; peek-promotes-to-top via key continuity | **Designer ask after first single-card swiper.** Single-card behavior had each card "respawn from nowhere" after swipe — broken physical-deck mental model. New: `OnboardingDeckStep` renders `ONBOARDING_DECK.slice(index, index + 3)` simultaneously, absolute-positioned in a shared aspect-locked container. Slot transforms: slot 0 = no transform (top), slot 1 = `translateY(10px) scale(0.95)` opacity 0.7, slot 2 = `translateY(20px) scale(0.90)` opacity 0.45. **DeckCardSwiper extended with `interactive` prop:** top card has full drag + commit + tint pipeline; peeks render `<DeckCard>` with no interaction, no transforms, no tint. **DeckCardSwiper extended with `onCommitStart` callback:** fires the moment a commit begins (button / kbd / drag-past-threshold). Parent uses this to set `swipingOut: true` which shifts peek effSlot by -1 — peek cards animate UP to their next-slot positions IN PARALLEL with the top's fly-off. One coordinated motion, not a respawn. **Reconciliation by character.id:** each card keyed by `character.id` so React keeps the underlying instance mounted when its position in the slice changes (peek-promotes-to-top). DeckCardSwiper renders the same wrapper structure for both interactive=true/false so swap doesn't cause unmount/remount blink. After commit, parent advances index → `swipingOut` resets via useEffect → next render: old slot 0 unmounts (no longer in slice), old slot 1 already at "top transform" (because effSlot was 0 during swipingOut) → continuity preserved. **Generalization (taste rule territory):** *deck-of-cards or carousel UI surfaces must show that there are more items behind/ahead; new items should appear to "rise from the deck" not "respawn from nowhere" — physical-stack metaphor demands persistent visible stack plus coordinated handoff motion.* Promoted to taste.md as a swipe/carousel rule. |
| S32 follow-up #3 — Opening bubble: typing-dots reveal → typewriter character-by-character; tags row moved above bubble (bubble closest to bottom) | **Designer ask for "more interactive and immersive."** Two layered changes: (1) **layout reorder** — overlaid content was `name/meta/desc → bubble → tags`; now `name/meta/desc → tags → bubble`. Rationale: the bubble's tail corner is at bottom-left (flat radius); placing the bubble at the very bottom of the overlay puts its tail visually closest to the card's bottom edge — reinforces the "speech bubble from the character" metaphor. Tags row moves up to act as the rhythmic separator between description and bubble. (2) **typing reveal sequence** — when a card becomes active (top + interactive), bubble starts hidden. After 1000ms, bubble pops in showing 3 animated typing dots (same dots-pattern as `ChatMessages.TypingIndicator`). After 1400ms more, dots are replaced with the message; the message reveals character-by-character at 25ms/char (typewriter). Peek cards behind the top don't render the bubble at all — the bubble is the "this card is active" signal, not just decoration. **`DeckCard` gains `animate` prop:** passed `animate={interactive}` from DeckCardSwiper. Animation re-runs on every `animate=true` transition (initial mount as top, OR peek-promotes-to-top after a swipe) so each new top card gets its own typing reveal. **Generalization (taste rule territory):** *bubble-as-character-speech UI should reveal sequentially (dots → typewriter), not appear all-at-once. The sequential reveal turns a static card into a live moment — matches the codified "character lives the limit; system offers the path" rule (voice scales with immersion).* Sibling to the codified rule about character voice in MemoryLimitPopup. |
| S32 follow-up #3 — Stage 1 popup: stage-aware height (preferences = content-driven auto, deck = min(880,94vh)) | **Designer caught dead space.** Initial implementation gave the popup a fixed `min(880px, 94vh)` height for both stages. Stage 1's actual content is ~500px tall — leaving ~380px of empty space between the Continue CTA and the rest. Designer's screenshot showed a comically tall popup. Fixed: `OnboardingOverlay` now reads the `stage` prop and applies `height: 'auto'` (with `maxHeight: '94vh'`) for preferences, and `height: 'min(880px, 94vh)'` for deck. Result: preferences popup sizes to content, deck popup stays tall for the card stack. **Generalization (already covered by taste rule "Spacing is hierarchy"):** *modal/popup heights should match the content's needs at the moment, not a one-size-fits-all default. Fixed heights pay off when content varies in vertical demand across states (e.g., a multi-stage flow with one step short and another step tall).* |
| S32 follow-up #3 — DeckCard rewrite: explore-style anatomy (image fills card, content overlays); per-category gradient badge with glyph; tighter typography; aspect derived from parent height | **Multiple-round designer iteration on the card.** Starting state: image-on-top + body-below layout (2-zone card), aspect-[4/5] image, italic description, "WHAT THEY OPEN WITH" label + opening bubble + tags. Designer asked for explore-CharacterCard parity: image fills the whole card, content overlays the bottom with gradient scrim. **Rewrite:** single root with `relative rounded-card overflow-hidden ring-1 ring-white-10`. Image fills via `<Image fill object-cover>`. Bottom gradient scrim `bg-gradient-to-t from-black via-black-60 via-[42%] to-transparent to-[60%]`. Bottom-anchored content overlays the bottom 40% of the image. **Per-category badge:** dropped neutral white-10 chip in favor of per-category `linear-gradient` backgrounds + per-category SVG glyph. Mapping: Mafia (dark red, spade), Romance (pink, heart), Teacher (green, book), Anime (purple, sparkle), Mentor (amber, bolt), Bold (red, flame), Friend (teal, smile), Sci-Fi (blue, orbit). Local glyph component fns keep the per-category dictionary inline. **Removed:** "WHAT THEY OPEN WITH" preamble label (the bubble itself signals "character speaking"); italic styling on description (matches explore CharacterCard's non-italic). **Sizing — final:** card aspect locked at `aspect-[9/16]` on a parent that owns the bounds. DeckCardSwiper Wrapper takes `h-full max-w-full aspect-[9/16]` — fills the parent flex region (height-driven), width auto-computes. No fixed `max-h` so the card scales with viewport. Card inside fills via `w-full h-full`. **Generalization (already covered by Gate 2):** *when an existing surface family (explore CharacterCard) has the anatomy a new related surface needs, inherit the anatomy — don't re-invent a parallel pattern. The new surface lands with built-in user familiarity.* |
| S32 follow-up #3 — Action buttons widened to icon+text pills; round 56×56 buttons replaced; keycap hints stay removed | **Designer ask.** Round outline-only `× / ♥` buttons read as utility/toolbar affordances, not as primary deliberate actions. Replaced with wider pill buttons: each flex-1 (50/50 split of the row width), 52px tall, icon + label (`× Pass` / `♥ Like`), border + low-opacity status-color fill. Tinder-style "I am committing to this character" weight. **Why outline + low-fill (not full-fill primary):** the card behind the buttons IS the focus; full-fill primary buttons would steal attention. Outline chrome reads as "committed but not loud." Status-alert / status-success border + glyph color carry the semantic. **Keycap hints stay removed** (decision from S32-follow-up — earn-the-slot rule). |
| S32 follow-up #3 — Swipe tint replaced corner-badge approach: full-card colored gradient overlay fades in with drag (red left / green right); ring border accents | **Designer ask, comparing to explore-card hover-tint convention.** Previous swipe affordance was a corner ×/♥ icon badge that scaled and faded in during drag. Designer wanted parity with the explore card's purple-on-hover full-card tint pattern. Replaced: full-card absolute-positioned overlay with linear-gradient color + 2px ring border. Left drag → red overlay (`rgba(222,90,72,0.55) → 0.15` gradient + ring-status-alert). Right drag → green overlay (`rgba(179,214,97,0.55) → 0.15` gradient + ring-status-success). Opacity scales with `Math.min(1, abs(dragX)/120)`. On commit, opacity locks to 1 so the color decision finalizes visually before the card flies off. **Generalization (taste rule territory — already covered by codified hover-tint pattern in explore):** *swipe-to-act surfaces should reuse the hover-tint pattern from the surrounding design system (here, explore CharacterCard's `from-accent/20` overlay), just with directional color semantics instead of a single brand color. Consistent visual vocabulary across surfaces; the user learns it once on /explore.* |
| S32 follow-up #3 — Stage 1 identity picker added: "I am" (Female/Male/Non-binary) placed FIRST in the form | **Designer ask.** Previous form: Age + I'm interested in (2 pickers). New form: I am → Your age → I'm interested in (3 pickers). **Ordering rationale:** identity first matches dating-app convention. The user defines themselves first, then describes the world they want to meet. Putting it last would force backtrack mentally. **Options:** Female / Male / Non-binary. Hyphenated form ("Non-binary") is industry standard. Three options keep visual parity with the existing "I'm interested in" row. **Subtitle copy adjusted:** "Two quick questions" → "A few quick questions" (numeric drift; explicit-count framing breaks if a fourth picker lands later). **Type propagation:** `OnboardingPreferencesStep` prop signature `{ age, interest } → { identity, age, interest }`; `OnboardingOverlay` + `useOnboardingFlow` consumer types updated same edit. Preferences are still demo-only. **Generalization (taste rule territory):** *form fields ordered by what-you-need-mentally-FIRST, not what's-easier-to-collect — identity questions before demographic before preference.* Sibling to the codified rule about action-first headlines on intervention popups — both about ordering UI to match user mental sequence. |
| S32 follow-up #3 — DeckActionButtons keycap hint row removed earlier in session; now bubble-as-typewriter; tags-row case fixed from ALL CAPS to title case; multiple small designer-iteration rounds | **Sweep of smaller catches across the polish round.** (1) **Boss/role removed from meta line** — designer clarified earlier "girlfriend teacher tags" referred to the role in `gender · age · role` meta (yellow text), not the tags row at bottom. Meta is now `gender · age` only. (2) **Tags row restored** — when I first removed tags via wireframe-misreading, designer asked them back; styled to match explore CharacterCard's chip chrome (`bg-white-10 backdrop-blur-bg border-white-10 text-white-80`). (3) **Tags case** — `BOSS / EXECUTIVE / COLLEAGUE` → `Boss / Executive / Colleague`. Title case matches explore CharacterCard tag convention; ALL CAPS was visual shouting on a card already busy with badges and bubbles. (4) **Bubble avatar repositioned** — was `items-start` (top-aligned); the bubble's tail corner is bottom-left, so avatar should align there. Switched to `items-end`. (5) **Bubble quotes stripped** — opening text had wrapping `"..."` quotes; appropriate when the bubble had "WHAT THEY OPEN WITH" label (quoted-snippet framing), inappropriate when the bubble is the actual AI chat-bubble pattern (chat doesn't quote its own messages). Stripped from all 9 deck entries. (6) **Bubble avatar restored** — designer wanted the avatar circle back after I removed it; reinstated with same `relative w-7 h-7 rounded-full overflow-hidden ring-1 ring-white-10` chrome. **No taste rules promoted from individual catches** — each was case-specific cleanup, not a transferable principle. |
| S32 follow-up #2 — OnboardingDeckBanner + its chat-side support code removed entirely (4 files deleted, lib/onboardingDeck.ts trimmed to liked-id only, ChatBannerStack abstraction reverted to inline Safety+Dormancy chain) | **Designer ask:** "we are letting go of the onboardingdeckbanner on chat screen right now, maybe we will use it later but right now we can remove it and its functionality from codebase." **Full removal, not commented-out / feature-flagged** — per the memory rule *"Avoid backwards-compatibility hacks like renaming unused _vars, re-exporting types, adding // removed comments for removed code, etc. If you are certain that something is unused, you can delete it completely."* **Files deleted:** `components/chat/OnboardingDeckBanner.tsx`, `components/chat/ChatBannerStack.tsx`, `app/chat/useChatOnboardingBanner.ts`, `app/style-guide/sections/patterns/OnboardingDeckBannerSection.tsx`. **Trimmed:** `lib/onboardingDeck.ts` dropped `ONBOARDING_ACTIVE_KEY`, `ONBOARDING_INDEX_KEY`, `ONBOARDING_BANNER_DISMISSED_KEY`, `readOnboardingActive`, `readDeckIndex`, `readBannerDismissed`, `dismissBanner`, `remainingCount`, `recordSkip` (all had banner as their sole consumer). Kept: `ONBOARDING_LIKED_ID_KEY` + `readLikedCharacter` + simplified `recordLike` + simplified `clearOnboarding` (matchmaking still works — `/chat` reads the liked id via `useChatCharacter`). **`useOnboardingFlow` simplified:** dropped the `?onboarding=resume` URL handler + index persistence calls; `skipCard` is now React-state-only (advances `deckIndex` until end-of-deck, then closes flow). **`chat/page.tsx`:** ChatBannerStack abstraction reverted to inline Safety+Dormancy chain — with only 2 banners the abstraction was carrying its weight thin; the inline conditional pair is 6 lines and clearer at the call site. **Style guide:** OnboardingDeckBannerSection removed from PatternsTab + NAV.Patterns; OnboardingOverlaySection's state-persistence callout simplified to just liked-id + deck-position-is-React-state. **No taste rules retired** — the banner-priority rule (intervention severity, then intent recency) stays codified; the principle is general and will apply whenever a third banner re-enters the chat slot. **Bundle impact:** /chat 14.8kB → 14.6kB, /style-guide 65kB → 64.5kB (small but real shrinkage from removing the banner + section + hook). **Generalization (already covered by feedback memory):** *removing a feature that may return = delete cleanly, not flag-gate. Restoring 60 lines from git history later is cheaper than maintaining a dead code path in every audit.* **Pre-flight check codified for future "let's pause this feature" requests:** confirm whether ANY adjacent functionality depends on the about-to-be-removed code (here: `useChatCharacter` was checked — depends on `readLikedCharacter` which lives in onboardingDeck.ts but is independent of banner code; safe to keep). |
| S32 follow-up — drag-to-swipe gesture added; my Q3 recommendation (buttons + keys only, no drag) reversed by designer | **Designer ask.** I had recommended deferring drag because of gesture-conflict bugs + JS weight, but the swipe IS the deck's primary mental model (it's literally Tinder). Buttons + keys without drag felt vestigial on mobile. **Implementation:** pointer events on the card wrapper (pointerdown captures start X + pointer ID, pointermove updates dragX state, pointerup commits if `dragX > 80px` else snaps back). `touch-action: pan-y` on the wrapper so vertical scroll still works inside the card body. **Same animation pipeline as the action buttons:** drag-released-past-threshold triggers `commit(dir)` which fires the same fly-off animation (translateX ±600 + rotate ±30° + opacity → 0, 320ms ease-out). One unified animation, three trigger paths (button / keyboard / drag) — keeps gesture and discrete-click feel identical. **Generalization (Gate 6.5):** *swipe-to-act surfaces should converge all trigger paths (button / keyboard / gesture) on one animation pipeline — different animation paths per trigger fragment the user's mental model of "this is the way to interact with the card."* The button is the gesture's keyboard shortcut, not a separate flow. |
| S32 follow-up — Tinder-style tilt + fade animation with intent-badge overlays during drag | **Designer ask.** Pre-follow-up, action buttons fired `onSkipCard`/`onLikeCard` directly — instant index advance with no animation. Felt clinical. New behavior: action buttons + keyboard + drag all converge on `commit(dir)` which sets `committing: 'left' | 'right'`, transitions to `translateX(±600) rotate(±30°) opacity(0)` over 320ms, then advances index. During drag (pre-commit), card translates + rotates proportionally to `dragX * 0.06`° and the matching badge (× red on left-drag, ♥ green on right-drag) fades in scaled to `min(1, abs(dragX)/120)` — telegraphs the impending action with progressive feedback. **Badge anatomy:** 64×64 round, 3px border (`status-alert` or `status-success`), bg-status-X/[0.15], rotated ±12° to read as a stamp. Positioned `top-l left-l` (skip) or `top-l right-l` (like) over the card image. **Generalization (taste rule territory):** *progressive intent feedback during a swipe-to-act gesture is non-negotiable — users need to KNOW which way is which before they commit. A badge that fades in proportional to the drag distance + matches the action's status color is the codified pattern; no badge during drag = "I don't know which way I'm swiping until it's already too late."* Sibling concept to the existing taste rule *"Trigger and result share spatial position"* — the badge IS the in-flight preview of the result, locked to the trigger's gesture. |
| S32 follow-up — DeckCard image aspect-[3/2] (landscape, post-Gate-8) → aspect-[5/4] (slight portrait); popup height bumped 800 → 880; desktop now shows characters' faces properly | **Designer flagged my Gate 8 fix as over-corrected.** First Gate 8 self-catch had bumped desktop aspect from `aspect-[4/5]` to `aspect-[3/2]` (full landscape) — that fit the popup BUT cropped character heads. Designer's image #6 showed the desired state: image more dominant, character face fully visible, popup tall enough to accommodate. **Fix:** `aspect-[5/4]` on desktop (slight portrait, not landscape) + popup `min(800px,92vh)` → `min(880px,94vh)` to give the taller card room. Mobile stays `aspect-[4/5]` (unchanged — full-viewport takeover absorbs whatever height the card needs). **Style guide preview shell synced same edit:** `h-[800px]` → `h-[880px]`. **Lesson (workflow.md territory — not promoted yet, watching for second occurrence):** *Gate 8 fixes should err on the side of "image more visible" rather than "everything fits without scroll" — fitting without scroll at the cost of unrecognizable character heads is the wrong trade-off.* When designing a swipeable card whose primary content is a portrait photo, the aspect should preserve the face; the body content can scroll if forced. |
| S32 follow-up — OnboardingDeckBanner: "View →" link sits INLINE with the title; close button now anchors the right edge alone | **Designer ask.** Pre-follow-up: title at left → flex-1 spacer → View link → close button (View was pushed far right). Felt teleported — title says "you have 8 more characters waiting" and the action to view them is on the opposite side of the row. **Fix:** desktop banner — title block is `<div className="flex items-center gap-s flex-wrap"><p>title</p><Link>View →</Link></div>` — View immediately adjacent to the count it describes. Close button stays at the row's far right (distinct intent: dismiss this banner, not act on the deck). **Mobile pill was already correct** — the gap-xs layout had count → View → close all inline; matched the right pattern even before this fix. **Generalization (already covered by codified taste rule "Related controls cluster — don't push them to opposite ends of a row"):** the View link IS a related control to the count text; pushing it to the row's right edge by space-between violated that rule. Logged here as a moment but the codified rule already handles the principle. |
| S32 follow-up — OnboardingHeader uses `/logo.png` directly (104×24 single asset) instead of composing `<Image>` + wordmark text | **Designer ask, per image #5 reference.** Pre-follow-up the header was `<Image>` (mark only) + `<span>wsup.ai</span>` text alongside — composing the mark + wordmark with separate sizing controls. Result: wordmark visually over-weighted relative to mark; the eye picks up the text first. **Fix:** swap to the codified `/logo.png` asset (same one Header.tsx uses — 104×24 composite where the wordmark is intentionally proportionally-smaller). One asset = one designed proportional relationship; recomposing always breaks it. **Generalization (already partially covered by Gate 7 — "Visual patterns: if links, buttons, badges have an established style, use it"):** *if a multi-element brand asset exists as a single file (logo+wordmark, brand+icon, etc.), use the file directly — don't recompose from parts. The brand asset embeds the designer's proportional decisions; composing separately always over-weights one element.* Sibling rule to the icon-fill-ratio rule (S30) — both about preserving designed proportions. |
| S32 follow-up — Deck expanded 4 → 9 characters (5 new: Aiden Cross / Vera Sloane / Mira Yagami / Cole Ramirez / Elysia Vance) | **Designer ask.** 4 cards felt thin — onboarding deck needs enough variety to demonstrate the matchmaking promise. **5 new entries spanning distinct personality archetypes:** Mentor (fitness coach, char1), Bold (sharp-tongued ex-coworker, char3), Anime (dominant cult leader, char6), Friend (gamer bro, char7), Sci-Fi (sentient AI philosopher, char10). All have category badges + roles + openings that match the personality. **Why 9, not 10:** 9 is an odd-count visual sweet spot for the progress bar — feels deliberate vs lazy round number. **Top-pick flag stays only on card 1 (Donovan)** — the curation gimmick reads as fake if every card claims to be a top pick. **Style guide unchanged:** the SectionPreview shells use index 0 and 2 — both still valid, no new section needed for the additional cards. |
| S32 follow-up — Action buttons drop keycap hints ("[←] skip · [→] chat" row); kbd handlers remain | **Designer ask.** Keycap hints were a leftover from when the swipe gesture didn't exist — explicit education for keyboard users. With drag + animation now telegraphing intent, the hints became visual clutter on a surface already dense with badges, name overlays, description, opening bubble, tags. **Removed:** the `<div>` row below the two action buttons that rendered `←` keycap + `skip` text + `→` keycap + `chat` text. **Preserved:** the keydown handler in OnboardingDeckStep still listens for ArrowLeft/ArrowRight and routes to the same `swiperRef.current?.swipe(dir)` imperative call. Keyboard users still get the feature; the visual hint just isn't worth a row of vertical real estate. **Generalization (taste rule territory):** *keyboard hints earn their UI slot when the keyboard IS the primary interaction model (forms, search, dev tools). When keyboard is a power-user shortcut layered on top of a gesture-primary surface, the hint is teaching the wrong audience — most users will swipe, the kbd-aware minority will discover ← / → on their own.* Pairs with the existing rule about dev shortcuts ("Don't keep dev shortcuts that don't earn their slot") — kbd hints in user-facing UI follow the same earn-the-slot logic. |
| S32 — Gate 8 self-catch (desktop card overflow): DeckCard image aspect changed from `aspect-[4/5]` to `aspect-[4/5] md:aspect-[3/2]`; popup height bumped to `min(800px, 92vh)`; card body gap tightened from `gap-m` to `gap-s` | **Caught in Gate 8 screenshot review, before declaring done.** First Playwright run showed the desktop card had image filling the entire 720px popup-body region — description + opening line + tags row all hidden below the fold. Mobile was fine because it's a full-viewport takeover with natural scroll. Desktop was broken because the 480×720 popup constrains the card area to ~445px while a portrait-aspect image alone wanted 550px+. **Three coordinated edits in same review:** (1) Image aspect becomes responsive — `aspect-[4/5]` mobile (matches wireframe portrait dominance, full-viewport context absorbs height) → `aspect-[3/2]` desktop (landscape crop, ~293px tall at 440w, leaves room for body). (2) Popup height bumped from `min(720px, 88vh)` → `min(800px, 92vh)` — another 80px of vertical room. (3) Card body inter-element gap from `gap-m` (16) → `gap-s` (12) — 8px shaved off body height. Result: full anatomy (image + name overlay + description + opening bubble + tags + action buttons + keyboard hints) visible on first paint at both viewports. **Generalization (taste rule territory):** *responsive overlays with a fixed-height desktop popup AND a tall scrolling mobile takeover need viewport-responsive child sizing — using one aspect ratio for the dominant visual element across both will overflow one or under-fill the other.* The mobile-first instinct here was wrong: the right move is to design the dominant visual element for BOTH viewports' constraints, then pick the aspect that fits each. **Pre-flight check codified for future overlays:** before shipping a desktop-popup + mobile-takeover dual surface, run a height budget — popup_height - (header + title + progress + action_buttons + gaps) = card_area. If the card's dominant visual alone exceeds card_area, the aspect needs to flip on desktop. **Self-catch consequence:** style guide preview shell also retuned from `h-[720px]` to `h-[800px]` so the inline mockup matches the live overlay's height — Gate 5 same-edit sync. |
| S32 — onboarding chat banner priority: Safety > Onboarding-deck > Dormancy (designer override of my initial recommendation) | **Designer call on Q9.** My recommendation was Safety > Dormancy > Onboarding-deck (treat deck as positive nudge, less urgent than character status). Designer picked (b) — deck banner above dormancy. **Reasoning the designer's call captures:** the deck banner is tied to a specific user intent the user just executed (they just matched with a character; the banner is the receipt + the resume path). Dormancy is character status the user can act on later. The user's most recently expressed intent outranks a static character state notification. Codified the priority in `ChatBannerStack.tsx` (Safety → Onboarding → Dormancy) and documented in OnboardingDeckBannerSection's "Banner priority" callout. **Generalization (Gate 6.5):** *banner priority in shared chrome slots should be ordered by (a) intervention severity first (safety always tops), then (b) recency of user intent that the banner addresses (a banner tied to "the thing you just did" outranks a banner about "the state this thing has been in").* Promoted to taste.md as the *"Banner priority: intervention severity, then intent recency"* rule. |
| S32 — onboarding flow shipped: Explore R-key two-axis panel (Auth + Demo flow) → Stage 1 Preferences → Stage 2 Deck → Chat with deck-waiting banner; chat character overrides Billie based on the liked card | **Major new flow.** Three stages on /explore → /chat hand-off: Preferences (age + interest pills) → Deck (4-card swiper with × / ♥ buttons + ←/→ keys) → Chat with persistent "N more characters waiting" banner. **Architecture:** Single overlay (`OnboardingOverlay`) mounted in /explore with stage machine in `useOnboardingFlow` hook. Mobile = full-viewport takeover; desktop = ~480px centered popup over scrim. NOT CenterPopup primitive — surface is taller than 80vh and behaves like a multi-step takeover; CenterPopup's overflow-hidden would clip the deck stage. Used custom scrim wrapper instead (matches the S31 MemoryLimitOverlay precedent + the codified S31 taste rule about self-chromed popups). **State persistence:** localStorage (matches the SUGGESTIONS_PREF_KEY pattern — no new context wiring). Keys: `wsup_onboarding_active`, `wsup_onboarding_liked_id`, `wsup_onboarding_index`, `wsup_onboarding_banner_dismissed`. **Chat character override:** new `useChatCharacter()` hook reads `wsup_onboarding_liked_id` on mount and swaps the default Billie name/image/avatar with the liked character. Default returned during SSR so first paint is stable. 4 spots in chat/page.tsx now read from `character.*` (bg image, ChatHeader, ChatMessages, MemoryLimitOverlay). **Resume contract:** chat banner's "View →" routes to `/explore?onboarding=resume` → useOnboardingFlow re-opens at saved index. **Skip semantics (Q2):** Skip dismisses the ENTIRE flow back to plain explore, clearing all onboarding localStorage state. Demo-flow radio in the dev panel also flips back to Default. **Continue gating (Q3):** disabled until both age + interest are picked — matches the wireframe and the codified ChatStyleSheet pattern (disabled-until-selection via Button's `opacity-40 cursor-not-allowed`). **No drag gesture (Q3):** buttons + keyboard arrows only. Drag is deferred until a real product need surfaces — adds JS weight and gesture-conflict bugs for marginal demo value. **Designer iterations during planning:** Q1 (two-axis dev panel), Q6 (4 cards not 10), Q9 (Onboarding > Dormancy priority — designer override; see separate row above). **Style guide synced same edit:** OnboardingOverlaySection (new pattern — Stage 1 + Stage 2 inline mockups + anatomy + state-persistence table), OnboardingDeckBannerSection (new — desktop + mobile + multi-/singular-remaining + anatomy), FormsSection's SelectionPillGroup section extended with the new `wrap` variant + explainer copy. PatternsTab + NAV.Patterns updated for both new entries. |
| S32 — SelectionPillGroup extended with `wrap` prop (not forked) for the onboarding age picker | **Gate 2 reuse decision.** Existing `SelectionPillGroup` uses `flex gap-m` with each option `flex-1` — works for 2-3 mutually exclusive options that need equal weight (Gender, Chat Filter). The onboarding age picker has 5 options that need to *wrap* to a second row (18-20 / 21-25 / 26-34 / 35-44 + 45+ wraps). Adding `wrap?: boolean` to the existing component is the right move per Gate 2 ("if a component does 80%+ of what you need, add a prop/variant — don't create a new component"). **Implementation difference:** wrap variant uses `flex flex-wrap gap-xs` with rounded-pill chrome (not rounded-button) + single-color toggle (white on dark, selected = `bg-white text-page-bg`). Original variant kept untouched — defaults to `wrap=false`. **Style guide synced same edit:** new wrap-variant PreviewBox added to FormsSection + explainer copy describing when to use the wrap path vs the default. **Generalization (Gate 6.5 — already covered by Gate 2):** *when an existing primitive serves a related use case at slightly different layout/density, add a variant prop rather than fork — the consumer surfaces drive the variant set, the primitive owns the chrome.* |
| S32 — chat/page.tsx narrowing chain compacted to a const array + includes-check to free file-budget for new mounts | **Mechanical refactor.** The discriminated-union narrowing chain for `headerCharacterState` (10 lines of `chatState === '...' \|\| chatState === '...' \|\| ...`) was hand-maintained and grew on each new popup state added in S31. Compacted to `const POPUP_STATES = [...] as const` + `(POPUP_STATES as readonly string[]).includes(chatState) ? 'active' : chatState as CharacterState`. **Saves 8 lines, makes adding new popup states a 1-line edit** (just append to the array). **Why this isn't a Gate 6 fail (overactive cleanup):** the file was at 311 lines after adding the onboarding hooks + banner stack mount; the 300-line cap is enforced. Two cleanup paths: (1) extract more state to hooks (already extracted twice this session — useChatCharacter, useChatOnboardingBanner), (2) compact existing code where logic doesn't change. The narrowing chain is the cleanest compact: no logic change, no semantic shift, just a denser expression of the same predicate. Final page.tsx: 294 lines (6 of headroom). |
| S32 — chat banner stack extracted to `<ChatBannerStack />` (single-mount component) so the priority chain lives in one place | **Refactor + new state.** Pre-S32 the chat banner stack was inline: `{activeSafetyVariant && <SafetyBanner />} {!activeSafetyVariant && bannerVariant && <DormancyBanner />}`. Adding the onboarding deck banner introduces a third surface in the same slot, and the priority chain (Safety > Onboarding > Dormancy) needs to be enforced in one place — not by chaining conditionals at the page level. **New component:** `ChatBannerStack` takes (activeSafetyVariant, dormancyVariant, showOnboardingBanner, onboardingRemaining, onboardingDismiss, onSafetyClose) and returns *one* surface based on priority. Safety wins outright (it has its own mobile-overlay + desktop-floating-card responsive behavior); onboarding-deck is in-flow under header; dormancy is in-flow under header (last). **File-budget impact:** chat/page.tsx replaced 6 lines of inline conditional + 1 absolute-positioning wrapper with a single 7-prop component call (net 0 lines saved at the call site, but the priority logic is now centralized + extensible — next banner type just adds a prop). |
| S32 — chat character override: useChatCharacter() hook reads localStorage liked-id on mount, swaps default Billie's name/image/avatar | **New hook.** Per Q4 (a): chat page should render the liked character, not always Billie. The hook returns `{fullName, shortName, image, avatar}` defaulting to Billie; on mount it checks `readLikedCharacter()` and if present, swaps to the deck character. **Avatar derivation:** maps `/chars/charN.webp` → `/chars/avatars/charN.jpg` via regex. Single-line conversion is cleaner than storing the avatar separately on `OnboardingCharacter` — the avatar path is deterministic from the image path, no extra data needed. **shortName**: first word of the full name — used in MemoryLimitOverlay's heading ("Billie's memory is full") where the surname feels formal. **SSR safety:** state initializes to default Billie; useEffect updates client-side once localStorage is readable. First paint matches what unauthenticated/onboarding-inactive users see — no hydration mismatch. **Style guide:** `useChatCharacter()` is a hook, not a visual primitive — documented inline in OnboardingOverlaySection's state-persistence callout, not as its own section. |
| S32 — ExploreDevPanel two-axis (Auth + Demo flow), mirrors ChatDevPanel pattern | **Per Q1 (a).** Pre-S32 the explore R-key just toggled an Auth toggler visibility (+ Shift+R flipped auth). New `Onboarding` option needed a home that doesn't conflate with auth concerns. Mirrored the chat dev panel pattern: top section "Auth" with two radio options (Logged in / Not logged in), bottom section "Demo flow" with two options (Default / Onboarding). Same DevStateToggle wrapper, same `<div h-px bg-white-10 my-xxs />` divider between sections, same StateLabel header for the lower section. Demo-flow toggle routes through `handleSetDemoFlow` → `onboarding.start()` or `onboarding.closeFlow()`. **Skip from inside the overlay** also flips the dev-panel radio back to Default via `handleSkipFlow` — keeps the panel + overlay state in sync regardless of which surface initiated the close. **Generalization (workflow.md territory):** *when a page acquires a second dev-time control axis, refactor the inline R-key handler into a sibling DevPanel component that follows the chat's two-axis template — keeps the dev surfaces visually consistent across pages and prevents drift.* |
| S31 close — Monthly subscription path removed from BuyCreditsSheet; going forward with one-time purchases only | **Designer directive** — "we are now going forward with only one-time, we don't need tabs now ... we might need monthly payment option in the future but not anytime soon". **Removed:** `PackModeToggle` component (deleted entirely), monthly mode state in BuyCreditsSheet, `mode` prop on FlowBody, `applyMonthlyBonus` + `MONTHLY_BONUS_MULTIPLIER` exports, `selectable` / `selected` / `originalCredits` props on CreditPackRow, "Continue on Patreon" button + Patreon-flow short-circuit, `subscription` ResultMode + `Manage subscription` link in BuyCreditsResultStep, `oneTimeOnly` filter flag on CreditPack interface. **Simplified:** BuyCreditsPackagesStep — single pack list, each row has its own Buy CTA (was: row-tap radio for monthly + per-row Buy for one-time). CreditPackRow — single render variant (no more `selectable ?` branching). BuyCreditsResultStep — only success/failure, copy fixed for one-time. **Architecture preserved:** Tabs primitive (`<Tabs>` / `<Tab>`) still in codebase — a future PackModeToggle would compose it again. The `mode` axis can return as a non-breaking extension if monthly comes back; today's removal is a simplification, not a permanent architectural shift. **Style guide synced same edit:** BuyCreditsSheetShowcase rewritten (one-time only, single Result variant toggle); TabsSection lost the PackModeToggle consumer mention (kept the badge example as a generic Tabs pattern); ModelPickerSheetSection's "PackModeToggle" reference dropped. **Generalization (workflow.md territory):** *when removing a feature that may return, delete the dead code completely AND leave a clear breadcrumb (a one-liner comment + style-guide TokenRow noting the removal + the path to reintroduce).* That's cleaner than commented-out code or feature flags — keeps the codebase honest while preserving institutional memory. **File-budget impact:** PackModeToggle deleted entirely; CreditPackRow shrank ~70%; BuyCreditsSheet shrank ~35 lines; BuyCreditsPackagesStep shrank ~50%. Net: 4 files smaller, 1 file deleted, no orphans |
| S31 close — chat curation reshuffle: Llama 3 + Mistral Nemo re-added; Claude Opus moved primary→primary (was always primary in this rebuild, but DeepSeek V4 went primary→other); tier `'advanced'` → `'other'`; "Less popular" framing replaces "Advanced" framing | **Designer iteration on the picker.** Earlier today S31 dropped Llama 3 + Mistral Nemo as "noise floor of the picker." Designer reversed that call after seeing the live curation: Llama 3 (free) is essential for cost-conscious users to discover; Mistral Nemo has a distinct dark-fantasy use case. Re-added both. **Final curation:** Primary = Llama 3, Qwen Plus Character, DeepSeek V3, Claude Opus 4.6 (4 popular default options). Other = Mistral Nemo, MiniMax M2 Her, DeepSeek V4 Pro (3 less-popular alternatives). **Rationale for the rename `'advanced'` → `'other'`:** the previous rename ('legacy' → 'advanced' earlier in session) was framed *"more powerful tier"* because Claude Opus lived there. Today's reshuffle put Claude Opus into primary, leaving the tier as a "less-popular fallback" set (Mistral, MiniMax, DeepSeek V4). 'Advanced' became wrong-coded ("less popular" ≠ "more advanced"); 'other' captures the actual semantics. Step name + tier value + UI label + step-2 description copy ("Less-popular alternatives. Pick one of these if the primary models don't fit your story.") all updated together. **Cross-rule check:** the S30→S31 amended taste rule *"density should match the user's information needs at the moment of choice"* still holds — both tiers use the dense ModelRow anatomy. The reshuffle is a curation/labeling change, not an anatomy change. **Style guide synced same edit:** ChatStyleSheetSection mockups + anatomy entry reflect 4-primary + 3-other split, new copy for step 2. ModelPickerSheetSection notes "7 entries (4 primary + 3 other)" |
| S31 close — `appOnly` model flag + `GradientChip` "Available only in mobile app" + dynamic "Continue in app →" CTA + `AppHandoffStep` QR view; redundant highlighter banner removed | **Designer ask** — the earlier full-width gradient banner ("Free models available on the wsup mobile app · Get app →") at the top of step 1 was a coarse cross-app promo. Designer wanted per-row precision: only Llama 3 (and later Mistral Nemo) are mobile-app-tier, not all primary models. **Architecture:** added `appOnly?: boolean` to the `Model` interface; tagged Llama 3 + Mistral Nemo. ModelRow auto-renders a `<GradientChip>Available only in mobile app</GradientChip>` next to the cost chip when `model.appOnly`. New `GradientChip` exported from ModelPickerInternals — same shape as Chip but uses WSUP brand gradient (`gradient.purple`/`gradient.blue` for both bg tint and text via `bg-clip-text text-transparent`). Used the codified gradient tokens, NOT the LoginSheet's cyan→indigo subtitle gradient — designer picked purple→blue from 3 options. **Dynamic CTA:** "Continue chat" → "Continue in app →" when `MODELS.find(m => m.id === draft)?.appOnly` is true. Right-arrow SVG matches the `BuyCreditsPackagesStep` "Continue on Patreon" path (`M5 12h14M13 6l6 6-6 6`) — codified WSUP cross-app handoff visual. **AppHandoffStep:** new `'app-handoff'` step inside ChatStyleSheet's step machine (was `'primary' \| 'other'`, now `'primary' \| 'other' \| 'app-handoff'`). Triggered when user taps "Continue in app" with appOnly model selected — replaces commit/close behavior. Renders QR placeholder + Back pill button (Cancel removed per designer call — header × already handles dismissal). Back routing derives from `draft.tier` so it returns to primary or other appropriately. **Banner removal:** once the per-row GradientChip + dynamic CTA were live, the standalone highlighter banner became redundant — same info conveyed twice, the per-row signal more precise. Removed banner from primary step + all style guide step-1 mockups. **Generalization (taste rule territory):** *cross-app or cross-platform promo signals work better at row granularity than container granularity when the surface lists distinct items — per-row chip + per-selection CTA gives precision the standalone banner can't match.* Promoted to taste.md. **Style guide synced same edit:** ChatStyleSheetSection mockups updated; new "Step 1 — Llama 3 selected (appOnly) → CTA flips" mockup added; new step-app-handoff mockup; anatomy entries for `GradientChip`, `appOnly` flag, dynamic CTA, and AppHandoffStep added. **Self-catch:** banner removal triggered a Gate 5 sweep — found 3 step-1 mockup variants in the style guide that all needed the banner removed. Removed via `replace_all` + manual edits |
| S31 close — chat dev panel two-axis (Flow + State) refinements: flow toggle resets full journey state; "No flow" option dropped | **Two follow-ups to the two-axis dev panel decision.** **(1) Flow reset on toggle:** designer noted having to refresh the page to re-walk a flow was friction. Wrapped `setFlowMode` with `handleFlowChange(newFlow)` that, on flow CHANGE (no-op if same): calls `sendGate.reset()` (logs out + zeroes counters + closes login gate via `useAuth.logout()`), sets `chatState` back to `'active'`, closes ModelPickerSheet + BuyCreditsSheet. Now flipping New user ↔ Returning user gives a clean re-walk without reload. ChatDevPanel's `setFlowMode` prop type changed from `Dispatch<SetStateAction<FlowMode>>` to `(flow: FlowMode) => void` since the wrapper accepts only direct values. **(2) "No flow" dropped:** the third radio option in the Flow section had no real demo use case — "Returning user" already covers most logged-in scenarios, and the credit-out gate fires regardless of flow. Removed `'none'` from `FlowMode` type, `FLOWS` array, `FLOW_LABELS`, and `FLOW_AFTER_LOGIN`. Type narrowing forces all consumers to handle just the two real flows. **Generalization (workflow.md territory):** *when a dev-panel selector has 3+ options, audit each option's demo value before shipping — "no-state" or "default" options that don't change anything compared to the active states are usually noise.* `FLOW_AFTER_LOGIN['none']` was the giveaway: it just fired a toast and did nothing else, which is functionally identical to "the LoginSheet's default behavior." If an option's behavior is the absence of behavior, it's not a real option |
| S31 close — CreditServicePopup follow-ups: "Take me there" → secondary variant, claim-daily-rewards row hidden entirely; state label "Credit service" → "Out of credits popup" | **Three small designer iterations on the credit-out popup.** **(1) Take me there → variant="secondary":** primary CTA on a row that's adjacent to the BuyCreditsPromoCard's primary CTA (gold "Buy Credits") created competing visual weight. Switched the claim row's CTA to secondary (transparent + border-white-20 from Button primitive) so the buy CTA owns the visual hierarchy. **(2) Claim daily rewards row removed entirely:** designer subsequently called the row redundant — "claim-daily-rewards is reachable via the dev toggler / streak claim popup directly." Removed the row + the `onClaimDaily` prop from CreditServicePopup + the wired callback in page.tsx. The popup now shows: header + label + BuyCreditsPromoCard. Single intent: buy credits. **(3) State label rename:** "Credit service" was the Figma node name; "Out of credits popup" matches the user's mental model when seeing it in the dev toggler. Updated `STATE_LABELS['credit-service-popup']` only — the internal state name + component file kept as `credit-service-popup` / `CreditServicePopup` (matches Figma source). **Generalization (taste rule territory — already covered by amended density rule):** *removing UI is also a design decision — when an option exists for completeness rather than user need, it adds choice paralysis without value.* The claim row was Figma-faithful; removing it traded faithfulness for clarity. **Style guide synced same edit:** CreditServicePopupSection mockup + anatomy table both updated for the row removal + label change |
| S31 close — LoginSheet subtitle tightened second time: "Your free messages are up. Sign in to keep chatting — new users get 50 free credits." → "Free messages used up. New users get 50 credits on sign-in." | **Two-step copy iteration.** First pass (entry 16 above) reworked headline ("Out of free / messages" → "Keep chatting") and subtitle (action-first framing). Designer flagged the subtitle scoping next: `Sign in to get 50 free credits` framed the credits as a personal promise, but returning users (post-login routes to ChatStyleSheet, NOT StreakClaimPopup) won't actually see credits added. Risk: returning user signs in expecting credits, doesn't see them, breaks trust. **Fix step 1:** scoped to "new users get 50 free credits" — credits framed as informational (about new users) not personal. **Fix step 2 (designer flagged "too wordy"):** tightened from 14 words to 10 — "Free messages used up. New users get 50 credits on sign-in." Headline already says "Keep chatting", so subtitle's job is just (a) why the popup appeared, (b) scope the bonus to new users. **Generalization (taste rule territory — already covered by `<br />` taste rule and action-first headline rule):** *intervention-popup subtitles should scope any credits/bonus mention to the user-segment that actually receives them, otherwise they read as a personal promise to all viewers.* Pre-flight check: read the subtitle as a returning user would — does the bonus apply to me? If no, scope it explicitly to new users / first-time / etc |
| S31 close — early-session changes (logged retroactively per audit): default chatState `'context-exhausted-popup'` → `'active'` (auto-popup-after-2s removed); DEFAULT_MODEL_ID `'claude-opus-4-6'` → `'qwen-plus-character'` | **Logged retroactively per the close audit (these slipped past Gate 6 at the moment of decision earlier in the session).** **(1) Auto-popup removed:** the chat page had a `useEffect` that auto-fired the memory-full popup 2s after page load. Designer removed it — popup is now strictly dev-toggler-driven. **(2) Default model:** changed the default model that appears in ChatBar when /chat first loads. Qwen Plus Character (1 credit/msg, primary tier) replaces Claude Opus 4.6 (20 credits/msg). Designer rationale: cost-conscious entry point matches what most demo users would pick. **Style guide synced:** MemoryLimitPopupSection's "Auto-fires 2s after a fresh visit" note removed (replaced earlier in session); ChatBar fallback prop default `selectedModelName` updated to "Qwen Plus Character" (entry 7 of session log). **Self-catch — Gate 6 fail at moment of decision:** these calls shipped without a decisions.md row at the time. Caught in close audit + backfilled here. Lesson reinforced: log before reply, even for "small" data/default changes — the changes ARE part of the journey VDA needs to remember next session |
| S31 — CreditServicePopup added as the new credit-out gate (replaces direct BuyCreditsSheet trigger); BuyCreditsPromoCard extracted from CreditSidebar (Gate 3) for shared reuse | **Designer ask** — show a popup BEFORE BuyCreditsSheet that gives user two top-up paths: claim daily rewards OR buy credits. Pulled Figma spec from node 28790:83953 ("Credit Service Popup", 414×396). **Architecture:** new chatState `'credit-service-popup'` added to `ChatDemoState`, STATES, STATE_LABELS (also gives designer R-key dev-preview access). useSendGate's gate trigger now sets `chatState` to `'credit-service-popup'` instead of opening BuyCreditsSheet directly — `outOfCreditsOpen` state removed from gateState. CreditServicePopup mounted at page-level alongside other overlays (consistent with the stacking-context fix from earlier this session). New `buyCreditsOpen` state in page.tsx; CreditServicePopup's `onBuyCredits` callback closes service popup + opens BuyCreditsSheet. **Gate 3 extraction — BuyCreditsPromoCard:** the gradient promo card with credit-bags illustration was inline-defined in CreditSidebar. Second consumer (this popup) triggered the share. Extracted to `components/shared/BuyCreditsPromoCard.tsx` with optional `onBuy` callback (defaults to dispatching the global `wsup:open-buy-credits` event for backward compat with Header listener). CreditSidebar now imports + uses the shared card; popup uses it with explicit local handler. **Hex sparkle icon:** Figma shipped a unique 82×82 gold-hex-with-sparkle PNG (node 28790:83955). Downloaded via figma_get_component_image to `public/icons/icon-credit-hexagon.png` — single-use asset, not worth converting to SVG since the Figma export is already optimized. **Designer directive followed:** *"instead of add credits / buy credits in App container we have to use buy credit container from credit sidebar"* — kept the Figma popup structure (header + claim card row) but swapped the Figma's plain "Add credits + Play Store button" row for the existing CreditSidebar promo card. Closer to WSUP's established visual language. **Style guide synced same edit:** new CreditServicePopupSection registered in PatternsTab + NAV.Patterns; full anatomy table documents the reuse map (BottomSheet+CenterPopup, BuyCreditsPromoCard share, callback wiring, dev trigger). **Generalization (knowledge-base territory):** *when Figma proposes a row that overlaps with an existing in-codebase container, prefer the in-codebase container even if it diverges from Figma's literal anatomy — system consistency wins over Figma fidelity for demo-handoff work.* **File-budget:** page.tsx ended at 297; CreditServicePopup is 60 lines; BuyCreditsPromoCard 47 lines. CreditSidebar shrunk by ~30 lines from the extraction |
| S31 follow-up — copy/UX trims: removed new-user "50 credits added" toast (StreakClaimPopup already announces credits); removed BuyCreditsSheet contextNote ("Out of credits..."); added accent banner in ChatStyleSheet step 1 ("Free models available on the wsup mobile app") | **Three small calls, all designer-iteration.** (1) **Removed new-user toast** — when StreakClaimPopup auto-opens immediately after sign-in, the toast competes for attention AND duplicates the popup's own "+50 credits" framing. Cleaner: silent sign-in → popup announces the credits. `FLOW_AFTER_LOGIN['new-user']` no longer has `toast`; only `nextState`. (2) **Removed BuyCreditsSheet contextNote in chat flow** — the "Out of credits — buy more to keep talking." line was redundant with the sheet's own header ("Buy credits") and the user's mental model ("I just got blocked from sending"). Kept the `contextNote` PROP infrastructure on BuyCreditsSheet (still documented in showcase) for future contextual triggers; just removed the chat-screen usage in `ChatSendGates.tsx`. (3) **Added accent banner to ChatStyleSheet primary step** — promo line "Free models available on the wsup mobile app" in `bg-accent/10` + `text-accent-light` + `font-medium`, centered, rounded-button. Pushes WSUP's mobile-app cross-promo at a moment when the user is consciously evaluating models (and might be cost-conscious). Step 2 (advanced) doesn't get the banner — that audience is already past free-tier interest. **Generalization (Gate 6.5):** *promotional / cross-app cross-promo lines belong on entry-level surfaces (primary step, free flow), not premium surfaces (advanced step, paid flow) — match promo to user intent, not symmetrically across all surfaces of a popup.* Style guide ChatStyleSheetSection mockups + anatomy entry updated same edit |
| S31 — chat dev panel split into two-axis (Flow + State); FlowMode drives post-login routing | **Designer ask** — two named demo journeys: *new user* (chat 3 → login → daily check-in popup → continue → buy credits) and *returning user* (chat 3 → login → model selection popup → continue → buy credits). Both share the gate trigger (3 chats logged-out → login); difference is the post-login popup. **Architecture chosen — two-axis dev panel:** top section "Flow" (radio: New user / Returning user / No flow); bottom section "State" (existing discrete-state toggler). Both rows show "active" simultaneously when in a flow — flow indicates *which journey is being demoed*; state indicates *what's currently rendered*. Designer can click a discrete state to jump for one-off preview without abandoning the flow. **Why config-not-runner:** the FlowMode is a CONFIG (sets post-login routing inside `useSendGate`), not a sequence-runner. Gating logic stays the same; on sign-in, useSendGate looks up `FLOW_AFTER_LOGIN[flowMode]` and dispatches the appropriate `nextState` + `toast`. User progresses through the flow naturally by sending messages — no scripted-step machinery. **Implementation:** `chat-config.ts` adds `FlowMode` type + `FLOWS` array + `FLOW_LABELS` + `FLOW_AFTER_LOGIN` config map. `useSendGate(flowMode, setChatState, setToast)` accepts the three deps and routes on sign-in. `ChatDevPanel.tsx` (new) renders the two-section panel using existing DevStateToggle + DevStateOption primitives + an inline divider. `chat/page.tsx` shrunk 300 → 290 lines (removed inline DevStateToggle JSX, replaced with one-line ChatDevPanel mount + 2 lines of state). **Default flow = `new-user`:** most comprehensive journey for handoff demos. Designer can switch to `none` for raw-state previews without auto-popups. **Generalization (workflow.md territory):** *for demo screens with multiple end-to-end journeys, separate the journey-selector from the discrete-state-preview into two visible axes — one is "which path?", the other is "where in the path?".* Promoted as a workflow note for VDA's future demo work |
| S31 follow-up — MemoryLimitOverlay mount moved OUT of chat-column wrapper to page-level (sibling of `<main>`); root cause was CSS stacking-context trap | **Designer flagged the bug:** even after switching to `fixed inset-0` + z-70, the backdrop visually only darkened the chat column — Header + Sidebar + Right Sidebar still appeared at full brightness. Initial diagnosis (containing-block ancestor with transform/filter) came up empty — Playwright DOM inspection confirmed the backdrop's bounding rect WAS 1440×900 with z-70 fixed. **Real cause:** the overlay was mounted inside `<div className="relative z-10 flex flex-col h-full">` (the chat UI inner wrapper). `relative` + non-auto `z-index` creates a NEW STACKING CONTEXT — z-70 was scoped to that context, competing globally only as "the chat UI inner's effective z" (which is z-10). Header (z-50, fixed) and Sidebar (z-40, fixed) live in the global root stacking context — both beat z-10. So even though the backdrop covered the viewport in DOM, it stacked BENEATH Header + Sidebar visually. **Fix:** moved MemoryLimitOverlay mount out of `<main>` to be a sibling of ModelPickerSheet / ChatStyleSheet / StreakClaimPopup at the page top level — direct child of `<div className="bg-page-bg">`. Now competes against Header / Sidebar in the same stacking root. **How I caught it:** Playwright DOM dump showed the WSUP overlay's parent chain was `[chat-UI-inner, chat-column, <main>, page-bg, body]` while ModelPickerSheet's CenterPopup was at `[page-bg, body]`. The depth difference + the z-10 in the chain was the smoking gun. **Generalization (knowledge-base territory):** *fixed-positioned overlays must be mounted in the highest-applicable stacking context (typically page root) to compete globally with fixed Header / Sidebar.* Don't mount overlays inside DOM regions that have `position: relative` + `z-index: <anything-other-than-auto>` ancestors — those regions create scoping traps. The fix is a 5-line move (cut from inside chat column wrapper, paste outside main). **Style guide updated same edit:** MemoryLimitPopupSection's anatomy now documents the mount-location requirement explicitly so future devs/designers don't reintroduce the trap. **Pre-flight check for next overlay:** when adding a new fixed-positioned overlay, walk the proposed parent chain — if any ancestor has `position: relative` + non-auto `z-index`, mount higher up. |
| S31 follow-up — MemoryLimitOverlay switched from chat-column-anchored backdrop to full-viewport overlay (matches ModelPickerSheet / ChatStyleSheet / StreakClaimPopup z-70 scrim) | **Designer ask** — "memory full popup bg overlay can be same as other popups, not need to be chat area specific". Old: `absolute inset-0 bg-black-60` inside the chat column wrapper, popup anchored at `bottom-[88px]`, z-20/30. New: `fixed inset-0 bg-black-55 z-70`, popup viewport-centered with click-to-dismiss scrim — same convention as the other recent popups. **Why custom scrim, not CenterPopup primitive:** MemoryLimitPopup's character DP overhangs 48px above the popup card via `absolute -top-[48px]`. CenterPopup's card has `overflow-hidden` (necessary for its rounded-popup chrome) — would clip the overhang. So we wrap with a custom centered fixed-viewport scrim and let MemoryLimitPopup keep its self-provided chrome + overhang. **Style guide synced same edit** — Position / Backdrop / SubLabel / StateLabel / Dev toggle notes all updated to reflect new positioning + auto-fire removal. **Generalization (Gate 6.5):** *when a popup has self-provided chrome with overhanging elements (DP, badge, decoration above the card), don't wrap in CenterPopup — its overflow-hidden clips the overhang. Use a custom fixed-viewport scrim wrapper instead.* Promoted to taste.md as a sibling rule to the existing centered-popup convention. **Cross-rule check:** doesn't conflict with the "use CenterPopup for centered popups" convention — narrows it with a scope clause for self-chromed popups with overhangs |
| S31 follow-up — added `'claim-credits-popup'` to chat R-key state cycle (StreakClaimPopup preview); extracted R-key handler into `useDevStateCycle` hook to stay under page.tsx 300-line cap | **Designer ask** — wanted the explore page's StreakClaimPopup ("Claim free credits") accessible via the chat-screen R-key dev toggler so the popup can be previewed alongside Memory full / Model selection / Safety variants. **Implementation:** added the new state to ChatDemoState union + STATES array + STATE_LABELS in chat-config.ts; mounted StreakClaimPopup gated on `chatState === 'claim-credits-popup'` with same demo defaults as explore page (balance 10, streakDay 3, tomorrowReward 15, dailyCheckInEarn 15). **File-budget pre-emptive split:** mounting the popup would have pushed page.tsx from 296 → 308. Pre-emptively extracted the R-key keydown effect (10 lines inline) into `src/app/chat/useDevStateCycle.ts` — typed `Dispatch<SetStateAction<...>>` for both setters; same dep array. Saves 9 lines of inline code, makes the dev-only behavior reusable if other demo pages adopt the same R-key cycle. Final page.tsx: 299 lines. **Narrowing chain updated:** added `'claim-credits-popup'` to the `headerCharacterState: CharacterState =` chain alongside the other popup states — same lesson as the Vercel build fix earlier this session (when adding to a discriminated union, every narrowing chain must include the new variant). **Style guide:** no changes — StreakClaimPopup already has its own section (StreakClaimSection); the chat dev toggler isn't in the style guide |
| S31 follow-up — chat LoginSheet copy reworked: headline "Out of free / messages" → "Keep chatting"; subtitle moved context-then-action | **Designer flagged the rendered popup as ugly.** Two issues: (1) forced `<br />` in headline ("Out of free<br/>messages") split a preposition from its object — read as grammatically broken. (2) subtitle was 14 words wrapping to 3-4 lines, dense and slow to scan. **Fix:** flipped to action-first framing — headline "Keep chatting" (2 words, fits on one line at any popup width), subtitle "Your free messages are up. Sign in to get 50 free credits." moves the context-then-action pattern into a single body line. Designer picked this option from 3 directions presented. **Self-catch — protocol followed:** per the *"Never tighten copy silently"* memory rule, surfaced 3 options with visual previews instead of picking-and-shipping. **Generalization (taste rule territory):** *avoid `<br />` in headlines — concise copy that fits the container without forced breaks reads cleaner across all viewport widths*. Forced breaks are calibrated to ONE viewport's width; container shrinks (mobile, narrower popup) and the break lands at a different word, often awkwardly. The fix is shorter copy, not load-bearing line breaks. **Action-first headline pattern:** when a popup interrupts a flow with a credit/login wall, the headline should describe the user's *desired action* ("Keep chatting", "Continue") rather than the *blocked state* ("Out of free messages") — keeps the surface positive and motivating. The blocked state belongs in the subtitle as setup for the action's solution. Promoted as a sibling rule to the existing taste rule about CTAs not pointing at generic external directories — both are about positive framing on intervention surfaces |
| S31 follow-up — free-msg threshold 9 → 3 (faster demo trigger); BuyCreditsSheet default tab Monthly → One-time; contextNote center-aligned | **Three designer-iteration tweaks after first preview.** (1) **Threshold 9 → 3**: 9 was too many sends to test the gate during demo; 3 surfaces the LoginSheet within the typical demo cadence (designer ships 2-3 example messages, gate fires immediately on next send). Symmetric with the post-login PAID_MSG_LIMIT=3 — both gates now fire after the same number of sends, simpler narrative for handoff. (2) **Default tab One-time**: production wsup.ai opens the buy-credits sheet to one-time by default — Monthly is the upsell behind the toggle. Showcase mockup also flipped to match (Gate 5 sync). (3) **contextNote center-aligned**: the note sits between the centered title ("Buy credits") and the centered tab toggle below. Left-aligned text broke the optical column. Added `text-center` to the `<p>`. **Generalization (taste rule territory):** *contextual subtitle lines below a centered title should also center* — when a sheet's title is centered (justify-between header with title pinned mid-row), any explainer/subtitle/contextNote below it inherits the title's optical column. Left-alignment looks like body content has started; centered reads as "still part of the title block." Pre-flight check: read the rendered popup top-down — the title + any contextual subtitle + any tab toggle should share the same horizontal axis. If one breaks the column, it reads as broken hierarchy. (Added to taste.md as "title-and-tagline column alignment" follow-on to the existing "header element + tagline column-align" rule for wide layouts) |
| S31 — chat-send gating: 9 free msgs (logged-out) → contextual LoginSheet → +50 credit toast → 3 paid msgs → contextual BuyCreditsSheet. New `contextNote` prop on BuyCreditsSheet; new `useSendGate` hook + `ChatSendGates` presenter | **PM directive** — credit-funnel flow mocked as part of design handoff. **Architecture:** state + gating logic isolated to `useSendGate` (`src/app/chat/`); two intervention sheets mounted via dumb presenter `ChatSendGates`; `chat/page.tsx` gains 3 lines (hook call + 2 lines in handleSend). Pre-emptively split before page.tsx pushed past 300 lines (final: 296). **Demo-compressed counters:** 9 + 3 are illustrative thresholds, not credit-cost-accurate (Qwen at 1/msg + 50 credits = 50 messages of capacity in real terms). For demo storytelling, the funnel needs to fire fast — full-realism credit math would never trigger BuyCreditsSheet within a session. Counters reset on page reload (no localStorage) per "fresh demo on every visit" answer from designer. **Trigger scope:** any model when logged out (not Qwen-only) per designer answer — keeps logic simple, gate semantics consistent regardless of model selection. **Post-login UX:** user re-sends manually after sign-in (per designer answer) — clean intent, no autoplay. Toast on sign-in: plain "50 credits added", no extra framing. **`contextNote` prop on BuyCreditsSheet:** optional string rendered as `text-xs text-text-body` line under the Buy credits header. Default-empty preserves existing call sites (Header). Used here for "Out of credits — buy more to keep talking." Documented in BuyCreditsSheetShowcase anatomy table. **Generalization (taste rule territory — see below):** *sheets with multiple trigger contexts should accept an optional `contextNote` prop to explain why they appeared* — Gate 6.5 cross-rule probe surfaced this as a design-system pattern, not a one-off. Promoted to taste.md. **LoginSheet usage:** inlined the chat-specific copy directly in `ChatSendGates` (vs creating a sibling preset like ProfileLoginGate) — single consumer doesn't justify extraction yet; if a third login-gate context appears, extract then |
| S31 follow-up — ChatStyleSheet hides signal + personality chips (cost chip only); ModelRow accepts `showSignal` / `showPersonality` flags (default true) | **Designer iteration after first preview.** Same `ModelRow` component (no fork, no duplicate), but surface decides which chips to surface. ChatStyleSheet passes `showSignal={false} showPersonality={false}` → row renders title + cost chip + description + CheckBadge. ModelPickerSheet keeps the dense 3-chip default. **Why this is right:** at start-of-chat, the deciding factor is *cost-per-message* (commits user to a credit burn rate for the whole conversation); latency (signal) is a power-user concern, personality (alias) is a stylistic flair already conveyed by description. In-chat switch (ModelPickerSheet) is the surface where latency + alias matter — user is mid-flow, may already have a feel for the chosen model and wants to compare alternatives by all three dimensions. **Generalization (already covered by amended taste rule):** *density should match information-need at point-of-choice* — this is just the chip-level application of the row-level principle amended earlier this session. The component is the canvas; the surface paints. **Component design:** boolean flags with default `true` keep existing call sites unchanged (ModelPickerSheet untouched), only the divergent surface opts out. Chose 2 separate flags over a `chips` array prop because the chips are a fixed enumerable set of 3, and explicit `showX={false}` reads better at call sites than `chips={['cost']}` (positive enumeration of inclusion is harder to scan than negative opt-out from defaults) |
| S31 — ChatStyleSheet rebuilt to share ModelPickerSheet's row anatomy (title + signal/personality/cost chips + description + CheckBadge); title → "Select Chat model"; "Legacy" → "Advanced"; Llama 3 + Mistral Nemo dropped; CTA disabled until first row tap | **PM directive** ("told to use the same model bars as before"). Row anatomy is now shared via `ModelRow` extracted into `ModelPickerInternals.tsx` (Gate 3 — second consumer triggered the extraction; ModelPickerSheet still auto-commits on tap, ChatStyleSheet drafts via tap and commits on Continue chat). **Curation (after S31 follow-up move):** Primary tier = Qwen Plus Character / DeepSeek V3 / MiniMax M2 Her (3 models). Advanced tier = DeepSeek V4 Pro / Claude Opus 4.6 (2 models). Designer moved DeepSeek V4 Pro from primary → advanced after first preview — its complexity-4 latency tier and 10/msg cost belong with Claude Opus's profile, not alongside the lighter primary set. Llama 3 + Mistral Nemo dropped from MODELS catalog entirely (free-tier Llamas were the noise floor of the picker — removing reduces choice paralysis). **"Advanced" not "Legacy":** rename signals *more powerful, requires more credits* not *older generation* — better matches what's in the bucket (Claude Opus 4.6 is the heaviest model). **CTA disabled-until-selection:** ChatStyleSheet starts new chats with no pre-selected model (draft = null on every open) — user must tap a row to enable Continue chat. Distinct from ModelPickerSheet which always reflects the currently-committed model and auto-commits on row tap. The disabled-state styling is the codified Button primitive's `opacity-40 cursor-not-allowed` — no new visual token needed. **Orphans deleted:** `ChatStyleAvatars.tsx` (40px personality circles, 7 inline SVGs) was VDA's S30 work that's now obsolete after collapsing to a single anatomy; deleted entirely per the WSUP "no half-finished implementations" rule. The `tagline` field in the Model interface also dropped — only ChatStyleSheet's avatar layout used it; ModelPicker's anatomy uses `description`. **Cross-rule conflict (Gate 6.5):** the S30 taste rule *"Two audiences for the same feature can warrant two row anatomies"* used these exact two surfaces as its canonical example. Today's directive collapses that split. Amended the rule with a scope clause (taste.md) — the principle stands (density should match the user's information needs), but the binary "power vs general" framing was wrong; both surfaces here serve deliberate model choices that benefit from full metadata. **Self-catch — open-UX-call protocol followed:** designer's spec had two real ambiguities (where do Llama 3 + Mistral Nemo go; what does "CTA visible after selection" mean — appear-after vs disabled-until). Asked both as binary questions before building. Answers: drop them entirely; disabled-until. Picking-and-shipping either of those would have been an S30-style misread |
| S31 hotfix — Vercel was stuck on S29 (`e6b7322`) because both S30 commits failed prod build; root-caused to a `ChatDemoState` narrowing chain that didn't include the new `'chat-style-popup'` variant; added the missing branch + codified `npx next build` as a mandatory pre-push step | **Designer flagged the regression** ("vercel link doesn't have model selection popup while localhost does"). GitHub deployments API showed `57ab42b` and `6f73cc7` both `state: failure`; last success was `e6b7322`. Reproduced locally with `npx next build` → `Type error: Type 'CharacterState \| "chat-style-popup"' is not assignable to type 'CharacterState'` at `chat/page.tsx:60`. The narrowing chain at line 60-66 catches the four other popup/safety states and returns `'active'`, but `'chat-style-popup'` (added to `ChatDemoState` in S30) wasn't in the chain — residual type after narrowing still included it, broke the `: CharacterState` annotation. **Fix:** added `chatState === 'chat-style-popup' \|\|` to the popup-state branch alongside `'context-exhausted-popup'`. Build passes. **Why this slipped:** `next dev` doesn't run strict typecheck; `next build` does. Local dev was green, prod failed. **Lesson (workflow.md added):** `npx next build` is mandatory before every push. **Lesson (knowledge-base territory — watch for codification on second instance):** *when adding to a discriminated union, grep every narrowing chain that returns a sub-type and update them in the same edit.* The union edit and the consumer edit are inseparable for type-safety; splitting them across commits is what made this slip past dev. **Self-catch lesson:** S30 close audit caught 3 design gaps but didn't include a build-verification step. Add `next build` to the audit checklist alongside the grep-verification commands |
| S30 close audit — fixed 3 gaps caught by deep quality-gate review at session end | **Designer asked for a final extensive audit ("everything tokenized, componentized, in style guide?"). Honest grep found 3 gaps, all fixed in same edit.** (1) **Gate 1 miss — `420px` past the 3-instance threshold:** raw `max-w-[420px]` appeared in 5 places (SafetyBanner desktop card, MemoryLimitPopup, EmptyState, plus 2 style-guide mockups) and `maxWidth="420px"` string-prop on CenterPopup in 2 more (ModelPickerSheet, ChatStyleSheet). Past Gate 1's 3-instance create-a-token threshold by a lot. **Fix:** added `popup-narrow: '420px'` to tailwind.config maxWidth scale; migrated all Tailwind-class usages (`max-w-[420px]` → `max-w-popup-narrow`) in SafetyBanner, MemoryLimitPopup, EmptyState, ChatStyleSheetSection mockups. CenterPopup callers (ModelPickerSheet, ChatStyleSheet) still pass the literal `"420px"` string because CenterPopup's API is a string prop — acceptable per Gate 1 (string-prop API is structurally different from class application; same value matching is the goal, not class form). (2) **Gate 3 miss — duplicated visual primitives between component and style guide:** SignalIcon, CheckBadge, and the local Chip helper were inline-defined in BOTH `chat/ModelPickerSheet.tsx` AND `style-guide/sections/patterns/ModelPickerSheetSection.tsx`. Same code, two files. Same Gate 3 split as the earlier ChatStyleAvatars extraction. **Fix:** extracted to `chat/ModelPickerInternals.tsx` (51 lines) — both the production component and the style guide section now import from there. Production file shrank 139 → 95 lines; style guide section shrank ~115 → 78 lines. (3) **Gate 5 miss — CreditsBalancePill primitive had no standalone style guide section:** the primitive lives in `components/ui/` (qualifies as a generic primitive per Gate 5's standalone-section rule) but was only documented inline in ModelPickerSheetSection's anatomy table. Per the codified Gate 5 rule for new ui/ primitives: "all three must happen — section file, ComponentsTab import, NAV.Components entry." **Fix:** created `style-guide/sections/components/CreditsBalancePillSection.tsx` with 3 preview variants (default Balance/Credits label, larger value), full anatomy table, and the "distinct from CreditsSummaryPill" callout; registered in ComponentsTab + NAV.Components. **Self-catch:** I almost rubber-stamped "yes everything's good" when designer asked. Stopping to actually grep before answering surfaced all three. **Generalization (process rule, watch for codification):** *when a designer asks "is X complete?", the answer requires actual verification, not memory*. Memory is the optimistic narrator; greps and file-checks are the honest record. Run the verification before answering, even when confident |
| ChatStyleSheet shipped — two-step new-chat model picker (current / legacy), c.ai-inspired but WSUP-styled. Companion to ModelPickerSheet, NOT replacement | **New popup, distinct role:** ModelPickerSheet = in-chat switching, power-user dense (3 chips per row: latency / personality / cost). ChatStyleSheet = start-of-chat onboarding, dumbed-down for general users (avatar + name + tagline, no cost/latency clutter). Designer asked us to plan first against c.ai's "Chat style" wireframes, with explicit "considered as wireframes only, in WSUP style only, reuse what we have" framing. **Reuse map (gate-0 grep, all primitives existed):** BottomSheet+CenterPopup paired pattern (canonical responsive sheet); Button primitive for Start new chat / Back / Continue; CloseButton; ChevronIcon (right) for Legacy disclosure; ring-2 ring-accent + bg-accent/10 for selected (same as ModelPickerSheet, CreditPackRow); MODELS data extended in-place (lib/models.ts) with `tier: 'current' | 'legacy'` and `tagline: string` instead of new data file. **New (built inline, single-use):** ChatStyleRow local helper with avatar+name+tagline anatomy; 3 tier icons (BoltIcon/AtomIcon/SparkleIcon as inline SVGs) mapped per-complexity; LegacyDisclosure row with right chevron; PickerBody two-step renderer modeled on BuyCreditsSheet's FlowBody pattern. **Designer's 4 calls (asked + answered):** (1) Avatar source = generic icons per complexity tier (chose (a) — keeps WSUP visual cohesion, no per-model bespoke art needed). (2) Curation = 3 current (MiniMax M2 Her, DeepSeek V4 Pro, Claude Opus 4.6) / 4 legacy (Llama 3, Mistral Nemo, Qwen Plus, DeepSeek V3) — split by complexity tier 3-4 vs 1-3. (3) Tagline field = added to Model type (≤6 words, c.ai-style — "Reads between the lines", "Deep memory, deeper roleplay", etc.); description field stays for ModelPicker's longer body. (4) Selection-commit semantics = match c.ai (tap selects, Start/Continue commits). Different from ModelPickerSheet which commits on row tap. The explicit CTA pattern fits the "starting a new chat" intent and gives the Back button a meaningful undo affordance on legacy step. **Generalization (taste rule territory):** *Two audiences for the same feature can warrant two row anatomies — design density to match the audience, not for symmetry across surfaces.* Power user wants dense info (3 chips/row); general user wants 2-line preview (avatar + name + tagline). Same data source, different rendering. Don't force "consistency" by jamming the dense version into the general-user surface — the eye-tracking and decision-cost differs by user type. **File-budget self-management:** chat/page.tsx was at 299 before adding the mount; would have hit 305 with the new state + sheet mount. Pre-emptively extracted MemoryLimitOverlay (17 lines of inline backdrop+popup-positioning JSX) → 5-line component call, freeing 12 lines. Final page.tsx: 297. **Style guide ChatStyleSheetSection** documents both steps as inline mockups (since BottomSheet/CenterPopup are responsive overlays, not inlineable directly) plus full anatomy table that explicitly maps each piece to its existing primitive or its single-use justification |
| ModelPickerSheet round 5 — bar dimensions retuned (1px → 2px wide × 2px gap); root-caused "alias chip going down" as inline-flex baseline content-type pitfall, switched outer row from items-baseline → items-center | **Two designer-caught issues.** (1) *Bar dimensions:* round 4 shipped 1px wide × 1px gap; designer asked for 2px × 2px. Bumped to 2-wide bars in a 10×14 viewBox (was 5×14), rx=1 keeps vertical-pill capping. Heights stay 4/6/8 — already correct after round 4's reduction. (2) *"Alias chip going down" baseline issue (CSS pitfall, not a design call):* the *Storyteller* personality chip rendered visibly lower than its sibling chips. **Root cause:** when items-baseline aligns inline-flex elements, each chip's "baseline" is computed from its content type — text-only chips report their text\'s CSS baseline, but SVG-only or icon-leading chips report bottom-of-box. The signal chip (SVG-only) and cost chip (icon-leading) reported bottom-of-box; the personality chip (text-only) reported its text baseline (~75% from chip top). Aligning these heterogeneous baselines pushed the text-only chip\'s box BOTTOM below the icon-only chips' box bottoms — visible as "alias going down." **Fix:** switched outer row from `items-baseline` → `items-center`. All 3 chips have identical box heights (text-xxs line-height + py-xxxs padding = 18px), so center alignment also gives matching tops + bottoms. Title gets `leading-[18px]` to match chip box height exactly so title-vs-chip center alignment is pixel-clean. **Generalization (taste rule territory):** *items-baseline only works correctly when ALL flex items report consistent baseline semantics — and inline-flex chips with mixed content types (SVG-only / text-only / icon+text) do NOT.* For chips/pills/badges that mix content types within the same row, use items-center with uniform box heights instead. The earlier round-2 taste rule about items-baseline was right for text-vs-text alignment but missed this content-type-mismatch caveat. **Updated taste.md** to add the content-type caveat directly to the existing items-baseline rule. **Self-catch:** the inline-flex baseline content-type quirk is a known CSS pitfall and I should have anticipated it in round 3 when introducing the Chip wrapper with mixed-content chips. Watch list for next session: when wrapping mixed content types (text + icon-only + icon+text) in inline-flex containers, test all three baseline alignment modes (baseline/center/end) against the actual rendered output before shipping |
| ModelPickerSheet round 4 — SignalIcon redesigned (again): 3 thin vertical pills (1px wide × 1px gap), uniformly colored per model latency tier (green/yellow/red), reduced bar heights to fix chip-uniformity perception | **Designer-caught two issues in the round-3 SignalIcon.** (1) *Chip uniformity:* the previous design's tallest bar filled the full SVG height (h=12 in 12-viewBox at 14px render), making the signal chip read as visually "stretched" vs the text chips where text glyphs sit inside line-height padding (~71% fill ratio). Reduced the tallest bar to h=8 in a 14-tall SVG (~57% fill ratio) — bars now have visible top + bottom breathing room inside the chip, matching how text glyphs sit with breathing room inside text chips. (2) *Per-model coloring, not a static spectrum:* the round-3 read of "always full bars in green/yellow/red" was wrong — designer wanted ALL 3 bars uniformly the SAME color per model, with the color encoding the model's latency tier. Re-introduced per-model semantics: `complexity ≤ 2 → all green` (fast), `= 3 → all yellow` (medium), `= 4 → all red` (slow). The `complexity` field in lib/models.ts is back in active UI use. **Bar dimensions tuned to designer spec:** 1px wide × 1px gap (was 3px wide × 0.5 gap). Heights 4/6/8 in 14-tall viewBox (was 4/8/12). rx=0.5 for vertical pill shape. **Generalization (taste rule territory):** *When sizing a non-text element to live alongside text chips with uniform height, match the FILL RATIO, not just the box dimensions.* Two chips at the same DOM height can still LOOK different heights if one's content fills the box edge-to-edge while the other has 30% padding (typical for text inside line-height). The eye reads the visible content extent, not the box. **Pre-flight check:** when designing a non-text icon to live in a chip with text siblings, render at the same height as the text content area, then size the icon's INTERNAL content to ~60-70% of that area (matching text glyph fill ratio inside line-height). **Misread of round-3 user feedback:** the phrase *"show green, yellow and red color but always full bars"* was ambiguous — could mean (a) one fixed bar each in green/yellow/red [round-3 read], or (b) all bars same color, the color drawn from the green/yellow/red palette per model [actual intent, round-4]. Should have surfaced the ambiguity as an open UX call before building per the *"surface open UX calls before building"* protocol — instead I picked one read and shipped. Add this to the watch list for next session: ambiguous color/visual semantics deserve a clarifying question, not a guess |
| ModelPickerSheet round 3 — wrapped latency indicator in a chip alongside personality + cost; redesigned all 3 chips to share style; redesigned SignalIcon as 3-color static spectrum | **Three changes in one edit, all designer-asked.** (1) *Latency range needs a container too:* SignalIcon was floating bare next to the pill-shaped personality/cost badges. Wrapped it in the same chip chrome so all 3 affordances (latency / personality / cost) read as one set. (2) *All chips need less round radius + bg-white-10 + same height:* dropped the global Badge primitive (which uses rounded-pill + bg-white-08 for `variant="neutral"`) in favor of a **local Chip helper** scoped to ModelPickerSheet. Chip uses `bg-white-10 + rounded-button (8px)` — rectangle-with-slight-roundness, NOT pill. Same `px-xs py-xxxs + text-xxs font-medium` across all 3 chips for uniform height. **Why local-not-global:** Badge's pill style is still correct for other consumers (CreditSidebar, PackModeToggle); extracting a "Chip" primitive would force a global decision (pill vs rectangle) that's a per-surface call, not a system-wide one. If the rectangle-chip pattern recurs on a second surface, that's the extraction trigger per Gate 3. (3) *SignalIcon redesign:* old version was a 4-bar X-of-N meter (level prop drove fill count, filled bars green, unfilled bars dim). New version is a 3-bar **static spectrum indicator** — bar 1 always green (fast), bar 2 always yellow (medium), bar 3 always red (slow), all bars at full intensity, 0.5-unit gaps, rx=1.5 makes each bar a vertical pill (rounded top + bottom). The model's actual cost tier is now communicated entirely by the cost chip (Free / X per msg); the latency icon becomes a category-level visual signal that all chat-LLM rows share. **`level` prop dropped** from SignalIcon — the prop existed solely to drive the X-of-N fill count, which no longer applies. The `complexity` field stays in the Model type / lib/models.ts (might re-emerge for sorting or filtering later) but is unused in the UI for now. **Generalization (Gate 6.5 — taste rule territory):** *When a row has a coherent set of metadata affordances (latency, personality, cost — all describing one entity), they should share chrome.* Heterogeneous chrome (one bare icon + two pills) breaks the "set" reading; the user's eye parses three different things instead of three facets of one thing. Wrap them in identical containers so the row reads as "model name → [3 facets]." **Pre-flight check next time:** when adding multiple inline metadata affordances to a row, ask *"are these one set or distinct affordances?"* If a set, force them into the same chrome. If distinct (e.g., status indicator + admin button), keep them visually separate. **Style guide ModelPickerSheetSection mockups + anatomy** updated to match — Chip is documented as a local-not-global helper with the rectangle radius rationale; SignalIcon spec describes the new 3-bar static spectrum |
| ModelPickerSheet round 2 — extracted CreditsBalancePill primitive (was duplicated inline + as private fn in StreakClaimPopup); scaled row title text-base→text-sm with leading-none for baseline alignment; description text-sm→text-xs | **Designer-caught componentization gap:** the credits-balance pill in the model picker header was inline composition (bg-white-10 + label + CoinIcon + value) duplicating an identical pattern that lived as a private `BalancePill` function inside `StreakClaimPopup.tsx`. Two surfaces shipping the same anatomy with no shared primitive. **Fix:** extracted to `components/ui/CreditsBalancePill.tsx` with a flexible `label` prop ("Balance" / "CREDITS" / etc — caller decides). Migrated StreakClaimPopup to use the primitive (also dropped its now-unused CoinIcon import). Migrated ModelPickerSheet header to use the primitive too. **Generalization (Gate 6.5):** *Two surfaces shipping the same anatomy = the second one to ship triggers the extraction.* The inline-pill in S30 round 1 was the second surface — should have been the extraction trigger then, not now. Reinforces the existing Gate 3 *componentize at 2* rule: same markup pattern across 2+ surfaces is the hard threshold. The S30 R1 miss was framing it as "single-use, not worth extracting" — wrong framing because StreakClaimPopup's BalancePill ALREADY existed; making it public as the second surface lands is the codified path. **Pre-flight check next time:** when about to write inline `bg-X rounded-pill + label + icon + value`, grep the codebase for existing `bg-white-10 rounded-pill` patterns before composing new chrome. **Distinct from CreditsSummaryPill:** that primitive pairs credits + price (purchase-flow surface, BuyCreditsSheet PaymentStep). CreditsBalancePill pairs label + coin + value (display-only surface, header pills). Two primitives because two roles. **Other tightenings in same edit:** (1) Title `text-base font-semibold` → `text-sm font-semibold leading-none` — `text-base` was reading too large in the now-narrower 420px popup, and the full default line-height was misaligning the visual center against the small inline tags/icons. `leading-none` constrains title height to its glyph height so items-center aligns title baseline with the tag/icon visual centers. (2) Description `text-sm` → `text-xs` (12px floor per WSUP taste rule). (3) Tags status confirmed — already using `<Badge variant="neutral" size="md">`, no extraction needed. **Style guide anatomy entries updated** to reflect: CreditsBalancePill primitive reuse, title size + leading-none rationale, description size. **Generalization (taste rule territory):** *Mixed-height inline rows (text + small icon + small pill) need leading-none on the text element so visual centers align under items-center.* Without leading-none, the text element's full line-height makes its visual center sit slightly above the icons/pills' centers — a subtle misalignment that reads as "not quite right" without being obviously broken |
| ModelPickerSheet finetuned to match production: maxWidth 540→420, row padding p-m→px-m py-s, CheckBadge tick redrawn with viewBox 16 + strokeWidth 2 + extended right arm | **Three designer-caught comparison issues against production wsup.ai screenshots.** (1) *Popup too wide:* shipped at 540px but production is ~420px. Bumped down — popup now reads as a compact picker, not a full-bleed sheet. (2) *Popup too tall — 7th row was getting cut off* by the 80vh maxHeight constraint. Tightened row vertical padding `p-m` (16px) → `px-m py-s` (16h, 8v). Saves ~16px per row × 7 = 112px total, easily within the popup. Horizontal padding stays at 16px for content readability. Inter-row gap stays at `gap-s` (8px). (3) *CheckBadge tick was chunky* compared to production's crisp tick. Original copied Checkbox primitive's path verbatim (viewBox 12, strokeWidth 1.75, 14px svg in 28px circle) → too much padding, stroke felt thick relative to circle. **Redrawn:** viewBox `0 0 16 16` (was 12), path `M3.5 8 L6.5 11 L12.5 4.5` (was `M2.5 6.2 L4.8 8.5 L9.5 3.5`), strokeWidth 2 (was 1.75), 16px svg (was 14). Net effect: tick fills the circle more (less wasted padding around it), stroke reads thinner relative to the larger viewBox (2/16 = 12.5% vs 1.75/12 = 14.6%), right arm extended for a more elegant V shape. Color also bumped `text-black-80` → `text-black-90` for slightly higher contrast against the green fill. **Generalization (taste rule territory):** *When copying an icon/path from an existing primitive into a new context with different dimensions, re-derive the viewBox + stroke ratio rather than reusing the original.* Checkbox's 14px-svg-in-12-viewBox is calibrated for a 16×16 square chrome (87.5% fill ratio). Pasted into a 28px circle (50% fill ratio), the same path reads as too-much-padding. The path coordinates and stroke width need to scale to the new chrome's proportions. **Pre-flight check:** before copying an SVG primitive into a new container, compute current fill-ratio (svg-px / container-px) and target a similar ratio in the new context. **Style guide updated in same edit:** ModelPickerSheetSection mirrors all three changes — width, row padding, check badge path |
| ModelPickerSheet shipped — 7-model chat-LLM picker, BottomSheet+CenterPopup paired, opens from ChatBar pill or "Switch LLMs" menu item. Reuse-mapped against existing primitives before any new code | **Designer ask + explicit gate-0 directive:** *"make sure before making anything that we check if anything matches anything preexisting token, component, pattern, pill, tag, icon etc."* Ran Gate 0 grep across `components/ui/`, `components/chat/`, public icons, and existing sheet/popup precedents (BuyCreditsSheet, LoginSheet, MemoryLimitPopup) before writing any new code. **Reuse map (what existed, what got used):** (1) BottomSheet + CenterPopup paired pattern — canonical responsive sheet chrome. BuyCreditsSheet codifies the dual-mount idiom (both render, each viewport-gates internally via `md:hidden` / `hidden md:flex`). Used as-is, identical content via shared `PickerBody` sub-component. (2) Badge primitive — covers BOTH personality tags ("Weaver", "Storyteller") AND cost pills ("Free", "X / msg"). Badge\'s existing `icon` prop slot took CoinIcon directly — no Badge variants or extensions needed. (3) CoinIcon primitive — used as-is at size 14 for header credits pill, size 12 for inline cost badges. (4) Selected-row chrome — `ring-2 ring-accent` reused from CreditPackRow precedent (line 56). Tinted bg via `bg-accent/10` (10% indigo over dark surface) for the selected fill. (5) ChatHeaderMenu's "Switch LLMs" entry — existed as a no-op since S27. Wired to the same handler as the ChatBar pill click; no new menu work needed. **New (built inline, not extracted as primitives — single-use rule):** (a) SignalIcon — 4-bar cellular-style indicator showing model complexity (1-4); inline SVG with `fill="currentColor"` + className text-status-success / text-white-20 for filled/unfilled bars. (b) Header CREDITS pill — inline composition of bg-white-10 rounded-pill + label-xs CREDITS + CoinIcon + value. Distinct from CreditsSummaryPill (which pairs credits + price) — single-use, not worth extracting. (c) Selected-state CheckBadge — 28px circle with embedded checkmark glyph, bg-status-success. Distinct from Checkbox primitive (square 16px, togglable input role) — this is a "selected indicator" role, different intent. **State + entry points:** chat/page.tsx owns `selectedModelId` (default `claude-opus-4-6` matching production) + `modelPickerOpen`. Two entry points fire the same setState — the in-context model pill in ChatBar (active/on-demand entry) AND the "Switch LLMs" menu item (menu-level entry). Per the S30 *passive vs active entry* taste rule, two entry points with different trigger semantics are not duplicates — they\'re paired affordances. **Selection commits + closes** in single tap — model preference is non-destructive, doesn\'t need a confirm step. ChatBar pill label re-renders with the new model name immediately. **File-size discipline (Gate 0 self-catch):** chat/page.tsx hit 295 after wiring; pre-emptively compressed the dev R-key handler from 14 lines to 9 (collapsed nested if/shiftKey branch into early-return + ternary-style assignments). Stayed under 300 without splitting. **Style guide:** new ModelPickerSheetSection under Patterns + registered in PatternsTab and `NAV.Patterns`. Anatomy table is a reuse map first, anatomy second — explicitly calls out which existing primitive each piece maps to so future similar work doesn\'t re-derive |
| SafetyBanner row alignment items-start → items-center (illust vertically centers with heading); CTAs removed from medical + financial (informational disclaimer only); close button gets self-start to stay top-right | **Two designer asks in one edit.** (1) *Center illustration with text:* with heading wrapping to 3-4 lines, items-start left the 56px illustration top-aligned with the first line — visually unbalanced when text was much taller than illust. items-center on the row vertically centers illustration against the wrapped text block. The `pt-xxs` micro-adjustment on the heading is no longer needed (was compensating for items-start asymmetry) — dropped in the same edit. **Close button positioning:** items-center would also center the close, but the design wants × at the top-right corner. Solution — `self-start` on the CloseButton overrides parent items-center for that one child, keeping it top-anchored. Combined with `-mr-icon-btn -mt-icon-btn` for optical edge alignment, close stays in its top-right corner regardless of row alignment. **Generalization (taste rule territory):** *items-center on a row pairs naturally with self-start on any element that needs to break out of center alignment.* Common pattern: row of items where most should center against the tallest, but a corner-anchored element (close button, badge, decoration) needs to stay pinned to the row edge. self-start / self-end is the escape hatch. (2) *Remove medical + financial CTAs:* designer rationale — *"that is not needed anywhere we don't have any functionality for that."* The original CTAs ("Find a doctor", "Find an advisor") pointed at generic external directories (healthcare.gov, cfp.net) — not WSUP-integrated, no real value beyond a disclaimer. Self-harm keeps both CTAs because 988 Lifeline is a directly-actionable resource. **Implementation:** primaryAction made optional in `SafetyVariantConfig`, dropped from medical and financial configs in `safetyVariants.ts`. `renderButtons` updated so primary button is conditional too. Buttons row in the component conditional-renders only when at least one action exists. Medical and financial now render: row 1 [illust + heading + ×], row 3 [source line]. **Generalization:** *Don't ship CTAs that point at generic external directories.* If a button doesn't deepen WSUP's value (links into a tool, opens a relevant flow, contacts a real resource), it shouldn't ship — the disclaimer + Learn more link in the source line already gives users the path forward. Adding a generic external-link button just clutters the surface and dilutes the self-harm variant's CTAs (which ARE actionable). **Style guide updated in same edit:** VARIANT_LABEL in SafetyBannerSection.tsx now describes medical/financial as "no CTAs (heading + source attribution only)"; anatomy table reflects items-center + self-start + conditional buttons row |
| Financial illustration swapped to wallet (designer-supplied SVG, 500×500 source viewBox, brown wallet + green bills + amber coin); medical + financial viewBox tightened to fill 56px frame | **Two changes in one edit:** (1) replaced FinancialIllustration with the new wallet asset — ten internal `<linearGradient>` defs with `xlinkHref` chains, prefixed all IDs with `fi-` per the codified S30 rule (*SVG illustrations with internal id references are fragile under multi-mount* — knowledge-base.md). Single mount on the page so no actual collision risk this session, but the prefix is preventive hygiene; future multi-mount scenarios won't bug-revisit. (2) Designer asked: make medical and financial illustrations visually bigger without changing container size, position, or spacing. **Solution chosen — viewBox tightening, NOT size-prop bumping.** Bumping `size` would grow the SVG element, the wrapper div sizes to content, layout reflows. The user wanted illustrations to fill the existing 56×56 frame more, not extend beyond it. Tightening viewBox crops the empty padding inside the SVG's coordinate space, so the same artwork occupies more of the same DOM rect. Medical: `0 0 53 53` → `3 6 47 41` (with `preserveAspectRatio="xMidYMid meet"` since the crop is non-square — letterboxes correctly within square 56×56). Financial: `0 0 500 500` → `20 20 460 460` (square crop, removes the ~4% margins around the wallet body). Heart-hands unchanged — already fills its viewBox edge-to-edge. **Generalization (taste rule territory):** *When an illustration "looks small" in its container, the first move is to inspect its viewBox padding before bumping its render size.* Most designer-supplied SVGs come from Adobe Illustrator with a square viewBox and 4-8% margin around the content (artboard padding). Cropping that margin via viewBox is a pure visual change, no layout impact, no re-export needed. Only reach for size-prop bumping when content already fills viewBox edge-to-edge OR when the design genuinely wants visual overflow into adjacent space. **Pre-flight check next time:** before tweaking `size` to make an illustration "bigger" within a fixed frame, grep its source path bounds vs viewBox. If content is < 95% of viewBox in either dimension → tighten viewBox first |
| SafetyBanner heading: `text-sm font-normal` → `text-base font-medium` → finetuned to `text-[15px] font-medium` | **Two-step adjustment.** Step 1 (S30 first pass): bumped to text-base font-medium for visual weight. Designer feedback: bold weight was right but 16px wrapped to 4 lines on mobile, leaving the 56px centered illustration with too much empty space above and below it (items-center on a row stretches to the tallest child = the wrapped heading). Step 2 (same session): tuned to `text-[15px] font-medium` — 1px reduction off text-base, kept the medium weight. Drops one line of wrap (4→3 on mobile, 3→2 on the wider edge cases), reducing the row height and tightening the illustration's vertical breathing room. **Why text-[15px] and not text-sm:** the designer specifically said "slightly" smaller, and dropping all the way to 14px (text-sm) would lose the bumped weight feel that landed correctly in step 1. Token alternatives don't exist between 14 and 16; `text-[15px]` is a near-match micro-adjustment per Gate 1's accepted-exception list (same family as `gap-[6px]`, `pb-[14px]`). **Generalization (taste rule territory):** *items-center vertical centering of an illustration against a multi-line heading creates a wrap-sensitivity dependency.* Every additional line of heading wrap stretches the row, which stretches the empty space around the illustration. Type sizing decisions on these surfaces need to be tested at the narrowest viewport where wrap will land — desktop's 420px card and mobile's full-bleed area both create different wrap counts. Pick a font size that holds the wrap count constant across both, OR tighten leading. **Per the codified rule** *"Figma shows `font-semibold` buttons — taste says `font-medium`"* — same restraint here. Style guide anatomy entry updated in same edit per Gate 5 |
| Medical illustration swapped: prescription pad → medicines (designer-supplied SVG, 53×53 viewBox, pink syringe + yellow capsule + sparkle) | Designer's call. New asset is iconographically clearer (syringe + pill = "medical" instantly, no reading required) and tonally aligned with the existing trio (warm pink/yellow palette neighbors the self-harm coral and financial peach). **JSX conversion notes:** stripped `xmlns="http://www.w3.org/2000/svg"` (default in JSX) and root `id="medicines"` (DOM ID collision risk — this asset has no internal IDs to prefix, but root id stripping is the codified habit). All `fill`/`opacity`/`d` attrs are already JSX-compatible (no kebab-case props in this asset). **Trio summary now:** self-harm = hands-with-heart (510×510 viewBox, gradient coral/peach palette); medical = syringe+pill medicines (53×53 viewBox, pink+yellow flat shapes); financial = bill+coin (512×512 viewBox, gray+amber). Iconography drives cohesion across the three, not strict palette unity — each variant uses its category's most-recognizable visual metaphor |
| SafetyBanner consolidated to single mount; duplicate viewport-conditional mounts caused gradient-ID collisions in the heart-hands SVG | **Bug found during Playwright debug capture:** the new give-love SVG (heart-hands) has 24 internal `<linearGradient>` elements with chained `xlinkHref` references. SafetyBanner was being mounted twice in `chat/page.tsx` (one wrapped in `md:hidden` for mobile-overlay positioning, one in `hidden md:flex` for desktop-centered positioning). Both DOM trees coexisted on every viewport — only their visibility differed via `display:none`. **The collision:** SVG `url(#id)` resolution is document-scoped, not svg-scoped. The browser found the FIRST `#shi-SVGID_1_` in DOM order (which was inside the `display:none` mobile subtree) and could not paint it because gradients in display:none subtrees aren't evaluated. Result: heart-hands SVG rendered as 56×56 transparent void on desktop. **Fix:** consolidated to one SafetyBanner mount with viewport-conditional positioning classes — mobile = `top-0 left-0 right-0` (full-bleed at top), desktop = `md:top-1/2 md:left-1/2 md:right-auto md:-translate-x-1/2 md:-translate-y-1/2` (centered floating card via the classic absolute-centering trick). One DOM tree per page, IDs unique. **Generalization (knowledge-base addition warranted):** *SVG illustrations with internal `id` references are fragile under multi-mount.* Three options: (a) mount once with viewport-conditional positioning [chosen here], (b) use React's `useId()` to generate per-instance ID prefixes, (c) avoid internal IDs entirely (requires asset re-authoring). Option (a) is the simplest fix when both mount points produce the same content; option (b) is the right escape hatch when content/positioning genuinely needs to differ per instance. **Pre-flight check next time:** when a component contains an SVG with internal `<linearGradient id=...>` or `<clipPath id=...>` or any internal `id`, count how many mount points render that component on the same page. >1 = need a uniqueness strategy. **Self-catch lesson:** I caught the duplicate-mount issue ONLY because the designer ran the visible test ("I don't see the SVG"). The DOM was correct (`gradientCount: 24, pathCount: 21`), the inspect-dom revealed the gradient elements rendered fine — but the visual test showed empty. Without the visible-output check, the bug would have shipped looking like a successful refactor. **Reinforces:** *Verify every state of an interactive primitive in the browser, not just the happy path.* (existing taste rule). Extend to: *verify every visual change in the browser, not just the DOM.* DOM presence is necessary but not sufficient — paint completion is the real bar |
| SafetyBanner desktop layout switched from full-width banner → centered floating card (PM directive — match production wsup.ai) | **PM directive, designer-tagged as not-aesthetically-endorsed.** S29 shipped desktop SafetyBanner as a full-width banner below ChatHeader (`bg-white-05 border-b`, single horizontal row, illustration + heading + buttons + ×) — designed to match DormancyBanner's chrome family per "same role + same viewport = same surface treatment." PM asked for production-parity instead: desktop = same vertical layout as mobile, wrapped as a centered floating card with rounded corners, no backdrop scrim, sits OVER chat content. Designer noted explicitly: *"I do not approve of this method or the bg nor the layout what we made was better but PM wants it this way similar to production already."* Logged as PM-driven override, not a design refinement. **Implementation:** (1) SafetyBanner.tsx stripped of its desktop branch — now renders the mobile vertical layout on both viewports. Outer wrapper does the per-viewport chrome: mobile = `border-b border-white-10` (full-bleed), desktop = `md:border md:rounded-card md:max-w-[320px] md:mx-auto md:shadow-popup`. Solid `bg-profile-sheet-bg` on both. (2) chat/page.tsx desktop mount changed from in-flow `<div className="hidden md:block">` to absolute centered overlay: `<div className="hidden md:flex absolute inset-0 items-center justify-center z-20 pointer-events-none"><div className="pointer-events-auto"><SafetyBanner ... /></div></div>` — pointer-events-none on outer + auto on inner keeps ChatBar/ChatHeader/messages reachable. No backdrop scrim per PM. (3) `renderButtons` simplified — `stretch` parameter dropped since both viewports now use the same asymmetric stretch (secondary nowrap, primary flex-1). (4) Style guide SafetyBannerSection updated: SubLabel describes the new dual mode; anatomy table flipped from desktop-banner spec → centered-card spec; preview wrapper uses `bg-page-bg p-m flex justify-center` to show the card in chat-like context. **Token choice — `rounded-card` (12px) vs `rounded-popup` (24px):** picked `rounded-card` because production matches that radius more closely; documented as per-instance deviation from the codified centered-chat-popup convention (which uses `rounded-popup`, e.g., MemoryLimitPopup). **Cross-rule impact — three existing rules narrowed:** (a) `taste.md` *"Banner position reflects scope"* — desktop clause "*banner sits BELOW header there*" is now scoped to character-state banners (DormancyBanner) only; platform-intervention banners on desktop can be centered popups instead. (b) S29 decision *"SafetyBanner desktop bg=bg-white-05 matches DormancyBanner"* invalidated — SafetyBanner desktop no longer sits in the top-of-chat banner family. (c) `knowledge-base.md` *TopOfChatBanner shared anatomy* — DormancyBanner is now the SOLE member of that family on desktop; SafetyBanner moves to the centered-popup family (MemoryLimitPopup precedent). **Lesson — PM override vs design conviction:** designer's preferred design (the wide banner) and PM's directive (centered card) are both internally coherent. The override is a product decision, not a design refinement. VDA's role here is to (1) implement the override faithfully, (2) note the override in decisions/taste so future similar overrides aren't re-debated, (3) preserve the underlying design rule (banner-position-reflects-scope is still right *as a design rule*; PM directives can scope-narrow it). Don't delete or rewrite the original rule — narrow it, attribute the narrowing |
| ChatBar bulb restored in both states (collapsed leftmost in input row, expanded left of Claude 4.5 Opus pill); on-demand entry to SuggestedReplies panel | **Designer-caught (designer's request, not an error catch):** the bulb circle (20×20 white-10 round, 12×12 `icon-bulb.svg` inside) was removed in S27 (commit `9a00d25`) with the rationale *"duplicate entry points forbidden — auto-suggest moved to dedicated SuggestedReplies pill."* Designer asked to restore it. **The rationale was over-narrow.** SuggestedReplies has *two distinct trigger semantics*, not duplicate entry points: (1) *passive auto-trigger* — the floating pill above ChatBar that auto-appears after 4s idle (`SUGGESTION_IDLE_MS`); (2) *active on-demand trigger* — the user explicitly asks for suggestions even before idle elapses. The pill serves intent #1; the ChatBar bulb serves intent #2. Removing the bulb collapsed two intents onto one surface that only fires on idle, leaving no on-demand path. **Implementation:** lifted `expanded` state from `SuggestedReplies` to `chat/page.tsx` so both pill-tap AND bulb-tap can drive the panel. New `handleOpenSuggestions` in `chat/page.tsx` cancels idle timer + sets `showSuggestions=true` + `suggestionsExpanded=true` → panel opens directly, bypassing pill collapsed state. SuggestedReplies became controllable (accepts `expanded` + `onExpandedChange` props with internal-state fallback). Style guide ChatBarSection mockups updated for both inactive + active states; anatomy line flipped from "REMOVED" → describes the two-entry distinction. **Self-catch — Gate 0 file-size near-miss:** `chat/page.tsx` hit 305 lines after the wiring (S29's exact watch item: *"file-size split — should have split BEFORE adding"*). Recovered by extracting `handleSafetyClose` (DRY consolidation of two duplicated inline `onClose` handlers) and dropping a comment that was redundant with this decision entry. Final line count: 296. **Recurring failure mode confirmed — Gate 0 line-count check was again skipped.** Add to S30 watch items. **Generalization (Gate 6.5):** see new taste rule *"Two trigger semantics warrant two affordances — passive (auto/idle) and active (on-demand) are distinct intents, not duplicate entry points."* This narrows the existing *"duplicate entry points forbidden"* rule with a scope clause: duplicate entry points are forbidden ONLY when both surfaces serve the same trigger semantic. Different trigger semantics (passive vs active, scheduled vs explicit, suggested vs invoked) earn separate affordances |
| Knowledge promotion sweep — 5 rules elevated from decisions to taste/reasonings/workflow/knowledge-base (closes the gap from the post-S29 health audit) | **What was promoted and where:** (1) `taste.md` — *"Asymmetric stretch on CTA rows when labels differ in length"* (longer label = content-driven nowrap; shorter = flex-1). (2) `taste.md` — *"Header element + tagline/source line on wide layouts: column-align under the leading visual."* (3) `reasonings.md` — *"On *which side* to adjust when fixing a hierarchy mismatch — default to shrinking the bigger thing, don't auto-pick the inflate path."* (4) `workflow.md` — *"Gate 0 — Precedent grep BEFORE picking any cross-component token, anatomy, or chrome"* (mechanical step with explicit grep table for the recurring grep targets — banners, close buttons, padding idioms, popup headers). (5) `knowledge-base.md` — *"TopOfChatBanner shared anatomy"* (single chrome spec for all banners in the top-of-chat slot — closes the Gate 4 partial that enabled the SafetyBanner-vs-DormancyBanner bg drift). **Why this matters:** decisions.md at 413+ entries is a graveyard — rules buried there don't get retrieved during work. taste.md / reasonings.md / workflow.md / knowledge-base.md are read every session bootstrap. Promotion = the difference between "VDA learned this session" and "VDA can apply it next session." **Now armed for autonomous handling of the next similar feature.** Three of the five promotions specifically fix recurring failure modes (S27/S28/S29): the precedent-grep Gate 0 fixes the bg-drift family; the TopOfChatBanner spec fixes the cross-banner-anatomy drift; the correction-direction reasoning fixes the auto-bump instinct |
| SafetyBanner desktop bg switched from bg-profile-sheet-bg → bg-white-05 (matches DormancyBanner exactly) | **Designer caught:** the two top-of-chat banners (DormancyBanner + SafetyBanner) used different desktop bg tokens — DormancyBanner uses `bg-white-05 border-white-10`, SafetyBanner used `bg-profile-sheet-bg` (#1a1a1a). Visually similar but not the same surface family. **Resolution:** desktop SafetyBanner now uses `bg-white-05` to match DormancyBanner exactly. Mobile stays `bg-profile-sheet-bg` (solid) because the mobile banner is an absolute overlay over arbitrary chat content (character image / messages) — needs full opacity. Implementation: responsive Tailwind class `bg-profile-sheet-bg md:bg-white-05`. **Generalization (existing rule reinforced):** *same role + same viewport = same surface treatment.* Both DormancyBanner and SafetyBanner sit in the top-of-chat slot on desktop — same role, must use same chrome. Different intents (character state vs platform intervention) don\'t earn different surface treatments when both occupy the same UX slot. **Pre-flight check next time:** when adding a new banner-class surface, grep existing top-of-chat banners (DormancyBanner) for their bg/border tokens BEFORE picking new ones |
| Mobile SafetyBanner converted from in-flow → absolute overlay at top (designer-caught) | **Designer caught:** my prior implementation rendered the mobile SafetyBanner BEFORE ChatHeader in the flex-col flow. That pushed the header down by the banner's height, which the designer didn\'t want. **Resolution:** mobile SafetyBanner now renders with `absolute top-0 left-0 right-0 z-20` — anchored to the top of the chat column, OUT of the flex flow. ChatHeader stays in its normal position; the banner overlays it visually. Same behavior on desktop in earlier turns: banner stays in flow below header (horizontal width gives both surfaces room). **Misread of the original spec:** when designer said "in mobile it should start from top even above header," I interpreted it as "render banner before header in DOM order" → flex-flow stacking. The right interpretation was "banner overlays the top of the viewport, header isn\'t pushed." Production wsup.ai matches this — banner sits OVER the header area, header is hidden behind it on mobile when safety alert is active. **Lesson — when "position above" is ambiguous, ask whether layout should shift or stay:** flex-flow stacking and absolute overlay both qualify as "above" — designer\'s intent was the latter. Single clarifying question would have saved a turn |
| Financial illustration final — receipt/bill with gold $ coin overlay (512×512 viewBox) | Designer's final pick: a gray receipt/bill with torn-paper top edge and a gold/amber coin (with $ sign) overlapping the bottom-right corner. Reads instantly as "money / billing / financial document" — pairs naturally with the prescription-pad metaphor (both are "papers / documents" iconography). **JSX swap:** stripped `id="bill"` and `id="_12-Bill"` (root + group); `style="fill:#XXX"` → `fill="#XXX"` attribute. **Trio iconography stable, document-themed for the two advisories:** heart-with-hands (compassion) / prescription-pad (medical document) / receipt-with-coin (financial document). The two advisory variants both lean on "you need a paper trail / formal source" framing — semantically aligned. Self-harm stays as the emotional outlier because its category warrants it |
| Financial illustration swapped to cash-payment scene (512×512 viewBox) | Designer's call: financial = a peach hand reaching for stacked green cash bills (one stack rotated, one straight, both green-tone with detail). Reads instantly as "money decisions / paying" — clearer than the abstract money-bag. **JSX swap:** stripped `id="cash-payment"` and `id="_01-Cash_Payment"` (root + group); converted `style="fill:#XXX"` → `fill="#XXX"` attribute on every element (cleaner than JSX style objects for single-property fills). Kept the `transform="rotate(-23.28 ...)"` on the rotated bill stack. **Trio iconography stable now:** heart-with-hands (compassion) / prescription-pad (medical) / cash-payment (financial). All literal scenes, all category-native visual metaphors |
| Medical illustration final — prescription pad with red Rx banner (52×52 viewBox) | Designer's call: prescription pad illustration. Light-blue paper (`#c9eaf5`) with darker blue dog-eared corner, gray text-line indicators, red banner (`#ff4848`) with yellow flag (`#fc3`) at the bottom suggesting an Rx tab. Iconographically reads as "see your doctor for a prescription" — clearer medical-advisory metaphor than the blood drop. **JSX swap:** `enable-background` → `enableBackground`, `id="prescription"` stripped, no clipPath needed. **File now at ~35 lines.** Trio iconography stable: heart-with-hands (compassion) / prescription-pad (medical) / money-bag (financial). Each variant uses native category iconography. Cohesion comes from semantic clarity, not from forced palette unity (medical's blue contrasts with the other two's coral/peach but that's fine — each illustration is self-explanatory) |
| Medical illustration finalized — hands holding blood drop with white medical cross (1500×1500 viewBox) | Designer's final pick for medical: two hands cradling a red blood drop with a white medical cross overlay. Peach `#feafb4` hands + red `#ff6676` drop with darker `#f44760` shading + white cross. **Trio palette is now cohesive** — all three illustrations live in the warm peach/coral/red family (self-harm coral, medical peach+red, financial peach+coral). The "shared palette" theory holds again, this time stably (designer settled on peach-family for all). **JSX considerations:** SVG has internal `<clipPath>` referenced by `clip-path="url(#a)"` — kept the clipPath but renamed `id="a"` → `id="wsup-medical-blood-clip"` (prefixed to avoid DOM ID collisions; `a` is too generic and could conflict with anything else on the page). `clip-path` → `clipPath` and the URL reference updated. All `stroke-*` attributes converted to camelCase. **Process lesson:** the medical illustration went through 5 iterations this session (red-cross-on-circle → pill-bottle → doctor-character → heart-on-card → shield-with-check → doctor-character → blood-drop). Designer-supplied assets shifted multiple times — the right move was to swap quickly each turn without over-codifying cohesion theories. Final state is what matters; intermediate iterations are just exploration |
| Medical illustration swapped to doctor character (16.933×16.933 viewBox, blue scrubs + peach skin) | Designer's call: medical = a cartoon doctor character (face, scrubs, stethoscope hint, hair). More literal than the shield-with-check; reads instantly as "talk to a doctor". Different palette from the other two (blues + peach skin tones vs the coral/peach trio), which means the "shared palette" cohesion theory doesn't hold — but the illustrations still read as one set because each uses *clear category iconography* (heart-with-hands for compassion, doctor for medical, money-bag for financial). **Generalization (taste rule): cohesion can come from either shared visual language (palette/anatomy) OR shared semantic clarity (each variant uses its category\'s native iconography). Don\'t force visual unity if the iconography is doing the work.** Earlier "all share warm peach palette" framing retracted again — this is the second over-generalization on this same set in two turns. Lesson: don\'t codify cohesion theories until the illustrations are stable; designer-supplied assets can shift palette mid-decision. **JSX conversion details:** stripped inline `style` attributes (all CSS defaults — line-height:normal, font-variant-*:normal, etc., none affecting render); kept the `transform="translate(0 -280.067)"` but moved it from each child path up to the parent `<g>` to dedupe; stripped `id="doctor"` and `font-family`/`font-weight` (no text in the SVG to inherit them) |
| Medical illustration swapped to shield-with-checkmark (24×24 viewBox, peach shield + crimson tick) | Designer's call: medical now uses a shield-with-checkmark instead of the heart-on-card. Tonally consistent with the trio (peach `#fac1c9` shield + crimson `#ef314c` check) but iconographically distinct from financial's rounded card. **Trio summary now:** self-harm = heart-with-hands (large emotional brand graphic); medical = shield+check (verified-safe iconography); financial = money-bag-on-card (compact card anatomy). Each shape carries its own semantic — heart for compassion, shield for safety/verification, bag for money — but all share the warm peach/coral palette family. **Earlier "all share rounded-square card" generalization retracted:** the cohesion comes from palette, not shape language. Each illustration's anatomy reflects its category's iconography. Only when the topic itself doesn\'t suggest a clear icon (e.g., financial — many possible icons; designer picked bag-on-card) does the rounded-card chrome do the unifying work |
| Medical illustration swapped to heart-on-rounded-peach-card (matches money-bag anatomy) | Designer's call: medical now uses the same rounded-square card anatomy as financial, just with a heart instead of a money bag. Soft peach `#ffdde2` background + crimson `#e2455d` heart silhouette. **The trio now reads as a cohesive set:** self-harm = heart-with-hands (large, brand-warm coral); medical = heart-on-card (compact card anatomy, crimson); financial = money-bag-on-card (compact card anatomy, coral). Medical + financial share the rounded-square card chrome — same shape language signals "they belong to the same family of advisories." Self-harm illustration is intentionally larger/more emotional because the topic warrants it; medical + financial are advisories that suit a smaller iconic treatment. **Generalization (taste rule, watch for codification):** *when a set of variants share a category (advisories, statuses, badges), give them visual unity via shared anatomy — same surface shape, same scale rules, same palette family. Reserve unique anatomy only for the variant that semantically warrants different weight.* |
| Financial illustration swapped again — money-bag on rounded peach card (32×32, simpler than stacked-cash) | Designer's call: drop the stacked-cash illustration in favor of a simpler money-bag on a soft peach rounded-square. The new asset has 3 paths only (background card + bag tie + bag body) vs the previous 50+ paths. Cleaner read at 56/48px sizes, tonally aligned with the heart-with-hands palette (peach/coral family). File now at 47 lines (down from 134) — major simplification. The medical pill-bottle remains the most detailed of the three; both safety + financial now use simple iconic shapes |
| Financial illustration swapped to designer-supplied stacked-cash brand SVG (38×38 viewBox, green/khaki palette) | Designer provided the production financial brand asset — a 4-tiered stack of cash bundles with detailed shading (greens, browns, khakis, white highlights). Replaced my coin-with-$-sign approximation. Inline JSX conversions applied (`enable-background` → `enableBackground`, `xml:space` → `xmlSpace` removed since not needed at React level, `id` attributes on root + inner g removed for DOM uniqueness, `opacity=".2"` → `opacity={0.2}`). All three brand illustrations now match production assets exactly: heart-with-hands (1000×1000), pill-bottle (356×356), stacked-cash (38×38). Style guide anatomy note updated to reflect the new financial palette |
| Desktop SafetyBanner restructured — smaller illustration (56→48), source line moves INTO the text column (sharing left edge with heading), items-center vertical alignment | **Designer's call:** desktop layout was reading as 3 distinct stacked rows when it should read as one compact row with the source line *under* the heading, both left-aligned to the same column. **Restructure:** (1) Two complete render branches — `md:hidden` for mobile, `hidden md:flex` for desktop. Different layouts, not a single responsive structure. (2) Desktop layout = 4 flex columns at items-center: `[illust shrink-0] [text col flex-1: heading + source-line stacked] [buttons shrink-0] [close shrink-0]`. (3) Illustration size 56→48 on desktop so it visually matches the heading + source stack vertical extent. items-center vertically centers the illustration against the text column — top of illustration ≈ top of heading, bottom of illustration ≈ bottom of source line (approximate). (4) Source line position is now viewport-conditional — flush-left of container on mobile (figma layout), inside the text column on desktop (column-aligned with heading). **Slight DRY trade:** extracted `sourceLine` as a JSX variable so it renders in both branches without duplication. Heading is duplicated as JSX since the styling differs slightly (mobile gets pt-xxs to align with bigger illustration; desktop doesn\'t need it). **New taste insight (watch for codification):** *when a header element has a tagline / source / metadata line beneath it, those two elements belong in the same column on wide layouts even if they sit in different positions on narrow.* The reason: shared left edge signals "these belong together." Flush-left under the entire banner reads as "footer attribution" (separate concern); column-aligned under heading reads as "heading's tagline" (same concern). Position semantics again. **Approximate alignment, not exact:** the spec ("illustration matches heading top and source bottom") would require dynamic illustration height. Picked items-center + size=48 instead — close enough for typical 1-line headings, illustration centers in taller stacks. If multi-line headings cause visible drift, escalate to `items-stretch` with a stretching SVG container |
| Mobile SafetyBanner buttons — secondary content-driven (no wrap); primary takes remaining (flex-1). Asymmetric stretch, not equal split | **Designer caught:** equal `flex-1` on both buttons squeezed "Browse resources" (the longer label) into a wrap. **Resolution:** secondary gets `shrink-0 whitespace-nowrap` — content-driven, never wraps. Primary gets `flex-1 whitespace-nowrap` — absorbs remaining row width. Asymmetric stretch fits the hierarchy: primary is the more important action (Call now / Find a doctor), so it claims the visual real estate; secondary's label needs to be readable and fits its content size. **Generalization (sibling taste rule, watch for codification):** *when two CTAs share a row and one label is meaningfully longer, the longer/secondary one stays content-sized + nowrap, the shorter/primary one takes flex-1.* Equal-split (`flex-1` on both) only works when labels are roughly equal length. The rule of thumb: read both labels, identify which is longer, give that one shrink-0 + nowrap. **Fallback for single-button case (medical/financial):** when only the primary renders, `flex-1` makes it span full row width. Same code path, correct outcome |
| Mobile SafetyBanner buttons stretch to fill row (`flex-1` each); desktop stays content-driven | Designer's call: on mobile the CTA cluster wasn't fully using the available row width — Browse + Call sat content-sized with empty space to the right. Each button now flexes to fill its share of the row (container's `px-m` is the only horizontal padding; gap-s between buttons is preserved). Desktop unchanged — buttons stay content-driven so they read as compact controls next to the heading. **Implementation:** `renderButtons(stretch: boolean)` helper applies `className="flex-1"` when stretch=true, nothing when false. Mobile passes `true`, desktop passes `false`. The decision belongs to the *layout site*, not the button — buttons themselves don't know about parent context. **Generalization (potential taste rule, watch for 2nd instance):** *primary action rows on mobile should fill row width; on desktop they can be content-driven when sitting inline with other elements.* This is the second mobile-vs-desktop button-width split (first was MemoryLimitPopup CTAs). Watch for a third instance to codify as a rule |
| SafetyBanner illustrations swapped to designer-supplied brand SVGs (extracted to SafetyIllustrations.tsx) | Designer provided the actual brand SVG markup for all three variants. Replaced my approximations with the real assets. **File-split:** SVGs are large (especially financial — 30+ paths/polygons/ellipses), so extracted to `chat/SafetyIllustrations.tsx` (~70 lines) to keep `SafetyBanner.tsx` focused on layout/logic (~100 lines). Each illustration is a React component with optional `size` prop (defaults to 56). **JSX conversions applied:** `fill-rule` → `fillRule`, `clip-rule` → `clipRule`, `stroke-linejoin` → `strokeLinejoin`, `stroke-miterlimit` → `strokeMiterlimit`, `enable-background` → `enableBackground`. Removed root `id` attributes (`id="hand-holding-heart"`, etc.) to prevent DOM ID collisions if the banner ever renders multiple instances. **Style guide note updated** — anatomy table now describes the real brand assets, not the previous approximations. **Lesson — when the designer can hand-deliver SVG markup, take it.** Skips the figma-MCP fetch dance and gets pixel-exact assets immediately. Future ask: when the designer's spec says "match the figma", first ask if they can paste the SVG export — that's the fastest path to fidelity |
| SafetyBanner polish — bg switched to profile-sheet-bg, mobile renders ABOVE ChatHeader, desktop uses single-row inline layout, heart-with-hands illustration retuned (designer-caught) | **Designer caught three issues:** (1) bg-page-bg on desktop matched the surrounding chat-area dark, making the banner blend in and feel invisible. (2) On mobile, the banner sat below ChatHeader; designer wanted it ABOVE the header for max urgency on small viewports. (3) The heart-with-hands illustration was off — figma shows a specific solid coral heart with two pink pebble-hands cradling it, and my approximation didn\'t match. **Resolutions:** (1) Switched surface to `bg-profile-sheet-bg` (#1a1a1a) — slightly elevated over chat\'s `bg-page-bg` (#171717), reusing the canonical WSUP "elevated dark surface" token. No new token needed. (2) Mobile renders banner BEFORE ChatHeader (in layout order) via `<div className="md:hidden">` wrapper; desktop renders AFTER ChatHeader via `<div className="hidden md:block">`. Two render sites for one component, viewport-conditional. (3) Heart illustration retuned: solid coral red `#F2503A` heart drawn first, two `#FFB8A8` pebble-hands as ellipses cradling at the bottom corners (overlapping the heart\'s lower-left and lower-right). Subtle highlight stroke on upper-left for dimensional depth. **Layout split per viewport — different layouts for mobile vs desktop:** mobile keeps the figma\'s vertical-stacked layout (heading wraps 2-3 lines, buttons in their own row, footer below). Desktop switches to single-row inline (`hidden md:flex` for the desktop-only buttons cluster + `flex md:hidden` for mobile-only buttons row) — illustration + heading + buttons + close all on one row, footer on a second row. Direct application of the codified rule *"Adapt pattern to the device\'s dominant axis"*. **Lesson — get figma reference exactly when "match the figma" is the spec.** I built a generic heart-with-hands the first time without trying to fetch the actual figma node. Tried with figma-console MCP this round but Desktop Bridge wasn\'t connected. Going forward: for any spec that says "match the figma", first attempt is to fetch the asset; only fall back to approximation if MCP is unavailable, and explicitly flag the approximation as demo-quality. **Mobile-above-header is the position rule\'s second instance:** the banner-position rule already holds (top-of-chat = something larger than the user\'s message). For mobile specifically, "topmost" wins over "below header" because vertical space is precious and the safety urgency outranks who-am-I-talking-to context. Adding to taste.md as a viewport-specific clause |
| SafetyBanner pattern shipped — 3 variants (self-harm, medical, financial), top-of-chat platform-intervention surface | **What:** new component at `chat/SafetyBanner.tsx` triggered by keyword detection on user-sent messages (`lib/safetyDetect.ts` + `lib/safetyVariants.ts`). Pinned to top-of-chat slot below ChatHeader, edge-to-edge (no rounded corners), persists until user dismisses. Self-harm has 2 buttons (Browse resources + Call now); medical/financial have 1 button each (Find a doctor / Find an advisor). All three include source attribution + Learn more inline link in the footer. **Why solid surface (`bg-page-bg`), not glass:** the codified glass convention (`bg-black-60 + backdrop-blur-popup`) signals "chat-bound" — it lives over the character image and reads as part of the conversation. Solid surface signals "platform speaking" — system intervention coming from outside the conversation. Wrong surface choice would read as Billie offering you the suicide hotline (glass) or as the platform breaking immersion to insert chat-level advice (solid where glass belongs). **New taste rule (Gate 6.5 generalization):** *"Solid surface signals platform speaking; glass surface signals chat-bound"* — formalizes the speaker-identifier principle that's been implicit until now. **Banner-position rule amended:** the codified *"top-of-chat = character state"* rule narrowed to *"top-of-chat = something larger than the user's current message"* — covers both character state (DormancyBanner) AND platform intervention (SafetyBanner). Mutual-exclusion clause added: when both could fire, platform intervention wins (safety > character state). **Architecture choices:** (1) one component, three variants via `variant` prop and `SAFETY_VARIANTS` data map (Gate 4 — patternize at 3 instances of the same anatomy). (2) Severity hierarchy `self-harm > medical > financial` on collision (most severe wins). (3) Fresh per session, no localStorage — safety alerts shouldn't be silenced across reloads (different from feature-disable like suggestions, which IS a user preference). (4) Inline SVG illustrations as demo-quality approximations of brand assets — production should swap to real brand SVGs/PNGs. (5) `Button` primitive used for actions with `navigate()` helper that routes `tel:` to `window.location` and external HTTPS to `window.open`. **File-size split (preventive):** `chat/page.tsx` was at 301 lines after additions — extracted constants/types to `chat/chat-config.ts` (`SUGGESTIONS_PREF_KEY`, `SEED_MESSAGES`, `STATES`, `STATE_LABELS`, `getBannerVariant`, etc.), bringing page.tsx back to 275. **Self-catch:** I missed the pre-edit line-count check before adding the SafetyBanner integration. The CLAUDE.md rule says "before adding code to any file, check its line count" — I added first, hit 301, then split. Should have split first. Logging as a watch-item for next session: pre-edit line-count check is a Gate 0 (do this BEFORE any edit on a long-ish file) |
| Header hierarchy fixed by SHRINKING ICONS, not bumping title — designer's redirect | **Designer's redirect:** my prior pass fixed the hierarchy issue by bumping title `text-sm font-medium` → `text-base font-semibold`. Designer wanted the *opposite* — drop the icons (× to 16px svg, bulb-off to 14px), keep title at original size. The hierarchy mismatch was real, but the right correction was at the icon end, not the title end. **Resolution:** title reverted to `text-sm font-medium`. Both buttons now hand-rolled at 40×40 hit area (`w-10 h-10 rounded-full hover:bg-white-10 text-white-90 inline-flex items-center justify-center`) with smaller svgs inside (× = 16px, bulb-off = 14px). Hit area decoupled from svg size — tappable parity preserved while visual icons feel subordinate to the title. Bulb-off intentionally smaller than × so the surface's primary control (close) reads slightly more prominent than its secondary disable affordance — *size as hierarchy within the icon cluster.* **Why hand-rolled rather than CloseButton primitive:** primitive uses `p-icon-btn`-driven sizing where hit area scales with svg size (20px svg → 40×40 button; 16px svg → 36×36 button). For this compact-panel context I needed decoupling: 40×40 hit area regardless of svg size. Same SVG path as CloseButton (per taste rule "Close icons must use the standard pattern everywhere"); same hover anatomy. **Lesson — when designer surfaces a visual hierarchy issue, ask which side of the relationship to adjust.** I jumped to "bump the smaller thing" without considering "shrink the bigger thing" — the latter was the designer's framework. *Correction route is itself a design decision; don't auto-pick.* The shrink-icons path keeps the panel slimmer overall (icons subordinate by size, not by competing-with-bigger-text). **Two prior taste rules retracted from this turn:** (a) the *"asymmetric padding pl-m pr-xs"* rule no longer applies because we returned to symmetric `px-m` + `-mr-icon-btn` on the rightmost button (the WSUP idiom). (b) the *"icon strokeWidth tuning to compensate density"* approach was a fix for the icons-too-big-vs-title problem; the real fix was sizing, not strokeWidth tuning. Strokes back to 1.8 (matches existing WSUP icon stroke convention) | **Designer caught two related issues:** (1) Both icons looked larger than the title — title was `text-sm font-medium` (14px medium) while icons were 20px svg, breaking the visual hierarchy a header should have (title primary, controls subordinate). (2) Bulb-off looked slightly bigger than × despite both being 20px svg, because bulb-off has 5 strokes packed into a 24×24 viewBox while × is just 2 strokes — higher path density = more optical weight at the same size. **Resolution:** (a) Title bumped to `text-base font-semibold text-text-title` — matches the canonical WSUP popup header convention (ConfirmSheet desktop dialog uses `font-semibold text-base text-text-title`). 16px semibold visually dominates 20px icons cleanly. (b) Bulb-off strokeWidth dropped from 1.8 → 1.6 (lighter than CloseButton's 2.0) to compensate for its higher path density. Both icons stay at 20px svg per codified rule "SIZE adapts to context: 20px in sheets/modals." (c) Switched header from `pl-m pr-xs py-xs` (asymmetric padding) to `px-m h-12` symmetric padding + `-mr-icon-btn` on the rightmost CloseButton — matches the existing WSUP idiom (ConfirmSheet uses `<CloseButton className="-mr-icon-btn -mt-xxs" />`). **Lesson — grep precedent before inventing:** I should have grepped existing sheet headers (ConfirmSheet, BottomSheet, BuyCreditsSheet) for header patterns BEFORE writing the original, and found the `font-semibold text-base` title + `-mr-icon-btn` close-button pattern already codified. Both my asymmetric-padding solution and my `text-sm font-medium` title were re-inventions of less-good versions of established patterns. The S23 watch item "grep precedent before inventing" still applies; I missed it twice this session. **Taste rule updated:** the optical-padding-compensation rule now points to `-mr-icon-btn` as the WSUP idiom, with asymmetric parent padding documented as a less-preferred alternative |
| Pill REVERTED to in-flow positioning; only the panel overlays — designer-caught | **Designer caught:** my first pass put both pill and panel as absolute overlays so toggling wouldn't shift ChatBar. Visual result: the 32px pill floating over a chat bubble looked broken — content-on-content at small size reads as a layout bug, not intentional chrome. **Resolution:** pill goes back to flow (`px-m md:px-2xxxl pb-xs`, no absolute), panel stays absolute overlay (`absolute bottom-full`). Toggling pill ↔ panel does briefly shift ChatMessages height by ~36px (the pill's flow space), but that shift is acceptable; what isn't acceptable is the pill *covering* a chat bubble at slim 32px. **New taste rule (Gate 6.5):** *Small surfaces stay in flow; only popup-sized surfaces earn overlay treatment.* Threshold isn't a fixed pixel count — it's whether the surface has enough visual presence (clear boundary, header/title, perceived elevation, ~120px+) to read as discrete-thing-on-top-of-content rather than content-on-content. A 32px pill fails that test; a 200px labeled panel passes |
| Header padding asymmetric (`pl-m pr-xs`) — compensates for icon button's intrinsic padding for optical balance — designer-caught | **Designer caught:** with `px-m` (16px) on both sides of the header, the `<CloseButton>` glyph optically appeared too far from the right edge — way more breathing on the right than on the left. Geometric padding was symmetric (16/16); optical padding was asymmetric (16 left vs 26 right because `p-icon-btn`'s 10px puts the glyph 10px inside the button). **Resolution:** drop right padding to `pr-xs` (8px) so the button's 10px internal padding adds back to ~18px optical right, close to the 16px optical left. **New taste rule (Gate 6.5):** *Compensate parent padding for internal button padding — measure optical edge, not container edge.* Rule of thumb: subtract the button's intrinsic padding from the parent's padding on the same side |
| Use `<CloseButton>` primitive for ×; bulb-off button mirrors its anatomy (p-icon-btn rounded-full hover:bg-white-10 + 20px svg) — designer-caught (Gate 2 miss) | **Designer-caught Gate 2 miss:** my first pass hand-rolled a `w-7 h-7` button with a `12×12` SVG path for ×. WSUP has a canonical `<CloseButton>` primitive in `ui/CloseButton.tsx` — `p-icon-btn rounded-full hover:bg-white-10 text-white-90`, default 20px svg, 40×40 hit area. Same anatomy used by every sheet/popover in the app. Hand-rolling created an out-of-system close button — different visual size, different padding, breaks the codified taste rule *"Close/dismiss icons must use the standard pattern everywhere."* **Resolution:** swapped to `<CloseButton>` primitive. Bulb-off button now mirrors the same anatomy (`p-icon-btn rounded-full hover:bg-white-10 text-white-90` with 20px svg) — both icons read as siblings in the header cluster, not as different button types. **The "Turn off?" pill morph adjusted:** `h-9 px-s` to fit alongside the 40×40 icon button without introducing a third height in the header. **Lesson — Gate 2 reuse check is mandatory:** when about to write a button/modal/sheet/icon, grep `ui/` first. The CloseButton primitive existed in the codebase the whole time; writing my own was a Gate 2 fail caught only by the designer's review |
| SuggestedReplies expanded state pivoted from horizontal chip row → labeled vertical panel (overlay above ChatBar) | **Designer surfaced the gap:** the chip-row pattern was designed against our short mock-data ("I missed you today.") but production generates roleplay-style replies with italic action narration + spoken text. Each chip would render at 600–1200px, breaking the read-and-pick scan-affordance the chip pattern relied on — horizontal scroll through a few massive chips. **Structural pivot, not a refinement:** chip-row was banner-anatomy applied to short reactive picks; long action+dialog drafts need sheet-anatomy. Direct second instance of the codified taste rule *"Pick the pattern by role, not by position"* — first instance was banner→sheet anatomy for chat-anchored cards (S22-ish); this is chip-row→labeled-panel anatomy for suggestions. **Implementation:** `bg-black-60 + backdrop-blur-popup` glass surface, `rounded-card` (12px), header row with "Suggestions" label + bulb-off + ×, body of full-width clickable rows showing italic action (`text-white-50 italic`, matching AI-bubble emotion convention) + spoken text (`text-white`) inline. Suggestion shape changed `string` → `{ action, text }` in `chatSuggestions.ts`. Dev handoff is now honest: devs see what production-shaped output looks like, not just our short mocks |
| × at top-right of panel header, NOT at leftmost band-position (narrows existing taste rule with scope clause) | Existing taste rule *"Toggling element stays at one position across states"* explicitly cites SuggestedReplies as the canonical example with × at leftmost, mirroring the pill's position. The new labeled-panel design breaks that — × lives top-right of the panel header. **Gate 6.5 rule-conflict cross-check:** is the new decision wrong, or does the rule need a scope clause? The rule was written for *sliver-to-sliver* toggles (collapsed pill ↔ chip row at same band) where the surface character is preserved. When the expanded state crosses into structurally-different surface territory (labeled overlay, sheet, modal — anything with its own header/title/padding-system), users' mental model shifts from "this same thing toggled" to "a new surface opened," and the new surface's universal close convention wins (top-right × on labeled panels). **Resolution:** added scope clause to the rule in taste.md — it now reads "sliver-to-sliver only" with explicit boundary conditions for surface-character change. Rule still holds for the case it was written for; doesn't apply to the new case. SuggestedReplies's labeled-panel state is the canonical example for the scope-clause case |
| Pill (collapsed state) also moved to absolute positioning (`bottom-full`) — not just the panel | If only the panel were absolute, toggling pill ↔ panel would shift ChatBar position because pill takes flow space and panel doesn't. ChatBar moving when toggling state would feel jumpy. By making both states absolute, ChatBar height never changes — only the surface above it changes character (pill → panel). The trade: most-recent message has ~36px of overlap with the pill. Acceptable because pill is a glass surface (`bg-black-60 + backdrop-blur-popup`) — overlap reads as expected layered chrome, not as a layout bug. Anchored via `bottom-full` on the inputAreaRef relative ancestor |
| Bulb-off (turn-off-forever) kept in-panel, not relegated to chat menu only | **Codified rule held:** *"two intents = two affordances: separate this-round dismiss from turn-off forever."* The earlier S25 reasoning (when suggestions are visible they're in the user's eye-line, disable belongs there) still applies. New placement: top-right of header next to ×, gap-xs between. 2-step confirm preserved (first tap morphs icon → "Turn off?" pill at h-7 px-s; second tap commits; 3s auto-revert). The chat-menu toggle remains the secondary route for users not currently in the suggestions flow |
| Skipped colored left-border accents on rows (designer's call) | Production screenshot showed blue/red/red/green left borders on rows. Designer's call: skip — borders carry no clear semantic meaning (not a tone classifier, not an action-type signal), so they'd be decorative noise. WSUP rule: don't add color signals without earned meaning. Rows differentiate via spoken-text content alone, hairline border-b for division |
| Suggestion shape changed from `string` → `{ action: string; text: string }` | Mirrors production shape. The action narration is part of the *picker decision*, not just decoration — users choose between "I narrow my eyes / You're being unreasonable" and "I tilt my head / Tell me more" partly on the action's tone and posture, not just the spoken text. Storing them as separate fields lets typography differentiate them (italic + low-opacity for action; full opacity for spoken) and lets `onPick` send only the spoken text to ChatBar (the action is scene-setting, not literal user input). **Generalization (not new — codified):** content shape determines layout; if you can't render the production-shaped content cleanly in your demo's layout, the demo's layout is wrong, not the production data |
| Blocked-list flip CTA uses "Unblock" ↔ "Block" (both verbs), NOT "Unblock" ↔ "Blocked" (verb-then-status) — designer-caught Gate 8 miss | **Designer-caught after the build shipped:** the toggled-state CTA "Blocked" reads as the user's CURRENT status ("this person is blocked") at exactly the moment when the truth is the opposite — they just unblocked. Misleads the user into thinking the unblock failed. **Root cause of the miss:** I copied SocialView's `Follow ↔ Following` toggle pattern without checking the linguistics — assumed any verb→status flip is the same. It's not. *Following* works as a status flip because it accurately describes the user's new state ("I am following them") — a real positive status word. *Blocked* doesn't work because after unblock, the user is NOT in a blocked state — there's no positive status word for "I just unblocked them." When the toggled state has no accurate status word, the CTA must describe the next *action* (Block), not a fictional status. **Fix:** label changed to `'Block'` in the toggled state. Visual model unchanged (filled = primary intent, outlined = de-emphasized reverse). **Sibling taste rule added:** *Toggle CTAs need a verb-or-status check before shipping the flip pattern* |

---

## Archive — pre-S29 entries (S22–S28 era)

**Pruned from active list at end of S30 per the agent.md self-maintenance protocol** (decisions.md was at 437 entries, 4 sessions past the 100-entry pruning milestone). Entries below this divider are KEPT for historical reference but are no longer the front-of-mind active set. Most have been absorbed into `taste.md` / `knowledge-base.md` / `reasonings.md` and that's where to look for the live rule. Use this archive as a paper trail for "what did we decide and when" — not as a working ruleset.

**If a rule from the archive is referenced in current work, promote it back above this divider AND ensure the codified version in `taste.md` etc. matches.** If the archive contradicts a codified rule, the codified rule wins (the archive entry was the moment-in-time decision; the codified rule is the principle that survived).

---

| Blocked creators header has NO count subtitle (skipped the "{N} total" pattern My Cards uses) | Designer asked whether to follow My Cards' "28 total" subtitle pattern. Surfaced as an open UX call (per new workflow rule). Both paths defensible: consistency with management-cluster sibling vs role-of-count differs. **Lean (validated by designer):** the count's *role* is what makes My Cards's pattern non-transferable — that count answers "how big is your collection?" (aspirational, part of the value). A blocked-list count answers "how many people did you block?" — semantically ambiguous (high = good curation, or = lots of conflict), and counts of-something-undesirable can quietly judge the user. Twitter and Discord do show counts; the WSUP call is to optimize for *cleanup surface* framing over *collection* framing on a short, functional list. **Generalization (potential taste rule, watch for 2nd instance):** *consistency-via-precedent isn't transferable when the count's semantic role differs* — copy the pattern only when the role matches |
| Blocked-creators list opens as a single-list overlay (mobile fullscreen / desktop sidebar) — same dual-surface pattern as `SocialView`, no tabbar | Designer's spec ("same way we open followers list") anchored the surface choice. SocialView is the codified precedent: mobile = full-screen slide-in via `SubpageHeader`, desktop = `absolute inset-0` overlay inside `ProfileRightSidebar`. Reused both surface modes. Dropped the tabbar — only one list lives here, a tab-of-one is empty ceremony. Built `BlockedListView` in `profile/` (not `ui/`) because it's domain-bound to the profile management flow, not a generic primitive |
| Menu entry labeled "Blocked creators" — noun phrase, NOT bare verb | Tension with codified taste rule "Action labels inherit context — bare verbs in nested menus." That rule's reasoning is target-redundancy: in `PublicMenuSheet`, "Block" is bare because the parent surface specifies *whom* (Honeybadger). In self `MenuSheet`, the entry doesn't perform an action on a specified target — it leads into a *list*. Bare-verb "Blocked" would read as a state label or be grammatically unanchored ("blocked what?"). Noun-phrase "Blocked creators" specifies the destination, matching siblings ("My cards", not "Cards"). **New sibling clause added to taste.md:** menu items that *perform a verb on the target context's subject* use bare verbs; menu items that *navigate into a list/management surface* use noun-phrase labels |
| Menu position: between "My cards" and "Log out" — management cluster sits above the destructive cluster | The existing order encoded an unspoken pattern: management entries (My cards) above the red destructive cluster (Log out + Remove account). New management actions extend the management cluster, never insert into the destructive one. Two reasons the destructive cluster stays atomic: (1) finger-rest position — destructive items live at the bottom because that's the hardest position for an accidental tap to land on; (2) visual cohesion — both reds reading as one zone makes severity legible. Inserting a non-red item between them would fragment the zone. **Codified as a knowledge-base rule:** *menu cluster ordering — management actions cluster above destructive actions; new entries insert into the matching cluster, never split a cluster* |
| Unblock CTA flips in place ("Unblock" filled ↔ "Blocked" outlined), single-tap on either state, no confirm | Designer's call after I flagged the flip-vs-remove tradeoff. Visual model mirrors SocialView's Follow / Following toggle for cross-list consistency. The downstream tension is that re-blocking from the toggled "Blocked" state silently re-blocks — which superficially violates the codified *Block needs confirm* rule. Resolution: the user is in a *deliberate-management context* (they navigated specifically into the blocked list to manage it). The friction the original Block-confirm rule guards against is *accidental block during incidental browsing* — not present here. **New taste rule:** *deliberate-management-context relaxes confirmation friction for state-flip actions inside it* — the navigation IS the friction. Cross-references the existing reverse-of-confirmed rule but covers the forward direction in a management surface |
| BlockedListView lives in `profile/`, not `ui/`; SocialView pattern documented at S28 (style-guide section), not retroactively for SocialView | Gate 4 trigger: a second instance of the dual-surface overlay anatomy (mobile fullscreen + desktop sidebar) — that's a pattern at 2. Documented in `BlockedListSection` under Patterns. Resisted the urge to retroactively create a `SocialListSection` for SocialView in the same edit — that's S27's pre-existing gap and would balloon scope. The recurring category from S27 ("over-correction on the next turn after a designer correction") guided this restraint: stay in scope, don't chase pre-existing debt during a new feature build. SocialView retroactive section can be a separate item if the designer wants it |
| Empty state uses EmptyState `variant='all-good'` (✨), copy "You haven't blocked anyone." | Considered the existing 'blocked' variant (🚫) — its emoji semantics fit "you've blocked someone here" not "your blocked list is empty." For the empty case, the 🚫 sign reads as an active block; ✨ ("all-good") reads as "nothing problematic, you're fine" — correct semantic for an empty management list. With mock data shipping 5 items, the empty path is design-only fallback (visible via dev: pass `blocked={[]}`); didn't add a new variant since the existing `all-good` covers it |
| Same `<Person>` shape across `FOLLOWERS` / `FOLLOWING` / `BLOCKED_CREATORS` | All three are `{ name, img }`. Mock data lives in `lib/mockData.ts` next to the existing FOLLOWERS/FOLLOWING. 5 items chosen — enough to scroll on a smaller mobile viewport, small enough to read at a glance in the sidebar variant |
| Gate 5 strengthened — subjective "did visual language change?" replaced with mechanical file-presence check | **Failure mode caught at end-of-S27:** despite Gate 5 saying "SAME EDIT," drift accumulated across 4+ component edits today. ChatBar lost its bulb but `ChatBarSection` still showed it. ChatHeader got the 3-dot menu wired but the section didn't note it. MenuPopover became a shared primitive but lived only as anatomy notes in another section, never as its own Components-tab entry. The audit caught it after the designer pushed — that's a backstop firing, not the gate working. **Three failure causes: (1)** subjective triage ("if visual language changed") gave a loophole to skip the gate when I rationalized "I'm just removing an element"; **(2)** I conflated "mentioned in someone else's anatomy" with "has its own section"; **(3)** I asked the forward question (new thing → new section) but never the reverse (modified existing thing → which sections demonstrate it?). **Gate 5 rewritten** to be mechanical: when editing a component file, grep for any section that imports it OR demonstrates its visual inline. If found, that section is part of THIS edit's scope. No interpretation. New primitives in `ui/` aren't done until they have their own section + ComponentsTab import + NAV registration. The strengthened rule lives in QUALITY-GATES.md so future sessions read it at session bootstrap. **Why this matters:** style-guide drift is the failure mode that splits the dev handoff from the live demo — devs read one truth, ship another. Gate 5 is the only thing standing between code and that split. Subjectivity in the gate's trigger makes it leak |
| Pill bulb-to-text spacing: settled at `gap-[6px]` after gap-xs (8px, too loose) → gap-xxs (4px, too tight) | **Designer-caught:** my last swing went too far the other direction. WSUP spacing scale jumps from xxs (4px) directly to xs (8px) — there's no native 6px token. Per Gate 1's accepted-exceptions clause, "near-match micro-adjustments where forcing to nearest token would break the design" explicitly lists `gap-[6px]` as legitimate. Used it. **Generalization:** when a 4px → 8px scale-step is the wrong size of jump for the intended visual rhythm, the arbitrary `[6px]` is the right answer — not a new token (one-off doesn't earn one), not forcing to the nearest token (breaks the design) |
| Toast width: switched from max-w (auto-shrinks to content) to explicit w-[calc(100vw-32px)] on mobile | **Designer-caught:** mobile toast was wrapping into 3 lines with significant gutter on left/right — wasted space made the wrapping LOOK heavy when it didn't need to. **Root cause:** `max-w-[calc(100vw-32px)]` only constrains maximum; actual width follows content. Short messages auto-shrink → narrow toast → forced wrap. **Fix:** explicit `w-[calc(100vw-32px)]` on mobile fills full viewport minus 16px each side. Desktop stays `md:w-auto md:max-w-[520px]` so it auto-sizes for one-line messages |
| Toast position: top on BOTH mobile and desktop (designer reverted my bottom-mobile placement) | **Designer reverted my call:** the previous turn moved mobile toast to bottom (thumb-reach reasoning). Designer redirected: keep top on mobile too. Likely reasoning (worth capturing): the toast is feedback for an action originating in the chat 3-dot menu at the TOP-RIGHT. Feedback at the TOP maintains spatial continuity with the action source — bottom-positioning would disconnect the feedback from the trigger ("I tapped at the top, why is the response at the bottom?"). Spatial continuity beats thumb-reach when the originating action wasn't itself in the thumb zone. **Implementation simplified:** single `top-[80px]` position, no viewport-conditional toggle. **Lesson — taste rule deleted, not replaced:** I added a "top on desktop, bottom on mobile" rule last turn based on a generic-feeling Material Design instinct. Designer's reversal proves it's not the right rule for *this* product. Removed from taste.md rather than replaced — better to have no rule than a wrong one |
| Toast scrim moved from bottom to top on mobile (220px tall, fades from 80% black at top to transparent at bottom) | **Follows from the position revert:** with toast at top on mobile, the scrim needs to darken the area AROUND the toast — which is now the top of the viewport. Flipped: `fixed top-0 z-[79]`, gradient direction `to bottom` (dark at top, transparent at bottom). 220px tall, covers the chat header (56px) and the toast band (top-[80px] + ~50px) with breathing room below. Stacks with the chat header's existing `bg-gradient-to-b from-black to-transparent` (mobile only) for guaranteed contrast against the character image. Desktop has no scrim — plain dark page-bg already provides enough contrast. **Generalization preserved (taste.md):** "scrim what's BEHIND the surface, don't make the surface itself heavier" — that rule still holds; only the location of the scrim changed |
| SuggestedReplies collapsed pill: tightened bulb-to-text gap, added right chevron (matches model picker pattern) | **Designer-caught:** the previous square-wrapper approach gave the bulb uniform 8px breathing on all sides (good) but then `gap-xs` (8px) ADDED to the square's 8px right padding meant 16px total visible distance from bulb to text. Felt loose. **Fix:** dropped the `w-8 h-8` square wrapper. Bulb sits directly inside the pill at `pl-xs` (8px) + `gap-xxs` (4px) before text. The pill's `h-8` auto-centers the 16px bulb vertically — `(32-16)/2 = 8px` top/bottom — naturally matching the 8px left padding. Bulb still has uniform 8px breathing on left/top/bottom; bulb-to-text drops from 16px → 4px. **Square wrapper rule from previous session is now narrowed:** use it when the icon's container needs to be perceptibly distinct (e.g., the inner anchor circle pattern); when there's no such need, direct padding is tighter and equally balanced. **Added a chevron** (4×8 SVG, strokeOpacity 0.5) at the right end of the pill — same anatomy as the ChatBar model picker. Visual signal that the pill expands. Subtle, not shouty |
| Toast: dropped "(⋮)" suffix from disable message, widened on desktop (520px max-w + md:whitespace-nowrap), wider mobile bound (calc(100vw-32px)) | **Designer-caught three issues:** (1) The "(⋮)" character at the end of "Turn back on from the chat menu (⋮)" was redundant — the kebab glyph isn't universal enough to add clarity, and the user immediately reads the text "chat menu" without needing visual emphasis. Just text is fine. (2) On desktop, the toast wrapped to 2 lines because `max-w-[360px]` was too narrow for the message. Should be a one-liner on desktop. (3) On mobile, the toast wrapped heavily because the same 360px max collided with the message length AND the disable message had been padded with extra characters. **Fixes:** (a) Removed "(⋮)" from message — "Auto-suggestions off. Turn back on from the chat menu." (b) Widened toast: `max-w-[calc(100vw-32px)] md:max-w-[520px]` — desktop fits one line at 520px, mobile fills available width minus standard 16px-each-side margins. (c) Added `md:whitespace-nowrap` to the inner `<p>` so desktop never wraps even if text grows slightly later | **Designer-caught:** the collapsed pill's left padding read as larger than its top/bottom padding, even though the explicit `pl-xs` (4px) matched the vertical breathing around the inner 24×24 circle. **Root cause:** the inner circle's distinct `bg-white-10` against the pill's `bg-black-60` made the eye treat the circle as a separate element with its OWN padding-from-pill-edge — a circle-on-pill effect that visually doubled the perceived left margin. **Fix:** removed the inner circle entirely. Bulb icon (bumped 14→16px) now sits inside a `w-8 h-8` square wrapper centered with `flex items-center justify-center`. The square fills the pill's height (zero vertical padding on it) and starts at the pill's left edge (zero horizontal padding before it). Result: bulb has uniform 8px breathing on all four sides — `(32-16)/2`. The square's left edge curves into the pill's rounded cap naturally, so the bulb visually sits inside the cap, not floating beside it. **Generalization (added to taste.md):** when an icon needs uniform breathing inside a pill/badge, give it a square wrapper sized to the container's height — don't try to balance left padding against vertical padding by trial and error |
| SuggestedReplies expanded: × moves to LEFT (same position pill occupied), bulb-off stays RIGHT | **Designer-caught two related issues:** (1) Original layout had × and bulb-off adjacent on the right → real misclick risk between two icon buttons of similar shape. (2) Toggling the panel felt asymmetric — pill was at left, but the close-back-to-pill control was at right. User had to track different positions for open/close. **Fix:** moved × to the leftmost position. Click pill (collapsed) → × appears at the same X position. Click × → pill returns at the same X. Toggling element stays at one position across states = seamless. As a bonus, × and bulb-off are now separated by all suggestion chips → impossible to misclick between them |
| Bulb-off icon: replaced manual strikethrough overlay with single integrated SVG (Lucide-style lightbulb-off) | **Designer-caught:** the previous bulb-off was an `<img>` of `icon-bulb.svg` with a CSS-positioned `<span>` painted diagonally across it. The two layers didn't share the same anti-aliasing, line-weight, or geometry — so the strikethrough read as "applied to the icon" rather than "part of the icon." Jarring visually. **Fix:** wrote a proper single SVG path that draws a bulb shape AND the diagonal slash within the same `<svg>` viewBox, with consistent `stroke-width: 1.8` and `stroke-linecap: round` across all paths. The slash now belongs to the icon's design rather than overlaying it. Same semantic ("the suggestion bulb is off"), much cleaner execution. **Lesson:** if an icon-with-modifier (off, mute, blocked) is needed, source or draw a single integrated SVG — don't compose it from two layered elements at runtime |
| MenuPopover restored to render TWO surface styles — sheet-style on mobile, popover-style on desktop — with shared items[] data (designer-caught my over-correction) | **What I got wrong:** in the previous turn the designer said "shouldn't this be same everywhere?" referring to chat menu vs profile menu spacing differing. I interpreted that as "use ONE visual style across all menus, mobile and desktop alike." Wrong. The designer actually wanted: keep the profile MenuSheet's tap-friendly mobile sheet (left-aligned rows, title label, dividers, Cancel) AND componentize THAT for all mobile menus. Desktop stays popover-style. They wanted ONE shared component, but TWO viewport-appropriate surfaces inside it. **What sheet-style gives that popover-style doesn't on mobile:** tap targets (40px+ rows vs 28px), readable text size (`text-base font-medium` vs `text-sm font-semibold`), categorial title that orients the user ("CHARACTER", "Account", "Chat"), Cancel button as a confirmed-exit affordance (mobile users may not realize backdrop-tap dismisses). All of these are mobile-UX wins — not arbitrary inconsistency. **Per the user's call, dropped the icons** that real WSUP profile sheet had — those were over-engineering for a 3-dot menu's role. **Refactored API:** MenuPopover now takes `items: MenuPopoverItem[]` (data, not JSX children) + optional `title?` + `showCancel?`. Internally renders `<MenuItem mode="sheet" />` rows for mobile body and `<MenuItem mode="popover" />` rows for desktop popover. Same `items` array drives both — viewport picks the surface, not the consumer. **Per-menu code is now ~15-25 lines** of pure config — `ChatHeaderMenu`, `MenuSheet`, `PublicMenuSheet` are all thin wrappers. **Wrong taste rule corrected:** "One pattern wins app-wide" was too aggressive. Replaced with the right framing: same viewport-role gets the same pattern, but viewport itself can legitimately have different patterns when mobile/desktop affordances genuinely differ |
| Migrated profile + public-creator 3-dot menus to use shared `MenuPopover` — one menu visual style across the app | **Designer-caught my rationalization:** the previous session claimed "chat menu is more compact than profile menu — same primitive, different content density." That was a rationalization, not a design choice. The real WSUP product uses centered-text-only items uniformly across all 3-dot menus (per the screenshot the designer shared earlier). Profile MenuSheet's sheet-style with icons + dividers + category label + Cancel button was the outlier. **Migration:** `profile/MenuSheet.tsx` (75 → 23 lines) and `profile/PublicMenuSheet.tsx` (85 → 38 lines) rewritten as thin wrappers over `MenuPopover`. Lost: per-row icons (`<BlockIcon />`, etc.), the "Account" / "Profile" category labels, divider hairlines between items, the Cancel button at the bottom of mobile sheets. Gained: visual consistency with the chat menu and any future menu. **Why drop the icons:** real WSUP doesn't use them in 3-dot menus; they were over-engineering. Center-aligned text with `font-semibold` is enough. Accidental scope was added in earlier sessions when the only menu pattern in front of us was profile's, so it became the default mental model — wrong. **Why drop Cancel:** redundant with backdrop-tap-to-close, drag-to-dismiss, and the trigger button itself toggling. Three exit paths are enough. **Refactor:** `MenuPopover` switched from `position: absolute` (required relative parent) to `position: fixed` with computed coords from `anchorRef.getBoundingClientRect()`. This means MenuPopover can be mounted ANYWHERE in the tree — at page root, inside an `md:hidden` ancestor, anywhere — and it still positions correctly relative to its trigger. Required because profile/creator pages have desktop 3-dot triggers inside `hidden md:flex` ancestors; mounting MenuPopover inside those would `display: none`-hide the mobile BottomSheet too. Now mounted at page root, anchorRef points to the desktop trigger button, both surfaces work cleanly. **`MenuPopoverItems` exports removed** from profile/MenuSheet.tsx and profile/PublicMenuSheet.tsx — items are inline now via `MenuItem mode="popover"`. **Still pending (deferred):** `CharacterMenuSheet.tsx` and `DormantMenuPopoverItems` — these are character-context menus inside cards (multiple instances per page), used differently. Out of scope for this consistency pass |
| Extracted `MenuPopover` primitive (ui/MenuPopover.tsx) — shared menu surface for centered-text "popover-style" menus | **Designer-caught:** the chat 3-dot menu had visibly more vertical padding between items than the profile 3-dot popover, even though both use the same `MenuItem mode="popover"` rows. Root cause: I had wrapped the chat menu items in `<div className="flex flex-col gap-xxs py-xs">` — the outer `py-xs` (8px) was redundant on top of the popover container's own `p-xs` and the items' own `py-xs` per-row. The duplication accumulated across all 7 items, making the surface look noticeably bloated. **Why a primitive solved this:** the surface chrome (rounded-card, bg, border, shadow, padding) belongs in ONE place — not duplicated across each menu file. Per-menu code should ONLY supply items. **Anatomy of MenuPopover:** Mobile = `<BottomSheet>` with content-driven height + `py-s pb-l` for breathing/safe-area; Desktop = anchored popover with `w-max` (auto-width to longest item), `p-xs` chrome, fade-in animation, outside-click-via-anchorRef, escape-to-close. Both surfaces wrap children in `<div className="flex flex-col gap-xxs">`. Pair with `MenuItem mode="popover"` children. **MenuItem updated:** added `whitespace-nowrap` to popover-mode (prevents text wrap on long labels like "Turn off auto-suggestions"). **ChatHeaderMenu before:** 99 lines of custom popover/sheet wiring + duplicated chrome. After: 27 lines, pure items list. **Future migrations (deferred to follow-up):** profile/MenuSheet.tsx's `MenuPopoverItems`, profile/CharacterMenuSheet.tsx's `CharacterMenuPopoverItems`, profile/PublicMenuSheet.tsx's `PublicMenuPopoverItems`, profile/CharacterMenuSheet.tsx's `DormantMenuPopoverItems`. They all currently compose Popover + items manually. None is broken — migrating is consistency cleanup, not a bug fix |
| First-click bug fixed: ChatBar's outside-click handler now respects a shared `containerRef` covering the suggestions surface | **Bug:** First tap on the suggestions pill caused ChatBar to collapse but pill DIDN'T expand — user had to tap twice. **Root cause:** ChatBar's document-level `mousedown` listener fired on the pill click (pill is outside ChatBar's wrapper) → `setIsActive(false)` → ChatBar shrunk vertically (model picker row 2 disappeared) BETWEEN mousedown and mouseup → user's mouseup landed on a different DOM element than mousedown → `onClick` (which requires both on same element) didn't fire on the pill. Classic layout-shift-during-click bug. **Fix:** ChatBar accepts an optional `containerRef` prop; outside-click checks against `containerRef` if provided, falling back to its own `wrapperRef`. Page wraps `<SuggestedReplies>` + `<ChatBar>` in a single `<div ref={inputAreaRef}>` and passes that ref. Now pill clicks register as "inside" → no chatbar collapse → no layout shift → click fires cleanly. **Belt-and-suspenders:** added `onMouseDown={(e) => e.preventDefault()}` on all suggestion buttons too — prevents focus-blur side effects that could re-trigger the bug in edge cases |
| SuggestedReplies expanded state: all elements standardized to h-8 (32px), gap-xs, NO ml-auto on close (designer-caught) | **Three issues caught in v2:** (1) Element heights inconsistent — bulb anchor `w-7 h-7` (28px), chips `~32px` from text+padding, close `w-7 h-7` (28px). Visual mismatch in a single horizontal row. (2) Close was pushed to far-right via `ml-auto` — on wide desktop viewports, the × ended up disconnected from the chips it was meant to control. Violated "related stuff together" UX principle. (3) Bulb anchor at left had no clear role — user tried clicking it expecting an action, nothing happened. **Fixes:** (a) ALL elements `h-8` (32px) — chips use `h-8 px-s flex items-center` instead of relying on padding to size them; bulb/close circles are `w-8 h-8`. (b) Removed `ml-auto` on close — controls (× + 🗲) sit immediately after the last chip with `gap-xs` between. Tight cluster, no orphan. (c) Bulb anchor at left REMOVED. Continuity now comes from the *band* (same horizontal slot as the collapsed pill), not from a redundant identifier icon |
| SuggestedReplies expanded state: bulb-strikethrough icon on the right is the disable affordance, with 2-step confirm | Per designer's suggestion. Layout in expanded row: `[chip1] [chip2] [chip3] [×] [🗲bulb]`. The `🗲` (bulb-with-diagonal-strikethrough) is the disable button. **Why 2-step:** user said they might tap to *see what it does* and accidentally turn off the feature. **Implementation:** first tap morphs the small icon button into a slightly brighter `bg-white-10` pill labeled `Turn off?`. Second tap calls `onDisable()` → toast appears + suggestions collapse. Auto-reverts to icon-only after 3s if no second tap. The 3s window is short enough that the affordance doesn't linger if the user moves on, long enough that the deliberate confirm tap is comfortable. **Why move it from the pill kebab to the expanded row:** when suggestions are *visible*, they're in the user's eye-line — disabling them right there is contextually appropriate. The chat 3-dot menu still has the toggle for users not in the suggestions flow |
| Chat 3-dot menu: items use `whitespace-nowrap`, popover container uses `w-max` (designer-caught wrap on "Turn off auto-suggestions") | **Bug:** `min-w-[200px]` was too narrow for "Turn off auto-suggestions" at `text-sm font-semibold` — text wrapped to a second line. **Fix:** items get `whitespace-nowrap`; popover container uses `w-max` (Tailwind's auto-width-from-content) instead of a fixed min-width. Container now sizes to the longest item. No more wrap, no arbitrary width to maintain |
| Chat 3-dot menu: mobile bottom sheet uses default content-driven height (no maxHeight constraint) | **Designer note:** the chat menu felt different from profile MenuSheet on mobile — should be more compact, not occupy the same vertical real estate. **Fix:** removed the `maxHeight="60%"` cap I had set earlier; let `BottomSheet`'s default behavior (content-driven height up to 82% cap) size it naturally. With 7 simple text items at popover-style padding, the sheet is ~40% of viewport — meaningfully more compact than the profile MenuSheet (which has icons, dividers, a category label, and a Cancel button). Same primitive, different content density = different feel. The compactness comes from what's *inside* the sheet, not from constraining the sheet itself |
| MenuRow inlined in ChatHeaderMenu instead of using shared MenuItem primitive | The shared `MenuItem mode="popover"` doesn't support `whitespace-nowrap` directly. Rather than adding a prop to the primitive (which would force every popover-mode menu to think about wrapping), inlined a small `MenuRow` helper inside ChatHeaderMenu that bakes in `whitespace-nowrap`. Localized fix; primitive stays simple. If a *third* menu ends up needing nowrap, that's the trigger to lift it back into MenuItem |
| SuggestedReplies redesigned: expanded state is horizontal chip row in same band as pill, NOT right-aligned chat bubbles (designer-caught) | **Designer caught two real failures in v1:** (1) The 3-dot kebab in the segmented pill had a broken-looking hover (rectangular hover bg vs `rounded-pill + overflow-hidden` clipping). (2) Suggestions appeared as right-aligned chat bubbles above ChatBar — same slot where SENT messages live — so they read as "already sent" despite being affordances. Tapping one made it disappear and re-appear in ChatBar input, which felt like teleportation. **The "spatial separation replaces visual differentiation" rule I added in the previous session was wrong** — position carries semantic meaning. Right-aligned bubbles ARE sent messages, regardless of what surface they sit in. Visual sameness + position-of-committed-action = looks done. **Fix:** (a) Drop the kebab from the pill. Pill becomes single-action button — clean hover. Disable lives ONLY in chat 3-dot menu (already wired). (b) Replace stacked right-aligned bubbles with a horizontal chip row in the SAME band as the pill: bulb anchor at left → 3 dark-glass chips → × at right. Trigger and result share spatial slot — eye stays in one place. Chips look like buttons (rounded-pill, bg-black-60, dark glass) — clearly NOT chat bubbles. Mobile: row scrolls horizontally on overflow. **Wrong taste rule deleted from `taste.md`; replaced with three corrected rules:** *position carries semantic meaning*, *trigger and result share spatial position*, *don't segment a single-action surface to fit a secondary control* |
| Auto-suggested replies: pill above ChatBar after 4s post-reply idleness, expands to 3 dark-glass suggestion chips, click-to-prefill (no auto-send) | Trigger is *user idleness* (took too long to think), not arbitrary timing. 4s feels intentional rather than instant — short enough to fire during demo review, long enough to not feel pushy. Pill anatomy uses chat-bound-surface convention (`bg-black-60` + `backdrop-blur-popup` — same as DormancyBanner). Suggestions render as **dark-glass chips** (same anatomy as the pill, smaller) — readable as buttons, not as chat bubbles. Tap fills ChatBar input, does NOT auto-send: user keeps agency. Typing in ChatBar collapses suggestions+pill silently |
| Suggestions content keyword-matched against AI's *last* message, NOT user's input | Mirrors `chatReplies.ts` (which matches on user's last message) but reversed — for suggestions we predict what the user might say next given what the AI just said. 8 entry banks (greeting-replies / how-are-you-replies / name / thanks / boundary-pushback / goodbye / waiting / question) + 3-line generic fallback. Real product would AI-generate these; for design-only handoff, scripted is sufficient and deterministic for review |
| Pill has bulb-prefix + label + `⋯` menu — 3-dot opens ONE option ("Turn off auto-suggestions") | Single-action menu is overkill for one option, but it's the right shape for what comes next: any future per-suggestion option (regenerate, change voice, suggest more) lands in the same `⋯` without restructuring. Better to ship the menu now than refactor later. Tap "Turn off" → toast surfaces the re-enable affordance ("Turn back on from the chat menu (⋮)") so users aren't orphaned in a disabled state |
| Close × on expanded suggestion stack returns to PILL, not full dismissal | User said it explicitly: cancel collapses *to pill*, not away. Pill stays visible after close — user can re-expand without waiting for next idle cycle. Different from "Turn off" (kills feature until manually re-enabled). Two distinct intents (this-round dismiss vs forever-off) need two distinct affordances |
| Suggestions preference persists in `localStorage` key `wsup_chat_suggestions_enabled` ("0"/"1", default on) | Matches the existing `wsup_game_seen` localStorage convention used for the Coachmark. Survives reloads. `localStorage.getItem` runs on mount in a `useEffect` (not initial state) to stay SSR-safe |
| Chat 3-dot menu wired now with all 7 future items, but only auto-suggestions toggle is functional | Per the screenshot designer shared (Memories / Cards / Clear Chat / Switch LLMs / Add Member / Report), the menu will eventually carry 6+ items. Wiring just the toggle would leave a dead 3-dot icon and force a future restructure when other items land. Built ChatHeaderMenu with the full 7-item shape (Memories / Cards / Clear Chat / Switch LLMs / **Turn on/off auto-suggestions** / Add Member / Report) — items 1-4 + 6-7 render as documentation labels (no handlers) so devs see the intended shape without implementing actions that aren't in scope. **Cluster order:** read/manage → reset → generation-config (Switch LLMs + Auto-suggestions) → people → escalation. Auto-suggestions sits with Switch LLMs because both are "how the chat is generated/assisted" — same semantic group |
| Auto-suggestions toggle uses action-style row (label flips with state) instead of a Switch primitive | Every other item in the menu (Memories, Clear Chat, Switch LLMs, Report, etc.) is action-style — adding a toggle-switch row would be the only stateful UI in the menu, visually inconsistent. WSUP has no Switch primitive currently (verified — `ui/` has Checkbox but no Switch); building one for a single use is wrong cost/value. Label is the affordance: "Turn off auto-suggestions" / "Turn on auto-suggestions". Same pattern scales to future per-chat settings (Mute character, Reset persona, Clear memories) — all read as actions |
| Chat menu items use `MenuItem mode="popover"` (centered, font-semibold) for BOTH mobile sheet and desktop popover | Real WSUP screenshot shows centered-text-only items, no icons, no category labels — different from the profile MenuSheet which uses left-aligned items with icons + "Account" label. Chat-context vs profile-context = different visual treatments. Reused the `popover` mode of `MenuItem` for both surfaces (mobile sheet body + desktop popover body) — same primitive, scope-adapted container |
| Bulb icon REMOVED from ChatBar entirely (both inactive and expanded states) | The bulb was a placeholder for the auto-suggest affordance with no behavior wired. Now that auto-suggest lives in the dedicated SuggestedReplies pill above ChatBar, keeping the bulb in the bar would be **two entry points for one feature** — direct violation of the "Duplicate entry points are forbidden" decision (S6 desktop). Removed both occurrences (inactive row prefix + expanded row 2 prefix). The expanded row 2 is now just the model picker, which is its only remaining purpose |
| ChatBar accepts controlled `value` + `onChange` props (falls back to internal state if not provided) | Required so SuggestedReplies can prefill the input via parent state without prop-drilling refs or imperative APIs. Pattern: `controlled = valueProp !== undefined`; if controlled, use parent's value and call `onChange`; else use internal state. Style-guide `ChatBarSection` passes nothing → falls back to internal state → no breaking change. Live `chat/page.tsx` passes `value={draft}` + `onChange={handleDraftChange}` → controlled. Single primitive, two modes |
| Toast appears at top-center, fixed, fades in, auto-dismisses at 4s | Top placement avoids conflict with ChatBar's bottom anchor. Dismissed via `setTimeout` (4s) — non-interactive (no close button) because the message is informational and short. Fixed positioning + `z-[80]` keeps it above bottom sheets (z-60) and popovers (z-50) but below modal-grade overlays. `pointer-events-none` on outer wrapper + `pointer-events-auto` on inner card prevents accidental click-blocking when message overlaps interactive content |
| Chat page wired with scripted reply mock (keyword map + random fallback, ~1.2s typing-indicator delay) | The chat page was previously a static state-toggler demo (R cycles through active / dormant / removed / memory-full) — devs received a frozen single message + always-on typing indicator. Couldn't see the *transition* states (send → typing → reply → scroll) which are core to the chat UX. Added `lib/chatReplies.ts` with 8 keyword intents (greeting / how-are-you / name / thanks / love-miss / bye / wyd / generic-question) + 8-line in-character fallback. `ChatMessages` refactored to render a `messages` array prop + conditional `isTyping`. `ChatBar` accepts `onSend`, fires on Enter or send-button click, clears input. Page-level state owns the array + 1200ms timer. Seed conversation (Namaskar Sara ji + Billie's reply) preserved so the chat shows real content on load BEFORE the user types — best of both worlds (static handoff state + interactive demo). Replies stay in-character with italic emotion lines, matching WSUP's existing AI-bubble pattern |
| Chat replies are keyword-matched, not AI-generated, in WSUP design-only handoff | Per project memory "WSUP is design-only dev handoff — no real auth/backend/payments; mock data is intentional." A real LLM call would (a) introduce a runtime dependency the dev handoff doesn't need, (b) make the demo non-deterministic for review, (c) burn API credits during design QA. Scripted replies + keyword routing demonstrates the full UX state machine (send / typing-delay / reply / scroll-to-bottom) without any backend. The 1200ms reply delay is itself a *design decision* — it's the typing-indicator visibility window. Too short and the indicator flashes; too long and the demo feels broken. ~1s–1.5s matches real chat UX expectations (Discord, iMessage, WhatsApp) |
| Always-on typing indicator removed from `ChatMessages` static render | The previous implementation hardcoded `<TypingIndicator />` to always show — fine as a documentation of the pattern, broken as live UX. Now conditional on `isTyping` prop, only renders during the 1.2s reply window. The style guide `ChatMessagesSection` still demonstrates the typing indicator as a static preview (that's what a style guide is for — pattern documentation), so anyone building chat-like UIs in the future can see the anatomy without needing to trigger the live state |
| EmptyState gets `px-l` horizontal padding + `max-w-[420px]` on the message at the primitive level | Designer-caught: the new "blocked" variant's message touched both viewport edges on mobile because EmptyState's container had `py-4xl` (vertical only) and no horizontal padding. Existing variants ("No active characters", "Nothing needs attention") happened to fit fine because they're short single phrases — the bug was latent until a longer message shipped. Fix at the primitive: 20px horizontal padding gives mobile breathing space; `max-w-[420px]` on the message <p> caps line length on desktop where the column is wider than reading-comfortable. Benefits all five existing variants too. **Gate 8.4 fail self-catch:** I added the rule "retune spacing when content size changes" today, then immediately failed to apply it on the very next content addition (the new variant's longer message). Codifying a rule ≠ internalizing it — next session must lead with Gate 8.4, not append it |
| Menu items are bare verbs ("Block" / "Unblock" / "Report"), NOT name-qualified ("Block Honeybadger") | The 3-dot menu lives ON Honeybadger's profile — the parent surface already specifies the target. Repeating the creator's name in every menu item is redundant context. The confirmation sheet keeps the name ("Block Honeybadger?") because that's the explicit destructive moment where target reaffirmation prevents mis-confirms — but the *menu items themselves* are bare action labels. Same pattern as WSUP's self-menu (My cards, Log out, Remove account — no "your" qualifier). Friction scales with specificity: low-friction affordance = bare verb; high-friction confirm = explicit target |
| Block creator lives in the 3-dot menu (next to Report), NOT as a top-level CTA | The top-level slot in a public creator profile belongs to actions that align with the *purpose of the surface* — and the surface's purpose is helping a viewer evaluate and follow a creator. Follow earns that slot. Block is a deliberate, low-frequency safety/control action — it should be findable but not in your face. Convention across Twitter, Instagram, TikTok, Discord: Block lives in the more-menu, never as a top-level button. Order in menu: **Block → Report** (personal-control action first, institutional escalation second) |
| Block confirmation is plain `ConfirmSheet`, NOT checkbox-gated | Per the taste rule "Friction in a confirmation should scale with the reversibility of the action": Block is reversible (PRD: "Block can be undone"), so a single confirm tap is the right friction. Gating with a checkbox would over-friction a reversible action and would visually equate Block (reversible) with Remove Account (irreversible). Plain ConfirmSheet with destructive=true (Block button gets the alert color); description explains both halves of the block effect ("they can't find you / their content disappears from your feed") |
| Unblock is single-tap (no confirm) | The reverse action of a confirmed action should not itself confirm — the friction was already paid at block time. Twitter/TikTok pattern. The user blocking made the deliberate decision; un-blocking is the un-friction action. Two taps to unblock would feel patronizing ("are you sure?" after a deliberate state change). One tap from either the Hero "Unblock" button OR the menu "Unblock {name}" item |
| Blocked state preserves Hero + Stats but replaces tabs+grid with `EmptyState variant='blocked'` | When the user is on the blocked creator's profile, the basic identity (avatar, name, bio, badge) and *credibility signals* (stats) stay visible — those are public information that doesn't depend on the relationship. What disappears is *the content the block is hiding*: characters and stories. Replaced with a single empty-state banner (🚫 + "You blocked {name}. Their characters and stories are hidden. Unblock to see them again."). New `'blocked'` variant added to `EmptyState`. ProfileTabBar is also hidden in this state — tapping a tab to show "blocked" twice is wasted clicks; one banner is enough |
| Block auto-unfollows the creator | If the user is currently following Honeybadger and blocks her, "still following someone you've just blocked" is a confusing state. Block should clean up: `setIsFollowing(false)` on confirm. If they unblock later, they're not auto-re-followed — that's a deliberate action they'd take separately |
| `BlockConfirmSheet` is a thin wrapper around `ConfirmSheet`, mirrors `LogoutConfirmSheet` pattern | Same primitive (ConfirmSheet), parameterized via creator name in title + description. `destructive` prop on. No gate. ~20 lines. Pattern reuse over abstraction |
| Public profile drops three owner-only signals: stats Followers/Following drilldown, character-card rank trend arrow, character-card 3-dot menu | All three are *movement / management* affordances meant for the creator monitoring or maintaining their own work. On a viewer surface they're either noise (a viewer doesn't care about Honeybadger's *rank delta* — they just want to know the rank), broken (3-dot menu opens management actions a viewer can't perform), or privacy-leaning (drilldown surfaces the social graph a creator may not want exposed). Public keeps the *absolute* signal (count, rank number, the character itself) — the credibility/identity of the creator. Drops the *change* signal (what's moving up/down) and the *management* affordance (actions on those characters). Spotify/TikTok pattern, not Twitter/Instagram. **Rule:** *Movement is private; absolutes are public.* |
| MyCharactersDashboard "My Characters" h1 + count subtitle removed (redundancy catch on existing code) | The dashboard had a header block (h1 "My Characters" + subtitle "X active, Y dormant, Z removed") rendered between the Characters tab and the FilterPills row. Designer caught the redundancy: the *tab itself* is the section header ("Characters"), and the filter pills below already break counts down per state ("All N · Active N · Needs Attention N · Removed N"). The header block was doing both jobs twice. Direct application of the existing S7 taste rule "Two rows of pills (summary bar + filter tabs) is redundant — merge into one row. Counts go inside the filter pills, not in a separate bar." Pre-existing miss — not shipped today, but caught while comparing self vs public profile compositions. The lesson: rules in taste.md only protect future code; existing code needs cross-screen audits to surface pre-existing rule violations |
| Public creator profile = self profile shell with `viewMode='self'\|'public'` prop threaded through, NOT a forked page | The viewer's mental model of "Honeybadger's profile" matches Alex's own profile in shape (Hero, Stats, Badges, Tabs, Grid) — what differs is *visibility* (Active Persona hidden), *affordance* (Follow CTA replaces Edit; Report-only menu), and *data depth* (no trend deltas, no lifecycle filtering). Forking would have meant maintaining two parallel implementations of essentially the same screen. Picked: components accept `viewMode` prop (ProfileHero, StatsRow, ProfileRightSidebar), separate `/creator/[username]/page.tsx` that mounts them with `viewMode='public'`, and a new `PublicMenuSheet` for the menu (different *intent* — self-management vs viewer-actions — so per S20 taste "different intent = leave it alone, don't migrate by grep" the menus stay separate components even though shape is similar) |
| Follow CTA placed below bio, full-width Button size m, NOT a top-right icon button | Top-right slot in ProfileHero is for icons (edit, share, 3-dot at 40×40px). Putting Follow there as a small icon-sized button would undersize a *primary action*. Per the WSUP taste rule "CTA size reflects the action's role, not just the container type": Follow on a public profile is the single most important thing a viewer can do. It deserves Button m fullWidth, placed below bio where the eye lands after scanning the creator's identity + story. `mt-m` (16px) above to anchor it as a distinct action zone, not a continuation of the bio |
| StatsRow `viewMode='public'` hides trend deltas (▲ +8%) but keeps clickable Followers/Following | Trend deltas are *creator-private feedback* — your own movement is yours to see, not the world's. Public viewers get the absolute counts (Followers / Chats / Views / Following), tappable on Followers/Following to open the social list. The chevron next to clickable counts stays — it's an affordance, not a trend |
| Active Persona card hidden entirely on public profile | Per the spec: persona is creator-private. Implementation: `{isSelf && <ActivePersonaCard {...persona} />}` in both mobile and desktop sidebar compositions. The empty space falls out cleanly because the surrounding `mt-m` gaps belong to badges and stats independently of the persona card's presence (Gate 8.4 spacing-content fit verified — no retune needed) |
| Public profile Characters tab uses `ContentGrid`, NOT `MyCharactersDashboard` | MyCharactersDashboard is the lifecycle/management dashboard for the creator's own characters (filter pills, "Needs Attention", "Removed", credit-fee accordion, info banner). On a public profile the viewer only sees *active, public-facing* characters — no lifecycle introspection. ContentGrid (the simpler grid used for Stories tab) is reused for both Characters and Stories tabs on public. Same pattern, different data |
| `PublicMenuSheet` is a separate component from `MenuSheet`, NOT a `viewMode` variant | Gate 3 trigger fired (2 menu instances with similar shape), but per S20 taste rule "Same shape, different intent — don't migrate by grep": the self-menu (My cards / Log out / Remove account) and public-menu (Report) serve fundamentally different jobs. Self is account-management; public is viewer-action. Their items, intent, and audience differ. Two thin components > one items-driven primitive. **Watch:** if a 3rd action-menu variant appears (e.g., character-context menu in another flow), THEN extract a generic `<ActionMenuSheet items={[...]}>` |
| ChatHeader "by [creatorName]" link wired to `/creator/${creatorName.toLowerCase()}` | Lowercase slug convention. The link previously went to `#` (placeholder); now it actually navigates. Single change — no need to change the visual styling (already underlined inline link, no hover-only) |
| MemoryLimitMoment (in-stream variant) removed from codebase; popup is the canonical memory-limit pattern | Strategic call by the designer: ship one path, not two. The popup variant won the canonicalization fight in S23 (default 2s auto-fire) and the inline variant has been dev-cycle-only since. Two parallel implementations of the same UX moment is maintenance debt and design-system noise. Removed: `MemoryLimitMoment.tsx`, `MemoryLimitMomentSection.tsx`, the dev cycle entry "Memory full (inline)", the `showMemoryLimit`/`characterName` props on `ChatMessages`, the `context-exhausted` chat state, the "Memory Limit Moment" style-guide nav entry, and the "Alternative to MemoryLimitMoment" / "When to use this vs" framing in the popup's anatomy notes. **Not a taste fail** — this is a PM/strategic decision (per the "PM-driven decisions aren't VDA's catch-bar" memory rule), not a UX correction. The taste rule "Live inside the conversation, not on top of it" still stands as a design heuristic for any *future* floating-card-without-backdrop temptation; it's the popup's chat-darkening backdrop that sets it apart from the ambient floating card the rule warns against |
| MemoryLimitPopup spacing restructured into semantic groups with hierarchical gaps | Flat `gap-m` (16px) between every element treated title/body/CTA/secondary as four equal-weight peers — but they're not. Title+body are one *narrative unit* ("what happened" + "what changes"); CTA+secondary are one *action pair* (primary path + alternate). The body→CTA boundary is the *pivot* from "reading" to "acting" — the surface should breathe THERE, not equally everywhere. Restructured: outer `gap-xl` (24px) between the two semantic groups, inner `gap-xs` (8px) within the narration, inner `gap-s` (12px) within the action. Mirrors ConfirmSheet's precedent (title/desc tight via `mb-xxs`, action stack tight via `gap-xs`, with a clear padding break between them) — same anatomy, scope-adapted gaps |
| MemoryLimitPopup divider removed between primary CTA and switch-model link | The horizontal divider (`border-t border-white-10 -mx-l`) was load-bearing when the popup had 5–6 stacked elements (title + model card + body + CTA + secondary). After the copy revision dropped the model card and the popup now has only 4 elements (title + body + CTA + secondary), the divider was visually carving up an already-short surface — making it feel busier, not clearer. Stack of 4 with `gap-m` reads as one unified surface; a divider in there reads as fragmenting unity for no functional reason |
| MemoryLimitPopup top padding reduced `pt-6xl` → `pt-4xl` (80px → 64px) | After the model-context card removed, the popup got materially shorter — but `pt-6xl` was still cleared for a longer stack. Result: ~32px of empty air between DP bottom edge and the title, which read as accidental rather than intentional breathing space. `pt-4xl` (64px) clears the 48px DP overhang with 16px breathing space — close to the canonical `m` (16px) gap used elsewhere between framed elements and their content |
| MemoryLimitPopup copy revised → system-voice title, condensed body, decisive CTAs | New copy: title `"{name}'s memory is full."` (was character-voice italic+attribution), body `"The app remembers 3× more of your story."` (was 10× + "stays with her"), CTA `"Open in app"` (was "Continue to chat in app"), secondary `"switch model instead"` (was "Or switch to a better model"). The popup variant is the high-interrupt path — it already breaks the flow with a chat-darkening backdrop, so leaning into system narration is more honest than disguising the interruption as dialogue. Inline `MemoryLimitMoment` keeps character voice for the low-interrupt path; the two variants now have *deliberately different voices* matching their *different interrupt levels* |
| MemoryLimitPopup model-context card removed | The inset card "You're chatting on {model} right now — it forgets context after a few messages" was an explainer for *why* the limit hit. Removing it: the secondary action label `"switch model instead"` already implies the model is the bottleneck — the user infers the cause from the offered alternative. Keeping the card alongside the new system-voice title would have been two adjacent system explanations doing overlapping work. Cleaner, more decisive |
| Drop "Or" prefix on the secondary action link | Centered "Or switch to a better model" had "Or" doing connective work the new copy doesn't need — `"switch model instead"` already contains the contrast word ("instead"). Saved a token of clutter; centered the bare link button |
| Title typography drops italic + attribution when voice shifts to system narration | Italic + `— Billie` attribution is purpose-built for character dialogue. Applying it to "Billie's memory is full." (third-person system narration about Billie) would have read as "Billie referring to herself in third person" — wrong vocal register. Treatment must follow voice |
| Checkbox unchecked-state invisibility bug fixed at source | The Checkbox primitive had `border-none` in its base className. Tailwind's preflight sets `border-style: solid` by default, so the conditional `border border-white-20` for the unchecked state was being neutered by `border-style: none`. Result: unchecked checkbox = invisible 16×16 box with no edge. Fix: remove `border-none` from base — checked state stays bordered-invisible because no `border` width class is applied to it; unchecked state now renders the explicit border correctly. Bug had been silently affecting BuyCreditsSheet ToS gate, CreditPackRow selection, and FormsSection style guide demos. Fixing the primitive fixes all three callsites at once |
| ConfirmGateRow styling matches BuyCreditsSheet ToS precedent exactly (text-xs, gap-xs, items-center) | First pass used text-sm + gap-s + items-start — invented styling for a pattern that already had a canonical instance (BuyCreditsSheet ToS gate at lines 88–92). Gate 7 catch on the second-pass review: when a precedent exists, match its tokens; the *position* (above vs below the CTA) can flex per role intent, but the visual identity of the row should not. Aligns with S23 watch item "grep precedent before inventing" |
| Remove account confirm = ConfirmSheet + new optional `confirmGate` prop, NOT a forked sheet | Logout is reversible (you can sign back in); Remove account is permanent. Both deserve double-check, but the latter needs *more friction*. Two paths considered: (a) fork a parallel `RemoveAccountConfirmSheet` with hand-rolled markup, (b) extend `ConfirmSheet` with an optional checkbox-gate that disables the confirm CTA until ticked. Picked (b) — extends the existing primitive without forking. RemoveAccountConfirmSheet is now a thin wrapper (mirrors LogoutConfirmSheet's wrapper pattern) that owns the local checkbox state, resets on close, and feeds copy + gate into ConfirmSheet. Wins: logout flow is unchanged (the gate is opt-in via undefined prop), the Checkbox primitive is reused (no new visual atom), the disabled-button treatment falls out of Button's existing `disabled` styling — zero new tokens, zero new components beyond the wrapper |
| Severity of action gates the friction, not the surface | Both logout and remove account use the SAME confirmation surface (ConfirmSheet — mobile bottom sheet + desktop centered dialog). What differs is the *gate*: logout = direct confirm, remove account = checkbox-gated confirm. Resisting the urge to invent a "danger sheet" with red header / scary iconography — that would split the design system over a behavior delta. Same shape, configurable friction = consistent UX vocabulary |
| Remove account row sits BELOW Log out in the menu | Both are `text-status-alert` red (siblings in the destructive zone). Order encodes severity: most-permanent action goes last. Reading order top-to-bottom = increasing commitment. Bonus: putting Remove account further from the user's natural finger-rest position on the menu reduces accidental taps |
| Both destructive menu items share `text-status-alert` exactly, no shade variation | Per S20 taste rule "When two states share visual treatment, their token shades must match exactly." Logout was already `text-status-alert`; Remove account adopts the same token. Tempting drift would have been Remove = darker/heavier red — but that's a sibling-shade trap. If a third destructive option ever appears, no canonical-shade question to resolve |
| Checkbox label sits left-aligned even though title/description are centered (mobile) | Title/description are *narration* (centered → reads like a heading + caption). Checkbox + acknowledgment is a *form input* (left-aligned + flush-left checkbox → reads like a control to be acted on). The alignment shift is the signal: this isn't more text to read, it's a control to act on before the CTA unlocks |
| Disabled CTA inherits Button's existing `opacity-40 cursor-not-allowed` | Button already handles disabled visual state. No need to invent a "soft-disabled" variant for the gated case — the existing pattern is the right precedent (Gate 7 consistency). The user said "just how we use other disabled primary button in WSUP" — so the canonical disabled state IS the answer |
| Default 2s auto-fire on /chat → MemoryLimitPopup (was MemoryLimitMoment inline at 3s) | Both variants shipped in S23. Designer's call to make the popup the canonical default and to tighten the timer to 2s: the install pitch is a meaningful conversion moment that should *interrupt the flow*, not slip into it. The inline MemoryLimitMoment optimizes for in-character continuity, but it can pass unnoticed during a fast scroll — the popup's chat-darkening backdrop forces the user to acknowledge the moment. 2s (down from 3s) reduces dwell-before-trigger so the demo lands faster on review without feeling instant. Inline variant remains accessible via Shift+R cycle for review; the popup is now what a fresh /chat visit produces |
| Promo CTA → in-place transition to provider chooser (Option A over A/B/C) | First-time guests can't be signed in by a single CTA — there must be a provider step. Three paths: separate sheet, default-provider, or in-place transition. In-place keeps the same surface, preserves the persuasion headline as continuity, and treats the two variants as different *entry points to the same flow* (Variant 1 = direct, Variant 2 = persuade-then-providers) instead of two parallel designs. Headline does NOT swap when the action area swaps — the user's last anchor stays put |
| Internal `internalMode` state in LoginSheet (separate from the `mode` prop) | The 2-step promo flow needs the popup to advance from cta → form *without* the parent re-rendering or the variant pill changing. `mode` stays the prop-driven outer truth; `internalMode` tracks the user's flow position within the popup. Reset to prop on open or on prop change, so reopen / pill-flip both reset to step 1 |
| Variant switcher pills float OUTSIDE the popup, top-right, 10px gap | First placement was inside the popup near the close button — competed with real product chrome and read like a real control to a confused user. Outside placement above the popup over the dim backdrop reads unambiguously as dev tooling. The `VARIANT` uppercase prefix label reinforces it. Pills are dev-only — they should never look like part of the design |
| Dropped `S`, `C`, `L` keyboard shortcuts | S (streak popup toggle on /explore) was redundant — the popup auto-opens on every visit, the dev panel just duplicated the X close button. C (creator-view on /chat) was redundant with the in-panel checkbox. L (streak login variant on /explore) sat at z-70 behind LoginSheet's z-90 backdrop and didn't actually work. When a dev shortcut is redundant, broken, or invented for one-off use, drop it — every key in the keymap is cognitive load on the designer who has to remember it |
| Distinct copy for inactive vs moderation, equal text/icon shades | Original PRD-aligned copy collapsed both into "Not listed — chat available" with a 10%-shade body color difference (text-white-50 vs text-white-60). Same copy + invisible color delta = functionally one variant in two costumes. New: "paused by its creator" / "being reviewed" — descriptive, neutral, audience-appropriate. Colors normalized to the inactivity values for both, since copy is now the only differentiator and consistent shade hierarchy makes them feel like siblings not strangers |
| When two states share visual treatment, their colors must match exactly | Coda from the dormancy fix. Subtle shade variation between sibling states (here white-50 vs white-60) is invisible on first read but creates *latent inconsistency* — a subsequent designer adding a third state has no signal which shade is canonical. Pick one, apply uniformly, document in taste |
| Dormancy revive link gating dropped (`isCreator` removed) | Creator-vs-viewer distinction was a backend-truth concern, not a banner-truth concern. The banner is shown to whoever's viewing — making revive availability driven by *variant* (not viewer role) means: on inactivity/moderation, anyone seeing the banner could in theory click revive (in production this'd be guarded server-side). Dropping the prop simplifies the component, the dev tools, and the style guide |
| LoginSheet earns `mode='form' \| 'cta'` instead of forking into a second sheet | Two PM variants for the streak login share the entire shell (logo, character image, legal footer, dismissal). Only the action area + an optional hint footer differ. A single component with a mode prop keeps both variants in sync on shell-level changes (sizing, breakpoints, animations) and avoids the LoginSheet/StreakLoginSheet split that would fragment maintenance |
| StreakClaimPopup owns the variant *presets* (`STREAK_LOGIN_VARIANTS`), LoginSheet stays generic | The mode + ctaLabel + footer combinations carry streak-specific copy. Putting them in StreakClaimPopup keeps LoginSheet domain-agnostic so other contexts (purchase, profile, future flows) can use the cta mode with their own copy |
| `STREAK_LOGIN_VARIANTS` exported, consumed by both StreakClaimPopup and the style guide | The style guide demonstrates the variants without going through the streak popup → gate dance. Exporting the preset map lets the style guide mount LoginSheet directly with the exact same copy. Single source of truth — a copy edit propagates to the live screen and the doc in one shot |
| Apple = `bg-black + border-white-20` and `bg-page-bg` hover; Google stays `bg-white` | Apple's brand-pure styles are black-with-white-text or white-with-black-text. Stacking two white buttons would erase the brand differentiation users expect from an Apple/Google row. Black-on-dark needs the white border outline to define the button against `bg-profile-sheet-bg`; the slight lift to `bg-page-bg` on hover gives feedback without breaking Apple HIG |
| Apple sits BELOW Google in the form stack | Web context (no Apple HIG mandate to lead). Google has been the existing primary social provider — keeping it first preserves user muscle memory. Apple as the second row is a discoverable add, not a reordering |
| `mode='cta'` mobile sheet = 360px (vs 440px form) | The cta layout has only 3 vertical sections (copy + button + footer + legal) versus form's 5 (copy + email + OR + Google + Apple + legal). A taller sheet for cta would create dead space; a shorter sheet keeps the value-prop screen feeling decisive, not under-furnished |
| `mode='cta'` footer is `text-xs text-text-small`, NOT a legal-style footnote | The cta footer ("We'll move your streak to your account on first sign-in") is a *value reassurance* attached to the CTA — it answers "what happens when I tap this." It deserves more weight than the legal line below, which is boilerplate. Different roles, different sizes |
| ~~Streak login variant toggle = `L` key, mirrors R/S pattern~~ → superseded | First attempt added an L key panel mirroring R/S. Failed in practice: dev panel z-index (70) sat behind LoginSheet's z-90 backdrop, so panel clicks were blocked while the gate was open — exactly when you needed it. Replaced with in-popup variant pills (no z-conflict). Lesson: a "consistent dev-key pattern" only earns its place if it actually works; consistency that breaks under real flow is just a memorized broken thing |
| Promo variant copy ("Save your free credits") leads with the user's *fear of loss*, not sign-in mechanics | Standard variant says "Sign in to claim your credits" — direct and instructional. Promo variant flips the framing: the gate is no longer about the act of signing in, it's about *not losing what you've already earned*. Loss aversion > acquisition framing when the user already has progress to protect |
| Profile login copy extracted to `ProfileLoginGate` wrapper | Header + BottomNav both triggered the same LoginSheet with identical profile headline/subtitle. One wrapper, one copy source. Future profile-gated triggers compose ProfileLoginGate; ad-hoc triggers compose LoginSheet directly |
| Mobile LoginSheet logo is 1.5× desktop (72px vs 48px) | Mobile has no competing chrome (no Header, no sidebar), so the floating logo carries more visual weight. On desktop the logo shares the panel with a 60% character image — a bigger logo would compete. Size scales to context |
| Mobile image must NOT use backdrop-filter: blur on the full container | The first implementation applied backdrop-blur(60px) across inset-0 which blurred the character image itself into a color blob. Fix: remove full-container blur; use only a linear-gradient fade toward the bottom where the form sheet meets. The form sheet keeps its own `backdrop-blur-popup` for the intended surface-over-image effect |
| Headlines in LoginSheet take no terminal punctuation | Titles, not sentences — applies uniformly ("Sign in to continue", "Let's dive into character creation", "Sign in to access your profile"). Subtitles keep periods since they ARE sentences |
| Purchase subtitle: "So your credits go to the right account" (was "So we know which account to credit") | Literal accounting language ("credit" as verb) reads like a ledger. The rewrite keeps the same *why* but in plain user English — "credits go to the right account" is unambiguous and warmer |
| Login gate is a PATTERN, not per-trigger modal copies | The LoginSheet component accepts `headline` + `subtitle` props so each *intent* can adapt copy. One component, N intents. Keeps copy close to the gated action instead of ballooning into separate sheets |
| Purchase flow gates share ONE message across all 4 steps | User's intent throughout BuyCredits is the same — "credit me." One-time buy, monthly subscribe, pay-in-app, and open-wsup-app all show "Sign in to continue. So we know which account to credit." Splintering into per-step copy would say the same thing four different ways |
| Login gate subtitle answers "why" directly, not marketing-speak | "So we know which account to credit" is literal and functional. Earlier draft "Top up your credits and keep the conversation going" sold the product instead of answering the question the gate raises. When a user is interrupted by a gate, they want to know why — not be pitched |
| Gated action resumes itself post-sign-in (no re-click required) | User clicks "Continue on Patreon" → LoginSheet opens → user signs in → sheet closes AND flow advances to result. Making them tap Continue again would punish them for being unauthenticated — classic re-entry friction |
| Sign-in handled by either email submit OR Google click (dev handoff) | Both paths call the same `onSignIn`. Flipping `isLoggedIn=true` is the only real effect. Production would swap in real OAuth + email flows — the shape of the contract stays the same |
| AuthContext stores only isLoggedIn for now | Dev handoff project. No profile data, tokens, or session state — just a boolean. When real auth lands, context expands without the consumer API changing for isLoggedIn-only checks |
| Header avatar swaps inline SVG geometric placeholder → profile-picture.jpg when logged in | Logged-out state keeps the generic geometric avatar (signal: you're a guest); logged-in state shows a real face (signal: this is your account). Same 32×32 slot, no layout shift |
| Login sheet: desktop = 2-col modal (form left, image right); mobile = bottom sheet (image fades into form above) | The character image frames the brand/mood in both; layout adapts to the device's dominant axis. Don't try to shoehorn the desktop split into a stacked mobile layout — the image-above-form is the mobile idiom |
| Desktop LoginSheet form uses 40/60 split + left-aligned content | Image is the hero at 60% — the face sells the product. Form at 40% is focused and scannable from top-left (natural reading entry). Center alignment reads as a utility modal; left alignment reads as a branded page moment |
| Desktop LoginSheet uses the W mark only, not the full wordmark | The modal already brands itself through the character image on the right. Adding the wordmark makes the form panel busy; the W mark alone is enough to anchor identity |
| ChatBar typing state: mic hidden, image replaced by send icon in bottom-right slot | Once the user starts typing, the intent shifts from "what do I want to do?" to "send this message." Voice and image become irrelevant; send becomes the primary affordance. Gift stays — it's a flex distinct from the send path. Send slots into image's position so the eye doesn't track a new location |
| ChatBar expands to a 2-row composer on input focus | Progressive disclosure — options (model picker, reorganized media actions) only appear when the user is actively composing. Inactive state stays slim; active state surfaces controls at the moment they're relevant |
| ChatBar stays expanded while input has a value | Collapsing after blur on a half-typed message would flicker the layout and hide the model picker mid-thought. Non-empty input == still composing |
| Bulb-circle moves from row-1 (inactive) to row-2 (active, next to Claude pill) | Row 2 becomes the "generation context" group — bulb (auto-suggest) + model picker live together. Keeps row 1 focused on the message itself |
| Image icon reflows from top-right to bottom-right when expanded | In inactive state, media actions are peers in a single row. When expanded, the right column mirrors the left's two-row structure — attach-image pairs with the model row, send-affordances (mic, gift) stay up top with the text |
| Action buttons use `onMouseDown preventDefault` to keep input focused | Clicking the Claude pill or bulb in expanded state would otherwise blur the input and collapse the composer. Focus belongs to the message being composed |
| Claude pill uses `rounded-pill` + `backdrop-blur-popup bg-black-30` + white-10 border | Distinct surface treatment (blurred dark chip) from the bubble — signals "this is a control I can tap to change the model", not decoration |
| Monthly subscription skips Payment + Scan steps | Patreon handles auth + payment off-site. The flow shape mirrors the real trust boundary — don't pad it with WSUP-hosted steps that do nothing |
| +10% bonus shown via strikethrough `~~1000~~ 1100`, not per-row badges | Badges label the promise; strikethrough SHOWS the value. The real numbers do the selling |
| Strikethrough original appears AFTER the primary value | Placing it before creates space between the credit icon and the meaningful number — icon adjacency matters |
| Pack selection uses `<Checkbox variant="success">` (not a radio) | Matches the payment-method indicator already in the project. Reuse over semantic purity when visual pattern already exists |
| Tab chip copy: "+10% credits" (not "Save 10%") | We don't discount the price — we add credits. "Save 10%" was inaccurate |
| Per-row bonus badges removed from monthly packs | Tab chip teases, strikethrough confirms. Four repetitions of the same badge = noise, not emphasis |
| "Manage subscription" link sits WITH the center confirmation pill | Belongs with "what just happened" group. Primary CTA stands alone at the bottom of success screens |
| Premium surface (`bg-surface-premium`) reserved for transactional moments | BuyCreditsSheet only. All other sheets use solid `bg-profile-sheet-bg`. Over-using premium dilutes its meaning |
| Tab underline matches content width via inline-flex inner span | Fixed 60% (old ProfileTabBar) looked off when content widths varied (Monthly+badge vs One-time). Content-sized underline adapts |
| Tab padding lives on the inline span, not the button | Underline at span's bottom-0 sits flush with the container border-b. Padding on button pushed underline upward away from the baseline |
| Dropped outer pill container on PackModeToggle | Three nested pills (outer + active + badge) competed visually. Matching ProfileTabBar's underline-only pattern eliminated the nesting |
| Billing note "Billed monthly · Cancel anytime" | Recurring charges need explicit disclosure; users should never be surprised by a renewal |
| 2-col character cards (mobile) | 3-col was too cramped, text truncated |
| 3-col badge grid in sheet | 4-col truncated labels; 3-col lets text breathe |
| `tracking-[0.8px]` for all 10px uppercase | Iterated from 0.2→0.7→0.8; uppercase at small size needs wide spacing for readability |
| Fixed-width follow buttons (108px) | Different text lengths ("Follow" vs "Follow back") caused visual jumping between tabs |
| "CHARACTER" label not character name | Sheet category labels should be generic (like "ACCOUNT"), not dynamic content |
| No show more/less on profile | Dev handoff project — simpler is better, show all content |
| Standard Header on profile (not custom AppBar) | Profile is a main navigational screen — needs full app chrome |
| `label-xs` for all 10px uppercase labels | Was using 4 different colors/weights — standardized for consistency |
| Active Persona label above name, not below | Category label → content is the natural reading order |
| `text-text-body` for readable descriptions | `text-text-dim` (40%) is too faint for copy users should actually read |
| Icons tab in style guide (not centralized components) | Dev handoff project — devs need a visual reference, not our React components |
| Outlined pills for category tabs (not filled) | Filled bg looked heavy; outlined with white border is more refined |
| FAQ question text `font-medium` not `font-semibold` | "Intentionally lighter weight" — questions should be readable, not shouty |
| FAQ max-width 768px (not 860px) | Narrower reading width is more comfortable |
| Character card description at 70% white (not 60%) | 60% "too subtle for readable body copy on a card" |
| Placeholder boxes instead of real icons in style guide | "Prevents icon drift, focuses on structure" in previews |
| Search gradient bumped brighter | Original "too subtle against dark input background" |
| Header Pill CTA vs Secondary/Outlined are different | "Intentionally different" — header recedes (10% border), standalone has more weight (20% border) |
| Sidebar widget label `font-medium` not `font-semibold` | Widget labels should match "same dim tone as inactive nav items" |
| `text-xxs` (10px) token created, 67 arbitrary sizes eliminated | Zero tolerance for arbitrary `text-[Npx]` values |
| Footer policies: flat list not accordion on mobile | Accordion hid important legal links — "cannot hide this under a dropdown" |
| External link icon: diagonal-arrow from WhatIsWsup section | Reused existing icon instead of creating a new box-with-arrow variant |
| Chat messages bottom-aligned with flexbox spacer | Spacer div before first message, compresses as content grows — natural scroll behavior |
| AI bubble max-width 290px (not flex-1) | Action row must match bubble width, not stretch to window |
| Gradient-border pattern for premium buttons | 1px padding outer with gradient bg + solid inner surface. Looks like a gradient border. |
| Chat bar scrim: different on mobile vs desktop | Mobile: absolute div in page.tsx. Desktop: gradient on bar's outer wrapper. Context-specific solutions. |
| `usePathname()` for active nav states | Never hardcode active state — always derive from URL |
| Custom JS scrollbar (not CSS pseudo) | Chrome transitions on scrollbar pseudo-elements are unreliable |
| Footer policies changed from accordion to flat list | "Cannot hide this under a dropdown" — legal links must be visible |
| Coachmark uses chat-ai-active bg (not chat-ai-bubble) | Distinct from regular bubbles — it's a UI overlay, not a message |
| Profile header kept (not replaced with custom AppBar) | Profile is a main navigational screen — needs full app chrome |
| Sidebar Generate Images widget label: font-medium not font-semibold | Widget labels match "same dim tone as inactive nav items" — they're waypoints, not CTAs |
| Story images square (1:1) not 4:3 | Designer corrected twice — square is the format for story images on all breakpoints |
| 64px (4xl) center area padding for layout type 2 | Standard across all screens with center + right sidebar layout |
| Desktop badges: full grid, no scroll, no "see all" | Sidebar has room to show all — horizontal scroll is a mobile constraint, not a pattern |
| 3-dot menu in center header, hidden in sidebar | No duplicate entry points — one location per action |
| Right sidebar reuses mobile components | Never rebuild — ProfileHero, StatsRow, ActivePersonaCard are the source of truth |
| No border-bottom on header when tabs sit below | Tabs already have bottom border — double lines are a bug |
| BottomSheet stays mobile-only, no dual-rendering | Wrapping in fragments or adding CenterPopup broke event handling — keep BottomSheet as a single div tree, handle desktop separately |
| Popover listeners gated to desktop via matchMedia | Global mousedown listener on Popover intercepted ALL clicks on mobile, killing BottomSheet buttons — hardest bug in session 6 |
| Separate scroll containers for center + sidebar | Root overflow-hidden, each panel scrolls independently |
| Custom scrollbar hooks tokenized | useVerticalScrollbar (2px, fade) and useHorizontalScrollbar (3px, hover) for consistent reuse |
| BottomSheet z-[60], above BottomNav z-50 | Sheet was rendering behind nav bar — DOM order put BottomNav on top. Higher z-index fixes it. |
| Sheet bottom padding pb-m not 80px | BottomSheet now above BottomNav, no clearance needed. 80px was a hack. |
| Popover variant: light (dark bg) vs dark (images) | white-10 bg invisible over images — need bg-black-70 for character card popovers |
| Popover rounded-card (12px) not rounded-popup (24px) | Small compact menus need tighter corners than full-screen sheets |
| Desktop logout = separate confirmation dialog | Not a BottomSheet variant — completely different layout (title, body, right-aligned Cancel + action buttons) |
| Mobile/desktop overlays: completely separate components | Dual-rendering broke 3 times. Shared state is fine, shared DOM rendering is not. |
| Profile avatar in Header linked to /profile | `<Link>` not `<button>` — navigation, not action |
| Solid bg on all overlays, no backdrop-blur | Frosted glass caused click-through, visual inconsistency, and multiple bugs. Solid `#1a1a1a` is reliable. |
| BioSheet: BottomSheet (mobile) + CenterPopup (desktop) | Bio "Read more" must work on both viewports — separate rendering paths, shared content component |
| Desktop badges: not clickable | Desktop shows all badges in grid — no need to open a sheet. Mobile badges remain clickable (opens BadgesSheet). Shared components must have viewport-appropriate behavior. |
| Map wireframe surface tokens to white opacity tokens | PRD proposed #201f1f, #2a2a2a, #353534 surface containers — mapped to existing `bg-white-05`, `bg-white-10`, `bg-white-20` instead. Designer corrected the plan that proposed new tokens. Token reuse > token creation. |
| Tailwind opacity modifiers for banner backgrounds | Banner tint colors use `bg-status-warning/[0.08]` and `bg-status-error/[0.08]` — opacity modifiers on existing status tokens. No new color tokens needed for lifecycle banner variants. |
| Horizontal list card for lifecycle views | Dormant/removed character cards use horizontal layout (image left + body right) — different from vertical ProfileCharacterCard. Management/status views need information density that vertical cards don't provide. |
| Dismissible banners are per-session only | DormancyBanner dismiss state is not persisted — resets on page reload. For a dev handoff project, localStorage persistence is unnecessary complexity. |
| Mock data extracted to shared file | When page.tsx approached 300 lines, mock data moved to `src/lib/mockData.ts`. Keeps pages as thin orchestrators. |
| Email templates use inline CSS only | Email clients don't support Tailwind or external CSS. Templates are self-contained components with inline styles, separate from the app's design system. |
| Merged summary bar + filter tabs into single FilterPills row | Two rows of pills serving the same data is redundant — counts belong inside filter pills, not a separate summary bar |
| Dormant/removed cards use compact 4:5 aspect in 3-col/5-col grid | Lower-priority cards should be visually smaller than active cards — size IS hierarchy |
| Active cards keep full 9:16 aspect in 2-col/4-col grid | Active cards deserve full visual weight — different importance = different card size |
| Revive buttons use secondary variant on cards, primary reserved for confirmation modals | Primary buttons should NOT repeat across a grid — secondary for repeated actions, primary for the single most important CTA |
| Violation reasons removed from all card surfaces | Showing IP infringement reasons on cards is shaming — badge is the signal, details discoverable on tap |
| Info banner icon removed when section header has warning icon | Duplicate alert signals close together add noise — one signal per concept |
| Default filter = "All" always, not "Needs Attention" | Showing problems first sets a negative tone — give the full picture, let users filter down to issues when they choose to |
| Banners use items-center not items-start | Horizontal banner elements should be vertically centered — top-alignment looks broken |
| Scrollbars hidden in popups/sheets | No visible scrollbars inside modals — always use scroll-hide + custom scrollbar |
| Demo toggle keyboard-activated (R key) not floating panel | Floating UI competes with the design being reviewed — keyboard activation is invisible |
| Chat dormancy banners use backdrop-blur + bg-black-50 on mobile | Banners over character images need stronger backgrounds to be readable |
| Email stat icons use colored dots not emojis | Matches app pattern, more email-safe, consistent brand language |
| Email "Revive" links don't repeat credit cost per row | Repetition is clutter — say "Revive →" once, cost shown elsewhere |
| FilterPills component reuses CategoryTabs pattern | Same interaction pattern = same component style, not a new invention |
| CreditButton with primary/secondary variants and 4 sizes | Standardized credit action button, uses actual /credit.png icon |
| CharacterStatesSheet: BottomSheet (mobile) + CenterPopup (desktop) | Shared content via StatesContent function, separate rendering per viewport |
| Chat dormancy banner = centered pill on mobile, full bar on desktop | Edge-to-edge banner breaks immersion over character bg image; pill is compact, translucent, lets background breathe. Visual weight must match info priority — informational ≠ alert |
| Dormant cards 2-col on mobile (same as active), 5-col on desktop | 3-col made text wrap on <360px screens — readability > hierarchy on mobile. Desktop keeps compact 4:5 aspect for visual hierarchy |
| Dormant card 3-dot menu: inactive = Share + Delete, moderation = Delete only | Inactive chars can still be shared; moderation chars are under review so only delete is appropriate |
| "Why does this cost credits?" accordion above cards, not below | Natural reading flow: banner → FAQ → cards. User asks "why?" before seeing Revive buttons, not after scrolling past all cards |
| Single FAQ entry point per section, not per card | "Don't repeat info per row" — one accordion for the whole section, not duplicated on each card |
| Edit Character page: same header pattern as Profile on both viewports | Back arrow + title uses identical `h-3xxxl px-xs` container, same SVG (20x13.5), `text-base font-semibold` — not SubpageHeader which is visually heavier (text-lg) |
| Edit Character form centered horizontally in center area | `max-w-[480px] mx-auto` — form doesn't stretch full width on desktop, stays readable |
| Bottom action bar: stacked on mobile, horizontal on desktop | Mobile: warning text (text-xs) + full-width button stacked. Desktop: inline warning + button. Bar is always pinned, form scrolls above it |
| Form page layout: header + scroll area + fixed bottom bar | Same flex column pattern as chat: header shrink-0, form flex-1 overflow-y-auto, bottom bar shrink-0 |
| Popover items inside Link wrappers need preventDefault + stopPropagation | Clicks on popover menu items bubble up to parent Link and trigger navigation — must block at popover container level |
| New reusable form components: FormInput, FormTextarea, SelectionPillGroup | All use form tokens (forms-bg, forms-border, forms-focus-border, forms-active-bg). SelectionPillGroup handles radio-style exclusive selection with active state |
| Removed section stays separate, moves to bottom of page | Terminal state needs visual separation from actionable cards. Not a filter-level peer but its own section below Active. Removed filter pill restored for direct access |
| Rejected badge uses red (status-alert), not orange | Red = user tried and failed (urgent). Orange = platform flagged it (Policy Review). Distinct colors prevent confusion between similar-sounding states |
| Approved characters move to Active section, not Needs Attention | "Needs Attention" = "you need to do something." Approved = nothing to do. Character returns to Active with a temporary dismissible badge |
| Approved badge: tap entire badge to dismiss, no close icon | Close icon inside a small badge is cramped and low contrast. Whole badge as dismiss target is cleaner — hover state signals interactivity |
| Contact Support button uses Secondary/Outlined pattern from style guide | `border-white-20 text-text-body font-medium rounded-pill` — not a custom blue button. Matches the established button system |
| `text-secondary` for links, `text-forms-focus-border` for form focus | Same color (#82a1ff) but different semantic roles. Links use `text-secondary`, form active states use `forms-focus-border`. Correct token selection matters even when values match |
| Dead-end text must be actionable or removed | "Have questions? Visit our help center" without a link is bad UX. Either link it or delete it |
| Badge tap → lightweight tooltip, not full states sheet | Tapping "Inactive" badge opens a small tooltip with just that state's explanation. Full 7-state sheet is too much context for a contextual question. Simpler = better |
| BadgeTooltip is a separate component from Popover | Popover = action menu (Edit, Share, Delete). BadgeTooltip = contextual info for one state. Same positioning/dismiss logic but different UX patterns — don't merge them |
| Button component: 3 variants × 4 sizes | Primary, Secondary, Dark × XS/S/M/L. Extracted after audit showed same inline classes repeated across 4+ files |
| All button variants use font-medium, not font-semibold | Same size = same weight. Variant changes color/bg/border, never weight. Designer prefers medium — lighter feel. Applies to Button AND CreditButton |
| Card CTAs use size S, not XS | XS (py-xxs = 4px) is too slim for card action buttons. S (py-xs = 8px) is more tappable. XS is for truly compact inline contexts |
| "Contact Support" shortened to "Support" on removed cards | Full text wraps to 2 lines at 5-col desktop card width (81px). Shortened label fits single line — context (grayscale card, removed badge) already communicates the meaning |
| leading-none must come AFTER text-size in Tailwind classes | Tailwind's text-sm sets both font-size AND line-height. leading-none before text-sm gets overridden. Put leading-none in size classes after the text-size token |
| 2 usages is enough to extract a component | Designer's threshold is lower than "wait for 3." If the same pattern appears twice with the same purpose, componentize it |
| Style guide must be synced in the same session as component changes | Don't defer style guide updates to "next session" — the gap grows and sync becomes a bigger task. Update style guide sections as components are built/changed |
| Style guide shows live components, not static HTML | FormsSection uses actual FormInput, FormTextarea, SelectionPillGroup components. LifecycleSection uses actual DormantCharacterCard. ButtonsSection uses actual Button component. Changes to components automatically reflect in the style guide |
| Style guide ButtonsSection: all variants use font-medium | Updated from font-semibold across all button examples — Primary, Secondary, Dark, Filled/Inverted, sizes, CreditButton |
| Revive button navigates to /edit-character | The revival flow IS the edit flow — user edits the character to address issues, then submits for review. Revive = edit + resubmit |
| Email template updated for full lifecycle | HTML email now shows all 4 actionable states: Inactive, Policy Review, Under Review (no Revive — in progress), Rejected. Arianda Grande removed (now Approved/Active). Under Review rows show "In progress" instead of deadline + Revive link |
| Plain text email as separate route | `/email/dormant-notification-text` — ASCII-formatted, scannable, full URLs visible (not hidden behind href), section dividers, no HTML dependency. Optimized for email clients that strip HTML |
| Text email: full URLs, not hidden links | In HTML email, links hide behind "Revive →". In text email, the full URL is visible. Users can see exactly where they're going |
| Text email is NOT monospace/ASCII art | A UX designer designs text emails like actual emails — proportional font, natural paragraph spacing, visual hierarchy through size/weight, not terminal-style code blocks with `===` dividers |
| Text emails need warmth and personality | "Your characters miss you!" > "Character Attention Required". Per-character action links, numbered tips, warm closing with team signature. Emails are a touchpoint — they should feel human, not automated |
| Pure text email = literally a string | No HTML, no CSS, no images. Unicode characters (● ◆ → ─) for visual structure. Preview renders on white bg with system font to show how email clients display it. Copy-to-clipboard for pasting into any email system |
| Remove redundant Support CTAs when main Support button exists | Don't duplicate entry points — underlined "Support" text + "Contact Support" in menu + Support button CTA = 3 entry points for same action. Keep only the main CTA |
| Blog link as tappable card, not plain text link | Plain text links are easy to miss. Card-style (bg + border + padding + hover state + arrow icon) reads as interactive immediately. Book icon + external arrow communicates "read more elsewhere" |
| ConfirmSheet extracted as reusable UI component after 2 usages | Logout and Revive confirmations share identical structure (title, description, confirm/cancel). Extracted to `src/components/ui/ConfirmSheet.tsx` — props: title, description (ReactNode), confirmLabel, cancelLabel, destructive |
| Revive CTA shows confirmation before navigating to edit | User needs to understand: (1) they must edit and resubmit, (2) credits are consumed on submission. Direct navigation to /edit-character skips informed consent |
| Badge tooltip: accent color bar matches severity | Thin top stripe (3px) using badge's color: yellow for policy review, red for rejected/removed, blue for under review. Visual connection between badge and tooltip |
| Badge tooltip: reason as primary text, guidance as secondary | Specific policy message is what the user needs — it's the answer to "why?". General guidance (edit and resubmit) is secondary context, shown smaller below a divider |
| Badge tooltip: no dismiss hint needed | Target audience (young US users, good computer literacy) knows tooltip behavior. "Tap to dismiss" is unnecessary hand-holding — removes it for cleaner UI |
| Badge tooltip width 240px not 200px | Policy messages are 1-2 sentences — 200px forces excessive wrapping that's hard to read. 240px fits most messages in 2-3 lines |
| Button size m for both mobile and desktop in ConfirmSheet | Consistent sizing across viewports. Confirmation actions need comfortable tap/click targets on both |
| Mock data reasons use actual block message copy | Prototype tooltips should show real user-facing messages from the moderation system, not shorthand labels like "IP infringement" |
| Accent colors in data configs use hex with token comment | Tailwind v3 doesn't auto-generate CSS vars — use hex values with `// status-warning` comments to maintain traceability. Inline style `background` needs raw values |
| Close icon 20px on all dialogs/sheets | ConfirmSheet was 16px, CenterPopup/BottomSheet are 20px — standardized to 20px. Close icons must include `strokeLinejoin="round"` |
| Book icon strokeWidth 2, not 1.8 | All icons in the system use strokeWidth="2" unless explicitly documented otherwise. 1.8 was inconsistent |
| Style guide updated: ConfirmSheet + BadgeTooltip | Overlays section now documents ConfirmSheet as responsive component with props, and BadgeTooltip with/without accent bar variants |
| PRD copy principle: descriptive not prescriptive | Never threaten removal, demand action, or frame moderation as failure. Revival is "available" not "required." Creators decide; we don't pressure. Applied across all lifecycle UI |
| Moderation banner neutralized — no yellow/warning styling for chat users | Chat users should not see moderation state details — that's private to the creator. All dormancy banners use same neutral bg-white-05 + info icon, regardless of internal reason |
| "Last chatted X days ago" replaces countdown | No "Xd left" or "removed in X days" — countdown language creates urgency/pressure. "Last chatted" is factual and descriptive |
| Removed card: text link not button for Contact Support | Button implies required action. Subtle text link in muted color (white-50) presents support as available without pressure |
| Creator banner variant: isCreator prop on DormancyBanner | Creator viewing their own dormant character's chat gets a subtle "Revive" link. Not shown for removed state (not self-service revivable). Available, not required |
| ReviveConfirmSheet: full character context + balance display | Shows character thumbnail (64px), name, state badge, reason, cost breakdown, user balance, insufficient credits state. User makes informed decision with full context |
| CreditFeeAccordion extracted as shared UI component | "Why does this cost credits?" accordion appeared in both ReviveConfirmSheet and MyCharactersDashboard. Extracted to single source of truth per "2 usages = extract" rule |
| EmptyState extracted as shared UI component | Dashboard empty states (No active characters, Nothing needs attention, No removed characters) extracted to reusable component in ui/ |
| Reason strings: human-readable labels not raw messages | PRD specifies short labels: "IP/Trademark", "Content policy", "Age concern", "Real person likeness" — not verbose explanation strings |
| Needs Attention section: info icon not warning triangle | Warning icon implies urgency. Info icon (circle-i) is neutral and informational — matches the PRD's descriptive tone |
| Credit icon replaces "credits" text in cost/balance rows | When /credit.png icon is present, the word "credits" is redundant. Icon + number is cleaner. Applied in ReviveConfirmSheet cost breakdown |
| `icon-btn` spacing token (10px) for icon button padding | 30+ instances of `p-[10px]` across all icon buttons. 10px sits between xs(8) and s(12), justified its own token. Used for hit area padding on close buttons, nav icons, action icons |
| Contact support link: always underlined, text-secondary | `no-underline hover:underline` is invisible on mobile (no hover). Always show underline with muted decoration. Use `text-secondary` for link color — distinguishes from surrounding `text-white-50` status text |
| "Last chatted" on its own row, not sharing row with chat count | Sharing a row caused wrapping at 2-col mobile. Tried shortening to "34d ago" and clock icon — both lost meaning. Solution: keep full text, give it its own row. Clarity > compactness |
| "Last chatted" text-xs not text-xxs | Same hierarchy as chat count — both are engagement data. Color (white-40) already creates visual hierarchy. Don't use size to demote meaningful data |
| Chat count moved to right of character name row | Saves a vertical line — name (left, truncate) + chat count (right, shrink-0) on same row. "Last chatted" gets its own row below. Tighter card without losing info |
| `.link` utility class for all inline text links | 4 links across 3 files had identical styles. Extracted to `.link` in globals.css — text-secondary, always underlined, subtle decoration. One link style across the whole app. Same logic as `label-xs` for repeated text patterns |
| Zero characters empty state with "Create Character" CTA | PRD D1.7 requires empty state for brand new creators. Plus icon + message + primary Button. Different from tab empty states which are just icon + message |
| R key data toggler on profile page (not D) | Must use R key — same as chat page toggler. D was wrong. All dev togglers in WSUP use R toggle + Shift+R cycle. UX consistency rule: check existing patterns before choosing interactions |
| Zero characters state hides tab empty states | When totalCount === 0, only show "Create your first character" CTA. Tab-specific empty states ("Nothing needs attention", "No removed characters") must not leak through — they imply the creator has characters in other states |
| Gate 7 — UX Consistency added to quality gates | Before implementing any interaction/visual/copy, check how it already works in WSUP and match it. Established after D-key mistake. Applies to both Claude and VDA |
| EmptyState uses emoji not SVG illustrations | SVG illustrations were too abstract and unreadable at small sizes. Emoji are universally understood, render well at any size, work in dark theme. 😴 no-active, ✨ all-good, 🛡️ no-removed, 🎭 create. Simple > clever |
| EmptyState accepts children slot for CTA buttons | Button outside EmptyState created double spacing (py-4xl + gap-m). Button inside keeps emoji + text + CTA as one tight group with consistent gap-s spacing |
| Gate 8 — UX Review added to quality gates | Designer should never catch UX issues. Spacing, readability, mobile tappability, text wrapping, empty state logic — all must be caught by Claude/VDA before presenting. Read gates before task, run after task. Every time |
| Buy Credits promo uses detailed bag illustration, not flat icon | Figma spec (node 28585:42889) shows a 3D money sack with coins + smaller coin jar, not a single flat `credit-bag.png`. Asset exported from Figma at 3x (471×303) as `/credit-bags.png`. Positioned `absolute -right-[18px] -top-[8px] w-[170px] h-auto` — overflows top-right per Figma `cart 1` spec |
| Buy Credits card gets `min-h-[104px]` | Without it the card collapses when bag image loads asynchronously — button was getting hidden behind the absolute-positioned bag during hydration. 104px = p-m (32) + text (12) + gap-m (16) + button (40) + 4px buffer. Guarantees button visibility |
| Buy Credits promo bag uses plain `<img>` not Next.js `<Image>` | Next.js Image intrinsic-size handling inflated layout pre-hydration, causing the card to render without proper height. Plain `<img>` with `h-auto` + `object-contain` renders predictably. Same rule as widget character thumbnails per WidgetsSection anatomy |
| Buy Credits button uses `Button` component at size="s" with 180×40 override | Figma spec is 180×40; no standard Button size matches exactly. Override via className (`w-[180px] h-[40px]`) instead of adding a new variant — dimensions are structural one-off per token exception list. Inherits primary variant styling (bg-accent, font-medium, rounded-pill, shadow-button) |
| Buy Credits widget added to style guide WidgetsSection | Previously uncovered — same-edit rule: visual change in CreditSidebar = style guide showcase in same edit. Includes anatomy call-outs (card surface, bag position, button sizing) |
| CreditPackRow extracted from 4 identical pack rows | Same structure repeated: name label, credit amount, rate, Buy button. Gate 3 threshold met. `featured` prop flips the visual treatment for the recommended pack |
| Featured pack gets unique visual primacy, not a "best value" badge | In a selection list with pricing tiers, one option must stand out via its whole styling — gradient button + ambient bg glow — not just a tacked-on label. All-equal presentation buries the recommendation; a badge is a developer's solution |
| Credit pack Buy buttons stay inline, not promoted to Button variants | Two one-off styles (glass + golden gradient) for a single flow don't justify two new Button variants. Inline buttons with tokenized colors are cleaner than polluting the component API for a commerce-only pattern |
| Buy Credits = one sheet with 3 steps (packages → payment → scan), not 3 separate sheets | The flow is a single decision chain with a shared visual shell (same gradient bg, same sheet frame, same credit summary pill). Separate modals would create re-render flickers and fragment the user's context. Internal `step` state + back-arrow navigation keeps orientation continuous |
| CreditsSummaryPill extracted after 2 usages (payment step + scan step) | Gate 3 threshold hit — same "credits + price" row renders identically on both steps. Extracted rather than duplicated inline |
| BuyCreditsSheet has fixed `min-h-[497px]` across all 3 steps | Designer flagged that the header back-arrow was jumping vertically between steps because each step had different natural heights. Fixed via `min-h-[497px]` (matches Figma spec) on the content wrapper + `flex-1` on step bodies so space distributes without the sheet resizing. Empty space inside a step is better than a moving control |
| ScanStep Back/Cancel buttons pushed to bottom with `mt-auto` | In a fixed-height container, CTAs belong at the bottom (Figma spec + convention). `mt-auto` pushes the button row down after QR content renders naturally from top |
| LowCreditsBanner uses status-alert tint + Button primary (accent purple), not a gradient | Wireframe showed a pink/purple gradient "Add credits" button, but WSUP primary actions are solid `bg-accent`. Using Button primary keeps the banner consistent with every other CTA in the app. The status-alert tint communicates urgency; the action button stays in the app's visual vocabulary |
| LowCreditsBanner "Add credits" dispatches `wsup:open-credit-sidebar` CustomEvent | Header owns CreditSidebar state. Prop-drilling or context for one cross-cutting action is overkill. CustomEvent decouples banner from sidebar — any component can trigger the sidebar by dispatching the event, Header just listens once |
| Banner context copy: "10 credits left · about 3 replies" (not just "10 credits left") | Raw number is meaningless to a user who doesn't know cost-per-action. Translation to user-visible impact ("about 3 replies") is what creates the right urgency — without needing aggressive styling to compensate for weak copy |
| BuyCreditsSheet step 3 is viewport-split: QR on desktop, deep-link on mobile | Asking a mobile user to scan a QR with their phone IS the phone. The QR is a device-to-device bridge; on mobile it's asking the user to point the phone at itself. Mobile step 3 replaces QR with a "Open wsup app →" deep-link + "Don't have it? Get the app" fallback. Desktop keeps QR. Shell + steps 1-2 remain identical |
| Viewport split uses scanVariant prop, not a new FlowBody per viewport | FlowBody already runs inside viewport-gated BottomSheet + CenterPopup. Passing `scanVariant: 'qr' \| 'app'` as a prop keeps everything in one FlowBody with a 2-line branch. Alternative (separate Mobile/Desktop FlowBody files) doubles code for one differing step |
| Mobile step 3 "Open wsup app" uses Button primary, NOT the wireframe's gradient | Wireframe showed a purple-pink gradient button. WSUP has established solid-accent primary CTAs across the entire app (including earlier in this same flow). Gradient would break consistency for one screen. Brand gradient is already expressed via the featured pack's golden button — adding another gradient here competes for attention |
| BuyCreditsSheet gets a `result` step (success + failure variants) | User pays in the wsup app, returns to web expecting confirmation. Without it, they don't know if the top-up went through. Result step uses status-success/status-alert tinted icon + heading + contextual pill or error copy + single CTA (Back to chat / Try again). Style guide has a toggle to preview both variants |
| FinishInAppStep uses `/app-icon.png` instead of `/logo.png` | Designer added a proper 256×256 character-forward app icon. `logo.png` is the wordmark. An icon represents the app in an OS context (like a tile); a wordmark identifies it in a header. Right asset for the right role |
| Header returns a Fragment — CreditSidebar rendered as sibling, not child | Header has `fixed z-50` which creates a stacking context. Nesting CreditSidebar (z-60) inside trapped its z-index as RELATIVE to Header's context, not global. BottomNav (z-50 at document root) was painting on top because CreditSidebar was effectively "at z-50" globally. Fragment-sibling places CreditSidebar at document root where z-60 wins |
| Checkbox extracted after 2 usages (payment method + T&C) | Same 16×16 tick primitive was inlined twice in PaymentStep. Extracted to `src/components/ui/Checkbox.tsx` with `variant: 'primary' \| 'success'` prop — primary for interactive T&C-style toggles, success for "this option is selected" visual indicators. Replaces the awkward bg-status-success inline indicator AND the native `<input type="checkbox">` with one consistent primitive |
| BuyCreditsSheet lifted from CreditSidebar to Header — two CustomEvents | `wsup:open-credit-sidebar` opens the sidebar (balance hub); `wsup:open-buy-credits` opens the purchase flow directly. LowCreditsBanner's "Add credits" now skips the sidebar detour — user wants to buy, not browse rewards. CreditSidebar's "Buy Credits" button also dispatches the buy-credits event. Two trigger points, one top-level owner (Header), zero prop-drilling |
| PaymentStep icon: `/app-icon.png` not `/logo.png` | `/logo.png` is the wordmark (white-on-transparent) — on a white-bg tile it renders invisible. `/app-icon.png` is the OS-style app identity tile with its own background. Use the right asset for the container |
| InfoIcon extracted to `src/components/ui/InfoIcon.tsx` | Gate 3 at 2 consumers — CreditSidebar and DormancyBanner both had inline SVGs with slightly different implementations (path vs circle for the dot). Unified to single component with `size` and `className` props; consumers pass color/hover behavior via className |
| CreditSidebar text-xxs → text-xs sweep | Multiple lowercase subcopy uses at 10px violated the 12px-minimum rule: streak day pills, Daily check-in "Earn 10", footnote, creator activity "X unique chatters", "Deposited" pill. "Yesterday's payout" and "Lifetime Earnings" converted to `label-xs` (uppercase + 10px is the allowed exception for category labels) |
| Transaction History + "How creator rewards work" use `.link` utility | Both were using `text-accent-light` (purple #9d76ff). Established link convention is `.link` class → `text-secondary` (#82a1ff blue), always underlined. Same fix pattern applies to any inline text link across WSUP |
| CreditHero pill uses `bg-credit-bg` + `border-credit-premium-border` tokens | Previously inline hex `#2d220c` bg and `#f4da5c` border. Tokens existed (`credit-bg: #2b1f0e`, `credit-premium-border: #f4da5c`) — swapped. Small bg color shift (2d220c → 2b1f0e) is imperceptible and keeps the design system consolidated |
| CreditSidebar Claim CTAs `size="xs"` → `size="s"` | Violated the "Card CTAs use size S, not XS" rule (decisions.md) — xs is too slim for tappable card actions. Applies to daily check-in, RewardRow (Daily/One-Time Rewards). Bumped heights from ~20px to ~30px — better tap target, still compact |
| Streak footnote stacked vertically instead of justify-between | At 365px sidebar width with text-xs (12px), "Day 3 · Tomorrow · Get +15" and "Miss a day? Streak resets." both wrapped when laid out horizontally. Don't widen the sidebar (intentional width) — stack vertically so each gets its own line. Also trimmed "Day 3 ·" redundancy (streak state is shown above) |
| Streak InfoIcon gets the same className treatment as SectionTitle's InfoIcon | Both info icons in CreditSidebar are interactive (hover, cursor), same color (text-white-40). After extracting to shared InfoIcon, the Streak usage lost its className and rendered bright-white via inherited `currentColor`. Fix: consumer-owned className pattern — each usage site declares interaction + color explicitly |
| Rewarded/Purchased breakdown hidden by default, revealed via "Show breakdown" toggle | Primary info is the total — 350. The rewarded-vs-purchased split is useful context, not primary. Showing both always makes the hero taller than needed and buries the total. Toggle keeps the default view clean; interested users expand. Toggle + Transaction History sit side-by-side under the total as peer actions |
| CloseButton primitive uses `cn` (twMerge) for className merging | Original used raw string concat — passing `text-white-40` would coexist with the default `text-white-90`, leaving Tailwind to pick the last-emitted CSS rule (unreliable). With twMerge the override wins deterministically. Lets the same primitive serve sheets (default), banners (muted color, smaller padding), and sidebars without forking variants |
| BuyCreditsResultStep + DormantCharacterCard rejected-badge are NOT close buttons | Same SVG path (`M18 6L6 18M6 6l12 12`) renders three distinct semantic uses across WSUP: (1) dismiss action, (2) failure status icon, (3) rejected-state badge icon. Don't conflate by shape — only the dismiss-action instances should compose CloseButton. The other two are visual indicators; their X-shape happens to match |
| 4 close-button instances migrated to CloseButton primitive | BuyCreditsSheet StepHeader (exact match), CreditSidebar (exact match), LowCreditsBanner (16px svg, muted color override), DormancyBanner mobile + desktop (custom padding + color overrides). All render identically via twMerge-resolved className overrides |
| CloseButton added to style guide UI Utilities section | Gate 5 (style guide sync) violation discovered during this session — primitive existed since Session 19 but was never documented. Added 3 variants: default 20px, compact 16px, banner muted. Style guide is the contract for "what primitives exist" — missing entries make the contract a lie |
| CoinIcon extracted from inline copies in CreditSidebar + CreditHero | Same 5-line wrapper around `<Image src="/credit.png">` lived in two files. Adding StreakClaimPopup as third use was the trigger to extract to `ui/CoinIcon.tsx`. CreditButton + CreditsSummaryPill keep their direct `<Image>` usage — they apply different classes that the wrapper doesn't expose, so wrapping would force prop bloat. Threshold-driven extraction, not "extract everything" |
| RewardRow extracted from CreditSidebar to `ui/RewardRow.tsx` | The "label + Earn N + Claim button" row pattern existed in CreditSidebar. StreakClaimPopup needs three rows of the same shape. Adding `claimable`, `claimLabel`, `onClaim` props lets the same primitive serve both consumers without forking. CreditSidebar's call sites unchanged (defaults preserved) |
| StreakClaimPopup uses BottomSheet + CenterPopup compound, not custom shells | LoginSheet builds custom mobile/desktop shells because of its 40/60 character-image split — content shape is not symmetric. StreakClaimPopup is a single content block (header → progress → list → CTA), so it composes the existing primitives instead of forking. Less code, automatic ESC + backdrop dismissal, automatic drag handle on mobile |
| StreakClaimPopup keeps default `bg-profile-sheet-bg` (NOT Figma's white@10%) | Figma's source spec used `#ffffff @ 10%` background — this would conflict with WSUP's codified taste rule "BottomSheet, CenterPopup, Popover: solid dark bg, NO backdrop-blur. Frosted glass was attempted and abandoned." Per Gate 7, the WSUP rule wins over the Figma value. Solid surface + layered `bg-white-05` items frame inside achieves the same visual layering reliably |
| CloseButton self-mounted absolute, not via primitive's `title` slot | BottomSheet/CenterPopup render the close button only when a `title` prop is passed, and the rendered header has its own border-divider + layout. Streak popup wants a custom header (title + subtitle row + balance pill on the same row) and no divider, so `title` is omitted and CloseButton is positioned `absolute top-xs right-xs` inside the content wrapper. Header reserves `pr-xxxl` ONLY on the title line so the subtitle row can use full width |
| Streak progress gradient hex matches LoginSheet exactly (`#72e9f1 → #7192e5 → #6257d7`) | Two near-but-different gradients across files would drift. Used the same 3-stop linear gradient that LoginSheet uses for its subtitle text, applied as inline-style `linear-gradient(to right, ...)` because the bar's `width` is dynamic. Currently 2 uses — a 3rd will trigger token extraction (e.g., `--gradient-streak`) per the 3+ rule |
| Streak popup mounts on Explore visit; S-key dev toggle mirrors R-key auth pattern | `S` opens the dev panel ("Streak Popup: Shown / Hidden"), `Shift+S` flips state directly — same shape as R/Shift+R for Auth. Two side-by-side dev panels in fixed bottom-right. Consistency with the established dev-toggle convention beats inventing a new key. Initial state is `open=true` so the popup appears on first explore visit (design-only handoff — real product would gate behind a "first visit per day" flag) |
| StreakClaimPopup row dividers `border-white-05` → `border-white-10` | A row divider at 5% white inside a frame that's also 5% white = same color, zero contrast, divider invisible. Bumped to 10% so the rule is barely perceptible but actually separates rows. Lesson: when stacking translucent layers, each layer's borders need to win against its OWN background, not the page bg |
| StreakClaimPopup items frame got `py-xxs` (4px) | Frame had `px-s` only — no vertical padding. First row's content sat flush against the rounded-card edge, giving a cramped look at the top/bottom. 4px (xxs) is enough to let the rounded corners breathe without inflating section height. Don't reach for `p-s` (12) like CreditSidebar's single-card pattern — that's overkill for a list-of-rows pattern where each row already has `py-xs` |
| StreakBlock extracted to `shared/StreakBlock.tsx` — single source of truth for the streak system | The Figma popup's "1x/2x multiplier progress bar" was a stale design language — the live product had since shipped a 4-day pill grid (Day 1/2/3-6/7+) inside CreditSidebar with a Daily check-in inset card and a "Tomorrow · Get +N / Miss a day? Streak resets" footnote. Two places (sidebar + popup) showing two different streak metaphors confuses the user about what their streak actually is. Promoted CreditSidebar's inline block to a shared component, replaced the popup's progress bar with it. Now the popup IS the sidebar's streak — same visual language, same numbers, same vocabulary |
| StreakClaimPopup drops "Daily Check-in" from the secondary rewards list | Daily Check-in is INSIDE StreakBlock (it's the inset card under the header strip). Listing it again as a row in the items frame would be a double-render — same action shown twice. Popup's secondary rewards are now scoped to non-streak earn paths only (Chat with any character, Create a post). Rewards prop is empty-allowed; if `rewards.length === 0` the secondary frame collapses entirely |
| StreakClaimPopup props shape changed to mirror StreakBlock | Replaced `streakDay`, `daysToLevelUp`, `currentMultiplier`, `nextMultiplier`, `progress` (5 multiplier-system props) with `streakDay`, `streakTiers`, `tomorrowReward`, `dailyCheckInEarn`, `dailyCheckInClaimable`, `onClaimDailyCheckIn` (6 day-grid props). Caller now describes the streak in the same vocabulary as CreditSidebar — easy to reuse the same data on both surfaces |
| StreakBlock day-pill grid made optional via `tiers` prop; popup omits it | The 4-tier pill row (Day 1 / Day 2 / Day 3-6 / Day 7+) is a *progression preview* — useful in the credit hub (CreditSidebar) where users browse where they're headed, redundant in the action-focused popup where the user is one tap away from claiming. Same component, two intents: full variant in the hub, compact variant in the popup. Implemented by making `tiers` optional + a `showTiers` guard inside StreakBlock — no separate component, no prop bloat. Popup also dropped its own `streakTiers` prop entirely since the popup never wants tiers |
| StreakClaimPopup auth-gates Claim via the BuyCreditsSheet `gateAction` pattern | Every Claim button (Daily check-in + each secondary reward) routes through one local `gateAction(action)` helper. If logged in: action fires. If not: action is stashed in `pendingClaim`, LoginSheet opens with `Sign in to claim` / `So your credits go to the right account.`. On sign-in, pending action runs. On LoginSheet close (decline), pendingClaim is dropped — user stays unclaimed and can re-click any Claim or close the popup. Once claimed, the button flips to secondary `Claimed` + disabled. Internal state (`dailyClaimed`, `claimedIds`) resets when popup closes — fresh demo each visit. Pattern is identical to BuyCreditsSheet's gating; copying the pattern beats inventing a new one |
| RewardRow + StreakBlock auto-swap "Claim" → "Claimed" when not claimable | Disabled-but-still-says-Claim is ambiguous — looks like a broken button. Auto-deriving the label from the `claimable` flag (no caller-side branching) keeps the consumer call sites compact and the visual contract clean. Caller can still override with explicit `claimLabel` if a different post-action word is needed elsewhere |
| StreakClaimPopup is now strictly streak-only — secondary rewards prop removed | "Chat with any character", "Create a post" etc. are *other* earn paths, not streak claim. They live in the credit hub (CreditSidebar's Daily Rewards / One-Time Rewards sections). Mixing them into the streak popup made the popup do two jobs at once. Removed the `rewards` / `onClaim` props, the secondary frame, and the RewardRow import. The popup now has exactly one action: claim today's check-in |
| StreakBlock got a `bare` prop; popup uses bare variant | When StreakBlock is the only content of a popup, the outer `rounded-card bg-white-05 border p-m` wrapper becomes a redundant 2nd surface (sheet → card → check-in inset = 3 layers). Bare drops the wrapper so the streak content sits directly on the sheet — 2 surface layers (sheet + check-in inset). CreditSidebar keeps the wrapper because it's one of many sibling cards there. Same component, two chrome levels, prop-driven |
| Streak popup reorganized into 3 explicit zones — header / action / exit — with a hairline between action and exit | Was: title row + STREAK strip + check-in card + dual-line footnote + link, all flowing as siblings under one gap-m. Felt cramped because four distinct text streams (subtitle, strip, footnote-tomorrow, footnote-miss-a-day) each pulled the eye in a different direction. Now: header is one zone (title + subtitle/balance row), action is one zone (DailyCheckInCard with DAY badge + single-line "Tomorrow · Get +N" footnote), exit is one zone (Explore link). Hairline divider (-mx-l, full-width) between action and exit. The "Miss a day? Streak resets." warning was dropped — loss-aversion belongs in the credit hub where users browse streaks, not at the moment of claim |
| STREAK header strip dropped from popup; "DAY N" moved into the check-in card as a small gold uppercase label | The strip "STREAK | DAY 3 | CHECK-IN REWARD" is a *section identifier* — useful in CreditSidebar where Streak sits among Daily Rewards / Creator Rewards / One-Time Rewards. In a popup with only one section, the strip duplicates the title's intent. Day status moved inside the check-in card (label-xs uppercase + credit-gold) so it stays visible without an extra row |
| DailyCheckInCard extracted to `ui/DailyCheckInCard.tsx` — shared by StreakBlock + StreakClaimPopup | Same row pattern (label + Earn N + Claim button, all in a `bg-white-05 rounded-button p-s` inset) lived inline in both StreakBlock and the popup's bare variant. Promoted to a shared primitive with an optional `dayBadge` prop — when provided, renders the small uppercase "DAY N" label above the row. Sidebar uses it without dayBadge (header strip carries that info); popup uses it WITH dayBadge (no header strip). One source of truth for the card visuals |
| Gate 5 backfill — RewardRow + DailyCheckInCard added to style guide via new "Reward Cards" section | Self-audit caught that two primitives extracted earlier in this session (RewardRow on the first popup build, DailyCheckInCard on the zone-reorganization pass) never got style guide entries. Both have multiple consumers (RewardRow → CreditSidebar; DailyCheckInCard → StreakBlock + StreakClaimPopup), so they cross the "shared primitive needs to be discoverable" threshold. New `components/RewardCardsSection.tsx` shows three DailyCheckInCard states (default, with dayBadge, claimed) and three RewardRow states (claimable, claimable, claimed) with copy explaining when to use each. Lesson: post-edit gate audit catches what mid-edit attention misses |
| StreakClaim login-gate copy is benefit-forward, not account-hygiene | Old copy mirrored BuyCreditsSheet ("Sign in to claim" / "So your credits go to the right account.") — but the two flows have different mental states. BuyCredits: user is paying, "right account" is a real concern. StreakClaim: user is in "give me my reward" mode, "right account" reads as friction. New copy: **"Sign in to claim your credits"** (specific, action-bound) / **"Unlock free credits and start chatting."** (declarative benefit). Subtitle drops the "Sign in to" / "Log in to" prefix to avoid doubling the CTA verb. WSUP convention "Sign in" used everywhere (only `Login with Google` keeps Login per Google's button norm) |
| BuyCreditsSheet gate copy rewritten to user-benefit framing — and consolidated into one constant pair | Same lens applied to the paid flow on second thought: old subtitle "So your credits go to the right account" was defensive ("we need this from you") and implied the *risk* of the wrong account rather than the benefit of any account. New copy: **"Sign in to continue"** (unchanged headline — works for all 4 payment progressions) / **"Keep your credits across every device."** (portability framing, user-benefit voice). Bonus discovery during the edit: 4 call sites had drifted into TWO different subtitles ("So your credits go to the right account." vs "So we know which account to credit.") plus headline punctuation drift ("Sign in to continue" vs "Sign in to continue."). Consolidated all 4 to share `GATE_HEADLINE` + `GATE_SUBTITLE` constants at the top of BuyCreditsSheet. Single source of truth — the next copy change touches one line, not four |
| ContextExhaustionPrompt anchored above ChatBar, not pinned at top of chat like DormancyBanner | Two distinct chat-banner positions emerged. Top-of-chat (DormancyBanner) is for *character state* — what the user needs to know about *who* they're talking to. Above-ChatBar is for *session state* — what the user needs to know about *this particular conversation*. Memory exhaustion is session-scoped, not character-scoped, so it lives at the composing surface where the user is creating the very content that will get forgotten. Two anchors, two roles, no conflict |
| Install prompt uses InfoIcon + bg-white-05, not status-alert tint like LowCreditsBanner | LowCreditsBanner uses `bg-status-alert/[0.10]` + pulsing red dot — that copy is a real urgency ("about to run out"). Install prompt is informative, not alarming — memory continuity is a benefit pitch, not a warning. Visual treatment matches: neutral `bg-white-05` + static `InfoIcon`. Same anatomy (icon + text + CTA + close), different urgency layer |
| Install prompt headline interpolates character name via `buildHeadline(name)`, subtitle stays generic | `${name} will start to forget the earlier parts of your story.` makes the pitch about *this* relationship, not about the abstract product feature. Subtitle "Install the app to keep the full conversation." stays character-agnostic — same action regardless of who the user is talking to. Both are overridable via props for A/B copy variants |
| Variant copy as props from day one (headline, subtitle, ctaLabel) — not hardcoded | The PRD already flags "tune copy with the actual app-side context-window size" as an open question, and the A/B framework will want copy variants. Building the component with copy props from day one means a variant test or post-launch tuning is a one-line caller change, not a component edit. Same pattern as LoginSheet's `headline`/`subtitle` props |
| Install action via `wsup:open-install-prompt` CustomEvent | Same pattern as `wsup:open-credit-sidebar` and `wsup:open-buy-credits` — banner doesn't need to know about deep-link mechanics. Production code attaches the actual handler (App Store / Play Store / universal-link) at one place. `onInstall` prop overrides for the style guide demo and edge cases |
| Install prompt dev toggle uses `I` key, mirrors R/Shift+R + S/Shift+S pattern | Chat already uses R for character state cycling. Each new dev toggle on a page gets its own letter with the same shape (key = panel toggle, Shift+key = state flip). I = Install. Two side-by-side dev panels in fixed bottom-right. UX consistency rule: copy the existing pattern, don't invent a new shape. Two-state cycle: Hidden (control) vs Shown (treatment) — mirrors the A/B variant the PRD specifies |
| Install prompt re-mounts via `key` bump when dev toggle flips | Component owns its own dismissed state (matches LowCreditsBanner pattern). Once user clicks X, internal `dismissed=true` hides it. To re-show it via dev toggle, parent bumps a `key` prop forcing a fresh mount with `dismissed=false`. Cleaner than lifting dismiss state to parent — keeps consumer API tight, dev affordance lives only in the dev wrapper |
| ChatBar wrapped in flex-col container so prompt + ChatBar move as a unit | Added `<div className="shrink-0 flex flex-col gap-s px-m pb-s md:px-0 md:pb-0">` around ChatBar when prompt is enabled. When ChatBar expands to 2 rows on focus, the prompt rides up with it (they're siblings in the same flex column). When mobile keyboard opens, the whole unit slides above the keyboard. Free positioning behavior — no absolute positioning, no sticky math |
| ~~Mobile horizontal padding (`px-m pb-s`) on chat-bottom group, removed on desktop (`md:px-0 md:pb-0`)~~ → superseded | First implementation wrapped both prompt AND ChatBar in a `px-m` flex container "for shared mobile edge padding." Wrong: ChatBar's own outer wrapper already declares `px-m md:px-2xxxl py-s` — the new wrapper double-padded ChatBar, narrowing its pill on mobile. *Designer caught this* before the broken layout shipped further |
| Prompt + ChatBar are siblings, not nested — prompt has its own padding wrapper that *mirrors* ChatBar's outer padding | After the fix: `<>{installPromptOpen && <div className="shrink-0 px-m md:px-2xxxl"><ContextExhaustionPrompt /></div>}<ChatBar /></>`. Each element owns its own horizontal padding using the same token scale, so they appear visually aligned on both mobile (`px-m`) and desktop (`px-2xxxl`). Vertical gap between them comes for free from ChatBar's own `py-s` top padding — no `gap-s` needed on the parent |
| **Rule:** when adding a sibling above a known component, never wrap the existing component to share padding — mirror its padding scale on the new sibling instead | Wrapping a component you didn't author silently breaks any internal padding/positioning that component already depends on. Even when the wrapper looks "additive" (just an extra `px-m`), it stacks with the wrapped component's own padding and corrupts the layout. The safe move is *coordinate via shared tokens, not via shared wrappers*: each sibling reads the same spacing token, the visual alignment falls out, and the original component is untouched |
| ContextExhaustionPrompt surface `bg-white-05` → `bg-black-60` to match DormancyBanner's chat-banner-over-character-bg pattern | First implementation used `bg-white-05 + backdrop-blur-popup` thinking "informative surfaces are subtle" — but mobile chat has a full-bleed character image as the bg, and white-05 (5% white) over a colorful image gives near-zero contrast. The card looked unanchored, like a ghost. *Designer caught this on the mobile screenshot.* DormancyBanner already solved the same problem with `bg-black-60 + backdrop-blur-popup` (line 58 of DormancyBanner.tsx) — the rule was already codified, the install prompt just hadn't applied it. **Lesson: when introducing a new chat-bound surface, grep DormancyBanner's mobile classes first** — it's the precedent for "card over character image" |
| **Rule:** chat banners over character bg need `bg-black-60 + backdrop-blur-popup`, NOT translucent white | "Subtle = white at low alpha" works on plain dark surfaces (page-bg). On chat mobile, where the bg is a saturated character image, white-at-low-alpha disappears. Black-at-medium-alpha + backdrop-blur produces a readable card that doesn't feel alarming and respects the character image (you can still see it through the blur). This is now the WSUP default for any new mobile chat-anchored surface — match DormancyBanner |
| ContextExhaustionPrompt rewritten to character-voice quote + dual-path CTA (Option C) | Designer ran a "deep UX think" review on first-shipped copy and called out three problems: (1) prompt explained the symptom ("Billie will start to forget") without the cause, reading as either confusing or manipulative; (2) hid the real in-product alternative (switching to a higher-memory LLM, available right now on web for credits) — railroading users into install when they have other options is a manipulation pattern that costs trust *and* leaves credit-spender revenue on the table from non-installers; (3) hierarchy was inverted — emotional pitch was visually heavier than the action. New copy: italic quote *"I can only remember so much of one chat." — Billie* (character-voice owns the system limit, turning it into a character moment) + body "The app holds ~3× more memory, so this story stays whole." (concrete ~3× number replaces vague "full conversation") + dual action: primary `[Get the app]` Button, secondary `Switch model →` text link |
| **Rule:** when the product has a real alternative to the primary CTA, show it as visually-secondary, never hide it | Hiding a real alternative is a dark pattern even when the alternative is paid (credits) and the hidden path is the revenue-driver (install). Two costs: (a) trust — users who later discover the alternative read the original prompt retroactively as deceptive, and WSUP is a returning-user product not a one-shot funnel; (b) conversion — users who can't install today (work device, storage, just-not-now) have only "install or dismiss" as options. A visible secondary path captures them as *paid model-switchers* instead of dismissers. Hierarchy stays clear via affordance weight: primary = Button, secondary = `.link` text — the eye still picks the button first |
| Headline copy is character-voice, italicized, with attribution | Owning the system constraint in the character's voice ("I can only remember so much of one chat." — Billie) makes the limit feel like a feature of the character (her memory has a shape) rather than a system failure (something is broken). Italic signals "this is dialogue, not UI copy" — same convention as chat italic action descriptions ("*She blinks, smiles*"). Attribution `— Billie` uses `text-text-small` (60%) so the name fades and the quote pops. The whole headline lives in `text-xs text-text-body italic` |
| CTA label "Install the app" → "Get the app" | "Install" is technical (action verb describing OS-level mechanic). "Get" is conversational (action verb describing user intent). Same affordance, friendlier voice. Save "install" for OS-level moments where the user is in app-store flow already |
| ~3× memory number moved from PRD open-question to component default | PRD's open question — "tune copy with actual app-side context-window size" — was treated as a future copy edit. But concrete number is the actual sell. Moved into `DEFAULT_BODY` constant; if the real number changes, the constant updates in one place. Caller can still override per-instance via `body` prop for A/B variants |
| Secondary action uses `.link` utility class on a button element | Visual treatment matches WSUP's inline link convention (`text-secondary` underlined with `decoration-white-20`) but the element is a `<button>` because the action doesn't navigate to a URL — it dispatches `wsup:open-model-picker`. Reset button defaults via inline classes (`bg-transparent border-none p-0 cursor-pointer`). The arrow `→` suffix reinforces "go somewhere" semantics. Same pattern as Removed-card "Contact Support" link-styled-as-button |
| `secondaryLabel=""` hides the switch-model action | Some surfaces won't have a model-switch alternative (character locked to specific model, unauthenticated user, etc.). API: pass empty string to hide rather than a separate `showSecondary` boolean — keeps prop surface minimal and the behavior locally readable at the call site |
| ContextExhaustionPrompt restructured into 3 explicit zones — title / body / action — with proper text hierarchy | First version compressed everything into `p-s` (12px) padding with `gap-s` (12px) inner spacing and uniform `text-xs` for both quote and body. Designer flagged: "everything compacts to a small area without proper breathing space" — no real hierarchy between title, body, and CTAs. Restructured: `p-m` (16px) outer padding, `gap-m` (16px) between zones. Title is now `text-sm text-text-title italic` (14px, 100% white) — bumps the quote into actual title territory, distinct from body. Attribution `text-xs text-text-small` on its own row with `gap-xxs` from quote. Body stays `text-xs text-text-body` but with `leading-relaxed` for comfortable reading. Pattern matches sheet/popup hierarchy (title-zone, body-zone, action-zone) which WSUP already uses for BuyCreditsSheet, StreakClaimPopup, ConfirmSheet |
| **Rule:** chat-anchored cards with significant emotional/decisional weight should follow sheet hierarchy (title 14px / body 12px / explicit zones), not banner hierarchy (uniform 12px) | Banner pattern (LowCreditsBanner, DormancyBanner mobile pill) uses uniform `text-xs` because the content is short and the action is single — the banner is a status indicator, not a decision surface. When a chat card asks the user to make a meaningful decision (install vs switch model, with copy that explains *why*), it stops being a banner and starts being an in-line sheet. Sheet hierarchy applies: bigger title, body distinguishable from title via size + color, action zone separated from body via gap-m. Don't shoehorn decision-prompts into banner anatomy — pick the pattern that matches the *role*, not the *position* |
| `headline` prop replaced with `quote` + `attribution` separate props | First API used a single `headline: ReactNode` prop where caller had to pass the full quoted text + attribution span as JSX. Verbose and error-prone — caller had to know the styling conventions (italic on quote, text-text-small on attribution). New API: `quote: string` and `attribution?: string` (defaults to `— {characterName}`). Component owns the styling. Caller passes plain strings. Same affordance with cleaner ergonomics |
| **ContextExhaustionPrompt deleted entirely; replaced with MemoryLimitMoment** (in-chat pattern, not floating card) | After 4 rounds of iteration on the floating-card design (typography, hierarchy, dual-path, copy), designer asked me to rethink completely as a UX designer. Real diagnosis: the floating card was an *interrupt pretending to be ambient* — it demanded attention but didn't justify the interruption with a moment. It also broke immersion to talk about preserving immersion: the system was speaking *about* Billie *to* the user, severing the character relationship in the very moment we were trying to extend it. Refining typography on a structurally wrong design just made the wrong thing more refined. New pattern: the AI's NEXT response *is* the memory moment — Billie pauses in her own voice ("Wait... what were we just talking about?"), and a small system action card attaches below offering Get-the-app / Switch-model. Lives in the chat scroll, not above ChatBar. Two pieces: stripped AI bubble (chat-ai-bubble surface, no toolbar, italic emotion + speech) + system action card (bg-black-60 + backdrop-blur, info icon, brief system text, Button + .link, X dismiss) |
| Stripped AI bubble for the moment — no toolbar, no thumbs/regenerate | Regular AI bubbles include audio-play, thumbs-up/down, regenerate, dots-vert as a feedback toolbar — they're chat MESSAGES the user wants to interact with. The memory-limit moment is a *one-time event*, not a regular response — regenerating a memory failure is meaningless. Stripping the toolbar keeps focus on the moment itself + the action card below. Same `bg-chat-ai-bubble` surface so it visually reads as Billie speaking; just shorter |
| System action card sits BELOW the bubble, visually distinct surface | Putting the install/switch action *inside* Billie's bubble would imply Billie is selling the app — wrong voice. Putting it on a separate surface (`bg-black-60 + backdrop-blur` per the chat-overlay rule) signals "this is the system speaking, not the character." Visual connection via `gap-xs` (8px) below the bubble — same flex-col container, so they read as one moment with two voices |
| MemoryLimitMoment dismissable on the action card only — bubble stays in chat history | Dismissing X hides the system action card (the *offer*), but the bubble (the *character moment*) persists in chat scrollback. Reasoning: the moment was real — Billie did experience the limit — and erasing the bubble would erase a fact about the conversation. The offer is dismissible because the user can choose to engage or not. Bubble is permanent because the event is permanent |
| Wired via `showMemoryLimit` prop on ChatMessages, replacing TypingIndicator | The moment fires *as Billie's response* to the user's last message. So it slots into the same position as TypingIndicator — the next-AI-response slot. Cleaner than appending a separate component to the chat container. ChatMessages now accepts `showMemoryLimit?: boolean` + `characterName?: string` props |
| MemoryLimitMoment unified into ONE bubble with internal divider — not two stacked pieces | First version: bubble + separately-styled system action card with a gap between them. Designer flagged "both feels disconnected or separated from each other" — the visual gap + different surfaces (chat-ai-bubble vs bg-black-60) made the moment read as two unrelated events. New version: single `bg-chat-ai-bubble` container, internal hairline divider (`border-t border-white-10 mx-s`) separating the character-voice section (italic emotion + speech) from the system section (info icon + explanation + CTAs + dismiss X). Same surface, same rounded shape, same horizontal alignment — reads as ONE moment with two voices. Voice differentiation comes from typography, not surface |
| System text now answers "WHY install the app" — was a "what happened" announcement | Old copy: "Billie's memory of this chat just hit its limit." — describes the event but doesn't explain why the install solves it. The CTA "Get the app" had no reason attached. New copy: "Billie's memory just hit its limit. The app remembers ~3× more — so your whole story stays with her." Three facts: WHAT happened, WHY the app helps (~3× more memory), BENEFIT (story stays with her). User now reads the buttons in context: install = your story stays with Billie, switch model = different memory option |
| **Rule:** when offering a CTA, the same artifact must answer "to do what" — never let the user infer the why from the button label alone | A "Get the app" button without preceding context is a faith-based CTA — the user has to guess what the app does for them. Conversion drops because the user can't tell if the action is worth it. Fix: the system/explanatory copy adjacent to the button must explicitly state the benefit (not the feature). "The app remembers ~3× more — so your whole story stays with her" is benefit-shaped (your story stays). "The app holds ~3× more memory" is feature-shaped (the app has a number). Lead with the user's outcome, not the product's spec. **Pre-flight question for any CTA:** *if a user only read the system text + button label, would they know what the action does for them?* If no, the system text is missing |
| Secondary action restructured as inline-link sentence on its own row, matching profile/explore link convention | First version had `[Get the app] · [Switch model →]` side-by-side as button + button-styled-link. Designer asked for "the same style as link text in profile/explore" and a separate row with a real "why." WSUP convention (DormancyBanner "Revive", DormantCharacterCard "Contact support", CharacterStatesSheet "help center") = inline `.link` element inside a sentence, no arrow, sentence-case. New layout: `[Continue to chat in app]` button on its own row, then a separate sentence row: "Or [switch to a higher-memory model] — uses credits". The link is just the verb phrase; "Or" prefix bridges from system text; "— uses credits" suffix discloses the trade-off so user knows the cost before tapping |
| Switch-model link drops the `→` arrow to match profile/explore convention | Previous version had a button styled with arrow ("Switch model →") which is the action-CTA-link pattern (Earn → / See more →). But the designer asked for the *inline reference link* style used on profile/explore (Contact support, help center, Revive — none of those have arrows). Switching the convention: this is now an inline reference link inside a sentence, not a standalone CTA button. Cleaner, and matches the established WSUP grammar |
| MemoryLimitMoment auto-fires 3s after page load on /chat | Designer asked for the prompt to appear by default (not just dev-toggled). Behavior: page loads in `active` state; a `setTimeout(3000)` in chat/page.tsx flips to `context-exhausted` only if the state is still `active` (so manual dev-cycling doesn't get overridden). Combined with the slide-up-fade entrance animation, simulates the real production experience where memory limit hits mid-conversation |
| New `slide-up-fade` keyframe in globals.css for the entrance animation | Existing `slide-up` keyframe goes from `translateY(100%)` (full element height) which is too aggressive for a small in-chat artifact. New keyframe: `translateY(16px) → translateY(0)` + opacity 0→1 simultaneously, 0.4s ease-out. Subtle pop-in that draws the eye without feeling jarring. Applied via inline `style={{ animation: 'slide-up-fade 0.4s ease-out' }}` on the bubble's outer div — re-plays every time the component mounts (e.g., when dev-cycled away and back) |
| Decided AGAINST adding an alert emoji (⚠️) before the system text | Designer asked whether to prefix the system text with an alert emoji. Rejected for three reasons: (1) violates the codified taste rule "Warnings are informative, not alarming" — emoji ⚠️ pushes the artifact from informative into alarming, undermining the careful work to make the moment feel like part of the conversation; (2) the divider line + 70%-opacity text + character voice context already does the voice differentiation we removed the InfoIcon to simplify; (3) emojis on system text aren't a WSUP precedent — would invent a new pattern. Logged as a "considered and rejected" decision to prevent re-litigation in future sessions |
| X dismiss now closes the WHOLE moment (bubble + system section), not just the system section | First version had the X dismiss only the system section, leaving Billie's italic emotion + speech bubble as a permanent orphan in chat history. Designer correctly flagged this: an orphan bubble without a toolbar (unlike normal AI bubbles) and without context reads as a half-broken chat message in scrollback. Mental model for an X is "make this go away" — users expect the whole artifact to dismiss. Component now does `if (dismissed) return null` at the top — entire moment unmounts on dismiss. In production, server-side state can persist the memory-limit-hit event independently if needed |
| **Rule:** when an artifact has a dismiss affordance, dismissing should remove the *whole* artifact, never half of it | A half-dismiss leaves a UI fragment in chat/scrollback that reads as broken — the user's mental model for X is "make this go away," not "minimize it." If you need to keep PART of an artifact alive after dismissal (e.g., for chat history), that's a server-side persistence concern, not a UI render concern. Visually, dismiss = full removal. Don't leave orphan fragments behind |
| MemoryLimitPopup added as parallel variant alongside MemoryLimitMoment — different *roles*, both kept | Designer wanted an alternative "modern interactive" variant: animated WSUP gradient surface, character DP overlapping the top edge in B&W, exclamation burst SVG top-right, character-voice title, sleek inset card explaining the model context (Llama 3), benefit body, full-width CTA, divider, switch-model link. This is NOT a replacement for the inline MemoryLimitMoment — it's a *higher-interrupt* variant for contexts that need more weight (model-specific framing, paid-user contexts, install-conversion-priority moments). Both shipped, dev-toggle cycles to either via R-panel: `Memory full (inline)` vs `Memory full (popup)` |
| Animated gradient surface for the popup variant | Subtle motion signals "this is a premium / brand moment, not a generic system alert." Gradient: `linear-gradient(135deg, #6257d7 0%, #7192e5 30%, #db315e 70%, #fc5372 100%)` — the LoginSheet purple (#6257d7) bleeds into the exclamation-burst pink (#fc5372), tying the icon's color to the surface palette. New `gradient-drift` keyframe in globals.css: position shifts from 0% 50% → 100% 50% → 0% 50% over 12s ease-in-out infinite. `background-size: 200% 200%` makes the position shift visible. Soft `bg-black-40` overlay layered on top for legibility — gradient is *under*, not *competing with*, the text |
| Character DP overlaps top edge in B&W (grayscale) | The DP poking out the top is the visual hook — signals "this is about a specific character, not a generic system event." B&W (grayscale filter) ties to the memory-loss theme: the character is "fading," her color drained from the chat the user is losing. White ring (`ring-2 ring-white-90`) defines the circle against the gradient surface. 80px size, positioned `-top-[40px]` (half above the popup edge) |
| Exclamation burst SVG saved as static asset (`/public/icons/icon-exclamation-burst.svg`) | Designer-provided SVG with multiple internal colors (pink/purple outline, yellow/orange "!" inside, white highlights) — not a currentColor monochrome icon. Saved as static asset rather than inlined React component because (a) the file is large and (b) it's reusable as a brand decoration token. Used via plain `<img>` tag (Next.js `<Image>` not needed since the SVG renders at any size). Positioned `absolute top-s right-s` |
| Inner sleek card explains the *model context* — bridges "why am I seeing this" gap | The popup adds a layer the inline MemoryLimitMoment doesn't: a small `rounded-card bg-black-50 backdrop-blur-popup border border-white-10` inset that names the LLM ("Llama 3 (free)") and explains its limit. This educates the user on the *system reality* — they're on a free LLM with limited context, switching to a better model OR going to the app are both real paths. Without this card, the popup just says "memory full → install" — the user doesn't learn anything about the system. WITH this card, the user understands the choice they're making |
| Backdrop click dismisses, X dismisses, both flow through the same handleDismiss | Standard modal dismiss UX. Backdrop is `bg-black-70 backdrop-blur-popup` to blur out the chat behind. X button positioned top-LEFT (not top-right) to avoid colliding with the exclamation burst SVG in the top-right corner |
| Popup variant only accessible via dev cycle, not auto-fired | Auto-fire (3s setTimeout) only triggers the inline variant (MemoryLimitMoment). The popup is a designer/PM-driven alternative — it requires explicit selection. Reasoning: auto-firing TWO different variants would confuse the demo; one is the "default" (inline), the other is the "alternative" (popup). Dev panel cycles to either |
| MemoryLimitPopup restructured: chat-anchored above ChatBar, NOT fullscreen modal | First version was `fixed inset-0` with full-page backdrop. *Designer caught:* "I wanted it like above chatbar only like floating popover." Re-positioned as a chat-anchored element (same slot the old ContextExhaustionPrompt was in) with `max-w-[420px] mx-auto` so it centers within the chat column. No backdrop, no fullscreen — sits above ChatBar like a contextual card. Lower interrupt cost while still being more visually elaborate than MemoryLimitMoment |
| Popup background: dark base + subtle WSUP gradient overlay (NOT full WSUP gradient as bg) | First version had the full WSUP gradient as the popup's primary background — felt overwhelming, brand-flooded. *Designer caught:* "the bg color can be our usual color but I wanted wsup color subtle gradients on it, not completely wsup color gradient as bg." Restructured: base = `bg-profile-sheet-bg` (the standard WSUP dark surface), with a low-opacity (~30%) gradient overlay applied via `linear-gradient(135deg, rgba(98,87,215,0.32) 0%, transparent 38%, transparent 62%, rgba(252,83,114,0.28) 100%)` — hint of brand purple at top-left and brand pink at bottom-right, transparent in the middle. The animated drift is now subtle tinting, not color flooding. Surface stays "WSUP dark" with brand whisper at the corners |
| DP cropping bug — outer wrapper was `overflow-hidden`, clipping the half-pokeout | First version had `overflow-hidden` on the popup wrapper (because of rounded corners + gradient bg). DP positioned `-top-[40px]` got clipped — only the lower half showed. *Designer caught:* "the profile image of AI is getting cropped inside container so out of the box portion is not even visible." Fix: outer wrapper has NO overflow-hidden; only the *inner* rounded card has `overflow-hidden` for the gradient surface. DP is positioned at the outer-wrapper level, so it pokes out cleanly above the inner card edge. **Rule:** when an element needs to overlap a rounded-clipped container's edge, position it on a parent that's NOT overflow-hidden, never inside the clipped container itself |
| Close X moved from top-LEFT to top-RIGHT to match WSUP convention | First version had close at top-left to "avoid colliding with the exclamation in the top-right corner." *Designer caught:* "the close icon is at left it should be on right only like all other popovers or menus we have on WSUP." WSUP convention is unambiguous — close is always top-right (BottomSheet, CenterPopup, ConfirmSheet, Popover all top-right). I deviated to solve a self-created collision (exclamation in popup's top-right corner) — the right fix was to move the exclamation, not the close. **Rule:** never break a strong UI convention to solve a collision you created — solve the collision differently |
| Exclamation burst moved from popup top-right corner → DP top-right (badge style) | First version had the exclamation SVG at the top-right corner of the popup (where Close X belongs per convention). *Designer caught:* "the exclamation was suppose to come on the top right of ai character black and white profile picture top right." Restructured: exclamation is now a 32px badge positioned `-top-[6px] -right-[6px]` on the DP circle. This makes more semantic sense — the alert *belongs* to the character (it's about her memory), so the badge attaches to her DP, not floats orphan in the popup chrome. Also frees up the popup's top-right for the conventional Close X |
| **Rule:** alert/status badges attach to the *subject* of the alert, not to the surface chrome | An exclamation/warning indicator should sit on the thing it's *about* (a DP, a card, an icon), not orphaned in a corner of the surrounding container. When the alert subject is identifiable (the character whose memory hit the limit), badge it directly — top-right is the universal badge corner (notification dots, status pills, exclamation badges all live there). This frees the surface's top-right corner for the universal close-X convention. Two top-right corners, two different scopes — the badge is scoped to the subject, the close is scoped to the surface |
| MemoryLimitPopup overlays chat bubbles instead of pushing them up | First in-chat-anchored version sat in the flex column above ChatBar — which pushed chat content upward to make room. Designer flagged: "is this pushing the chat bubble above? it shouldn't... it can have bg gradient helping it overshadow the chat bubbles behind it." Restructured the wire-in: popup wrapper around ChatBar is `relative shrink-0`, popup is `absolute bottom-full left-0 right-0` so it floats *above* ChatBar without taking flex space. Chat bubbles flow naturally; popup overlays the area just above ChatBar. Outer overlay wrapper uses `pointer-events-none` and the inner popup uses `pointer-events-auto` — chat content not covered by the popup remains scrollable/interactive, only the popup itself captures clicks |
| Popup bg changed from solid `bg-profile-sheet-bg` → translucent `bg-black-60` + `backdrop-blur-popup` | Solid dark bg fully covered chat behind the popup; designer wanted the chat bubbles to stay *visible* (just overshadowed) so the popup feels like it's floating over a continuous chat surface, not blocking it. `bg-black-60` (60% opaque black) + `backdrop-blur-popup` lets chat bubbles show through dimmed and blurred. Combined with the WSUP gradient overlay (purple top-left, pink bottom-right) at low opacity, the chat behind reads as "context that's there but currently overshadowed." Same pattern as the chat-banner-over-character-bg rule — translucent bg + backdrop-blur is WSUP's standard for "ambient overlay over chat content" |
| **Rule:** chat-anchored overlays should never push chat content upward — use absolute positioning so chat flows naturally underneath | Pushing chat upward to make room for an overlay creates a layout shift the user reads as "this is part of the conversation," forcing them to scroll up to see context they were just looking at. Floating an overlay (absolute, bottom-full of the ChatBar wrapper) preserves chat flow — the overlay is clearly *additive*, not *integrated*. Combine with translucent bg + backdrop-blur for the "overshadowing" effect that signals "ambient overlay" without fully hiding chat |
| Backdrop halo glow added behind the popup — bleeds beyond the popup edges into chat | Designer asked: "should we add a bg gradient overlay to help this popover come out from behind more naturally." Without a halo, the popup appeared "pasted on" — there was no visual transition between popup edge and chat behind. Added an absolute-positioned div at `-inset-16` with a radial WSUP gradient (purple → pink → transparent) and `filter: blur(28px)` — it extends ~64px beyond the popup edges in all directions, fading into transparency. Effect: the popup feels like it emerges *from* a soft branded glow rather than appearing pasted onto chat. The blur makes the halo edges indistinct, so the gradient feels like ambient light, not a hard tinted area |
| Replaced single-position-shifting linear gradient with multi-layer "wavy blob" animation | Designer feedback: "the gradient animation is subtle but I was thinking more modern and mixed wavy kinda subtle animation in this only one color moves slowly. kinda too bland." First version was a static linear gradient with `background-position` shifting 0% ↔ 100% — technically animated but visually flat and uninteresting. Replaced with three composited gradient layers: (1) static purple radial tint at top-left corner; (2) static blue radial tint at bottom-right corner; (3) a single wavy-drifting pink radial blob (220px circle) that translates around the surface in a 5-point figure path with subtle scale variation. New `blob-drift` keyframe uses `transform: translate(...) scale(...)` for the path. Effect: one color moves in a more organic wave-like arc while the others hold position — the surface reads as alive without busy multi-color motion |
| **Rule:** for "alive but subtle" surface animation, animate ONE element on a wave/figure path — keep other elements static | A surface where every gradient layer animates feels noisy. A surface where ONE layer moves on a non-linear path (figure-8, wavy arc, irregular polygon) feels organic and intentional. Static anchor layers give the eye stable reference points; the moving element is the focal motion. Keyword: animate the *transform* (translate + scale), not the gradient stops or background-position — transforms are GPU-accelerated and produce smoother motion than position shifts on background-image |
| Halo glow → vertical-fade scrim (matches WSUP popup convention) | First version added a halo glow `-inset-16` around the popup. Designer corrected: that's not how WSUP popups work — they use a vertical-fade dark scrim (black-70 at bottom → transparent at top). Replaced halo with a 420px-tall scrim positioned `absolute bottom-full` above ChatBar in chat/page.tsx. Provides the dim backdrop that makes the popup pop, while still letting chat above stay visible/scrollable. Halo glow is a custom invention; vertical-fade scrim is the codified pattern |
| Popup bg: `bg-black-60 + backdrop-blur` → `bg-profile-sheet-bg` solid (matches BottomSheet/CenterPopup/Popover convention) | Designer asked: "this popup bg color is that preexisting in other popups too?" The answer was no — `bg-black-60 + backdrop-blur` was a custom translucent treatment I introduced to let chat show through the popup. But WSUP convention (codified in earlier session decisions: "BottomSheet, CenterPopup, Popover: solid dark bg, NO backdrop-blur. Frosted glass was attempted and abandoned") is solid `bg-profile-sheet-bg`. Switched to solid. The chat-darkening concern is now solved by the scrim *outside* the popup, not by translucency *inside* the popup. Cleaner separation: scrim handles the chat dim, popup handles its own opaque surface |
| Animation rethink — single drifting blob → two pulsing memory motes themed for "AI memory fading" | Designer feedback: "the glow blob and animation needs to be different too i don't like this rethink it. for a AI character platform saying memory full." First version was a generic single-color drifting gradient — not thematic, just modern-looking motion. Rebuilt as two soft orbs (purple top-left, blue bottom-right) inside the popup, each on a different opacity-pulse + drift cycle (11s and 14s, both ease-in-out infinite). Asymmetric timing creates an organic "two thoughts dimming and brightening" feel — themed for an AI character's memory fading. The two orbs occasionally cross-fade, giving the surface a contemplative, melancholy mood without being alerting. Single element drifting was generic; two elements pulsing asymmetrically reads as *intentional theme*: thoughts, memories, fading consciousness |
| **Rule:** animation should serve the *meaning* of the artifact, not just its modernness | A popup about "memory full" with generic drifting gradient motion has motion but no theme. A popup with two orbs pulsing/dimming asymmetrically *means something* — it visualizes the thing the popup is about (memory fading, thoughts dimming). For UI moments with strong semantic content (memory limit, login, payment success, error), pick animation primitives that visually echo the meaning: dimming for fading, ascending for success, dispersing for error/loss, pulsing for waiting. Don't reach for "modern motion" generically — reach for motion that *says the thing* |
